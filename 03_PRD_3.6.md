# PRD — 충남 서해 연안 고수온 설명 서비스

| | |
|---|---|
| 버전 | **3.6** |
| 작성일 | 2026-07-31 |
| 프로토타입 | 2026-07-31 |
| 최종 마감 | **2026-08-27 (목)** |
| 개발 | Claude Code (맥북). 코드는 직접 작성하지 않음 |
| 배포 | GitHub |

**3.6에서 바뀐 것 — 프로토타입을 실제로 만들면서 나온 정정 다섯.**

2026-07-31 프로토타입(지도·설명·정보 세 화면 + 견본 A·B·C)을 구현하면서
**문서만 봐서는 드러나지 않던 것**이 다섯 나왔다. 화면이 이미 그렇게 돌고 있으므로
문서를 맞춘다.

1. **§5·§11 요인 막대에 임계선과 라벨 명세를 넣었다.** §5 목업이 두 마커를 모두
   임계선 오른쪽에 그려 놨는데, 일사(87.3)는 임계 90에 못 미치므로 왼쪽이어야 한다.
   **§9가 견본 A를 고른 이유와 그림이 반대였다**(§14 #16).
2. **임계선 라벨 문구를 `기준 {상위 N%}`로 정했다.** `90`이라고 적으면 옆의
   `상위 13%`와 숫자가 커지는 방향이 반대라 비교가 안 된다(§11, §14 #17).
3. **§11에 요인 영역 표시 규칙을 넣었다.** 채택 그룹이 하나도 없을 때
   그룹을 나열하지 않는다. `ⓘ` 안내는 막대가 있는 화면에만 띄운다.
4. **§8에 보류일 필드 규약을 넣었다.** `groups: []`, `current_sst`·`climatology_mean`·
   `anomaly`는 `null`, 최상위 `climatology` 블록과 `daily_max_sst`는 담는다(§14 #18).
5. **§5 지도 해역명 `(추정)`을 여섯 곳 모두에 붙인다.** 창리도 예외가 아니다.

**3.5에서 바뀐 것 — 구조 변경 둘.**

1. **§10 검사기: AI가 숫자를 쓰지 않는다.** 자유 문장을 생성하고 정규식으로 항목을 특정하던 방식을 폐기하고, AI는 자리표시자만 쓰고 렌더러가 값을 넣는 방식으로 바꿨다. 문형 패턴표·우선순위·절 경계·단위 검사·표본 금지·한글 수사 금지가 전부 불필요해졌다(§10, §14 정정 #13).
2. **§13 지표: 침묵 일치율 신설.** 이 서비스의 주장은 "더 잘 맞춘다"가 아니라 "모를 때 모른다고 말한다"인데, 지표가 그걸 재지 않아 더미 A′가 구조적으로 이기게 돼 있었다. 판정을 네 갈래로 바꾸고 대조군 20% 게이트를 보고 지표로 내렸다.

그 밖에: `evidence.json` 갱신 아키텍처 신설(§12), "시각이 두 개인 이유" 복원(§8·정정 #11),
ASOS 결측 구간 날짜 규약 정정(§7·§13·정정 #12), 정보 화면 진입 경로 명세(§5·§11),
평년값 창 경계·결측률 20% 근거·30분→1시간 영향을 미확정 항목으로 승격(§16),
견본 E(음수 anomaly) 추가 예정, 철회 체크리스트 신설(§12).

<details>
<summary>이전 판 변경 이력</summary>

**3.4:** `anomaly` 절댓값 대조, 패턴 경합 규칙, 탐욕적 탐색 금지, 특보 기준값 `3` 복원,
우선순위 0 항목명 단서, 한글 기간어 금지, 요인 일치율 판정 규칙 신설, 8/3 후퇴선.
*(이 중 검사기 관련 항목은 3.5에서 접근 변경으로 대부분 폐기됐다. §14 #13 참조)*

**3.3:** 금지 표현·인과 단정·순위 표현 검사 표 복원(3.2 회귀), 특보 기준값 패턴 최우선,
`24시간 평균` 고정 문구 허용, 백분위 요인별 격리, `…` 절 단위 명시,
입력본에서 `daily_max_sst` 제외, 정보 화면 목업 위아래 분리.

**3.2:** 단위 검사·연속일수 패턴·한글 수사 금지, 표본 일수 문장 금지,
평년 대비 분기를 표기값 기준으로, 정보 화면 목업, 일정·자를 순서 반영.

**3.1:** 정보 화면 신설(3면 구조), 지도 배경 교체, 범위 표 분리와 확장 조건 열,
천수만 특보 이력 표, 2026년 운영 검증 분리, 줄임말 폐지, 견본 B `stage` 정정.

</details>

**검토했으나 반영하지 않은 것:** 채점용 구간 분리 폐지(2026년으로 대체) —
2026년은 마감까지 최대 105일로 2022~2025의 556일에 비해 얇고 자료 확보도 불확실하다.
2022년을 개발용으로 옮기는 안 — 얻는 것에 비해 재계산 비용이 크다(§13).

---

## 0. 서비스의 위치

이 서비스는 **경보를 울리지 않는다.**
어민은 기상청·수산과학원에서 이미 특보를 받는다. 그 다음에 생기는 질문이 있다.

> **"왜 이런 거지?"**

이 서비스는 그 질문에만 답한다.
따라서 "공식 특보를 대체하지 않는다"는 면책 문구가 아니라 **설계 원리**다.

**그리고 원인이 없을 때는 없다고 말한다.** 이것이 이 서비스의 두 번째 주장이며,
§13의 채점 지표는 이 주장을 재도록 설계되어 있다(침묵 일치율).

대상 지점은 **충남 천수만의 서산 창리(fsch6)** 한 곳이다.
창리는 서산시 부석면의 포구다. **수산과학원 고수온속보와 해양수산부 보도자료가
충남 연안 수온을 인용할 때 쓰는 관측점**이며(2017년 제1호, 2022년 제65호),
천수만 행에 창리 값이 실린다. 다만 속보 원문에 `천수만(창리)` 형태의 공식 병기가
있는지는 확인되지 않았다(§16). 확인 전까지 "대표 관측점"이라는 단정은 쓰지 않는다.

---

## 1. 용어

| 말 | 뜻 |
|---|---|
| **평년값** | 지난 몇 해 이맘때는 보통 몇 도였나. **직전 5년**(모자라면 있는 만큼, 3년 미만이면 판정 안 함) |
| **롤링** | 평년값 구간이 매년 한 칸씩 밀리는 방식 |
| **백분위** | 평년 분포에서 **아래에서 몇 %인지.** 백분위 90 = 상위 10% |
| **이상군 / 대조군** | **일 최고수온**이 28℃ 이상인 날 / 미만인 날. **§2 참조** |
| **판정 대상일** | 요인 백분위를 계산할 수 있었고 보류되지 않은 날. 채택률의 분모 |
| **오검출(헛방)** | 대조군인데 요인을 채택하는 것 |
| **정당한 침묵** | 속보도 원인을 특정하지 못한 날에 시스템도 채택하지 않은 것. **§13 참조** |
| **골든 케이스** | 손으로 미리 계산해둔 **정답 견본** |
| **분석 기간** | **우리가 정한** 감시 구간. 5/15 ~ 9/30 (139일). §2 참조 |
| **특보** | 수산과학원 발표. 예비특보 → 주의보 → 경보 |
| **위기경보** | 해양수산부가 발령하는 **다른 체계**. 관심 → 주의 → 경계 → 심각 |
| **ASOS** | 기상청 지상 관측소(서산 129). 일사량·기온 |
| **RISA** | 수산과학원 **실시간 해양수산환경 관측시스템**(웹). 과거 수온 CSV |
| **실시간어장정보** | 공공데이터포털 **API**. 실시간 수온. RISA와 같은 기관이지만 별개 창구 |
| **자리표시자** | AI 문장에서 숫자 대신 쓰는 `{current_sst}` 같은 표시. 렌더러가 값으로 바꾼다. **§10 참조** |

---

## 2. 두 개의 28℃ — 용도를 갈라 쓴다

**같은 28℃가 두 곳에서 쓰이고, 기준 수온이 다르다. 섞으면 안 된다.**

| 용도 | 기준 수온 | 필드 | 왜 다른가 |
|---|---|---|---|
| **화면 단계 판정** | **일평균** (`current_sst`) | `status.stage` | 하루를 대표하는 안정적인 값. 순간 변동에 흔들리지 않는다 |
| **채점 분류 (이상군/대조군)** | **일 최고수온** (`daily_max_sst`) | 채점 스크립트 | **이상군 표본을 확보하기 위한 우리의 선택이다.** 수산과학원 특보 문구는 "수온이 28℃에 도달하였거나 도달할 것으로 예상되는 경우"로, 일평균인지 일 최고인지 명시돼 있지 않다 |

**두 값을 모두 근거 파일(완성본)에 담는다.** 하나만 담으면 다른 하나를 계산할 수 없다.
`daily_max_sst`는 입력본에서 제외한다(§8).

**이상군은 특보 발령 여부로 정의하지 않는다.** 관측값 기준이라 재현 가능하고,
2022~2025 특보 이력이 아직 전량 확보되지 않았기 때문이다.
특보 이력은 §13의 **요인 일치율·침묵 일치율**에만 쓴다.

### 분석 기간은 우리가 정한 것이다

**5/15 ~ 9/30, 139일.** 이 값은 수산과학원 대책기간을 참고했으나,
**수산과학원 대책기간이 매년 동일한지는 확인되지 않았다.**

따라서 이 기간은 **외부 사실이 아니라 이 프로젝트가 정한 분석 구간**으로 규정한다.
연도마다 같으므로(5~9월은 윤년과 무관하게 139일) 모든 계산의 기준으로 쓸 수 있다.
국립수산과학원 기간과 다르더라도 우리 계산은 흔들리지 않는다.

### 이 기간이 실제 특보를 다 담는가 (1주차에 채운다)

선언만 하고 검증이 없으면 임의로 정한 것처럼 보인다. **천수만 해역의 연도별
첫 특보일과 마지막 해제일을 조사해 이 표를 채운다.**

| 연도 | 첫 특보 | 마지막 해제 | 분석 기간 안인가 |
|---|---|---|---|
| 2017 | (1주차) | | |
| 2018 | | | |
| 2019 | | | |
| 2020 | | | |
| 2021 | | | |
| 2022 | | | |
| 2023 | | | |
| 2024 | | | |
| 2025 | | | |

**반드시 「천수만 해역」으로 한정한다.** 공개된 2018년 7.24~9.4, 2019년 8.8~8.29,
2020년 8.14~9.4, 2021년 7.15~8.26은 **전국 기준**이다.
2021년만 봐도 천수만은 7.23 주의보 확대 이후이고 8.4에 경보가 발령됐다.
§9 견본 A의 날짜를 틀렸던 것도 같은 이유다(§14 #11).

**결과가 불편해도 그대로 적는다.** 어느 해 특보가 5/15보다 이르거나 9/30보다 늦었다면
우리 분석 기간이 특보를 놓친다는 뜻이다. 숨기지 않는다.

이 작업은 §16의 "2022~2025 특보 발령 이력 확보"와 같은 작업이므로 한 번에 한다.

---

## 3. 자료 구간

| 구간 | 연도 | 쓰임 | 볼 수 있나 |
|---|---|---|---|
| 2016 이전 | — | **쓰지 않는다.** 창리 관측 개시가 2017-06-13 | — |
| **평년값 재료** | 2017~2019 | 평년 분포 계산에만. 이 해의 근거 파일은 만들지 않음 | 봄 |
| **개발용** | 2020~2021 | 근거 파일 생성 가능. 견본·골든 케이스는 전부 여기서 | 봄 |
| **채점용** | **2022~2025** | 4주차까지 성능을 보지 않음 | **제한적** |
| **운영 검증** | 2026 | **채점용이 아니다.** 실시간 파이프라인이 실제로 도는지만 본다 | 봄 |

**2026년을 채점용으로 쓰지 않는 이유.** 분석 기간이 마감(8/27)까지 최대 105일로
2022~2025의 556일에 비해 얇고, 자료 확보 경로도 불확실하다 —
지금까지 쓴 RISA 과거자료는 연도별 zip이라 2026년치는 시즌이 끝나야 올라올 가능성이 높고,
실시간 API가 창리를 주는지는 8/3에야 확인된다.
**자료 확보 자체가 불확실한 구간을 채점 근거로 삼을 수 없다.**
8/3 확인이 실패해도 프로젝트가 죽지 않도록 별도 항목으로 둔다(§12 후퇴선).

**분할의 목적.** 채택 임계를 90퍼센타일로 사전 고정했으므로 보정할 대상은 없다.
분할이 필요한 이유는 하나다 — **채점 결과를 본 뒤 기준을 바꾸고 싶어지는 것을 막기 위해서.**

### 채점 구간을 열어본 기록 (원칙)

**성능 정보(채택률·일치율)는 4주차까지 보지 않는다.**
그 외의 접촉은 전부 여기 적는다.

| # | 무엇 | 성능 정보인가 |
|---|---|---|
| 1 | 롤링 평년값을 정할 때 2022~2025의 수온 상승을 근거로 삼음 | **문제 있음. 아래 참조** |
| 2 | 채점 구간의 표본 규모를 셈 (분석 기간 총 556일) | 아니오 |
| 3 | 2022년 제65호 속보의 천수만 행에 창리 값이 실려 있음을 확인 | 아니오 |
| 4 | 요인 일치율 정답지로 2024·2025년 원인 서술을 확보 | 아니오 |
| 5 | **(예정)** 1주차에 2022~2025 특보 발령 이력 확보 | 아니오 |
| 6 | 평년값 계산에 채점용 연도가 재료로 들어감 (2024년 → 2019~2023) | 아니오. 운영상 정상 |
| 7 | **(예정)** 채점용 이상군 일수를 셈 — §13 유의성 판단에 필요 | 아니오. 표본 크기만 |

**1번은 문제다.** 창리의 2017~2021만 보면 추세가 없다.

| 2017 | 2018 | 2019 | 2020 | 2021 |
|---|---|---|---|---|
| 25.59 | 23.43 | 23.75 | 23.00 | 23.69 |

(2017년이 높은 것은 6월 13일 개시로 5~6월 냉수기가 빠진 편향이다. 관측일도 110일뿐이다.)

**따라서 롤링 채택의 근거를 이 지점 자료가 아니라 공개된 기후 자료로 바꾼다.**

> 수산과학원은 2024년 우리 바다 연평균 표층수온이 관측 사상 최고치였다고 발표했다.
> 과학조사선 관측 기준 18.74℃로, 이전 최고였던 2023년 18.09℃보다 0.65℃ 높다.
> (§16에 발표 제목·일자를 각주로 병기할 것)

기후 추세는 이 프로젝트가 새로 발견할 사실이 아니라 이미 확립된 전제다.
임계값이나 요인 목록을 채점 결과에 맞춰 조정한 것은 아니므로 검증 자체는 유효하다.

### 롤링 평년값이 추세에 어떻게 반응하는가 (3주차에 측정)

바다가 더워지는 중이라면 롤링 5년 창에는 서로 반대인 두 힘이 함께 작용한다.

| 힘 | 방향 |
|---|---|
| 현재 연도가 직전 5년 평균보다 구조적으로 높다 | 백분위가 **올라간다** → 채택률 상승 |
| 창이 매년 따라 올라가 편차를 흡수한다 | anomaly가 **작아진다** → 채택률 하락 |

**어느 쪽이 이기는지는 문서로 답할 수 없고 측정해야 한다.**
개발용 구간에서 연도별 대조군 채택률의 추세를 확인하고 §13에 적는다.
이 결과가 실패 기준 해석에 직접 영향을 준다 — 채점용에서 대조군 채택률이 높게 나왔을 때
그것이 시스템 결함인지 기후 신호인지를 가르는 유일한 근거다.

---

## 4. 사용자와 진입 경로

| | |
|---|---|
| 주 사용자 | 천수만 양식 어가, 어촌계 관계자 |
| 기기 | **휴대폰 세로 화면** |
| 진입 시점 | **특보를 받은 직후**, 또는 수온이 걱정될 때 |
| 진입 경로 | 기상청·국립수산과학원 알림 → 검색 또는 브라우저 북마크 |
| 싫어할 것 | 전문용어, 근거 없는 안심, 근거 없는 경고 |

**우리는 사용자를 부르지 않는다. 사용자가 찾아온다.**

**전문용어 검토는 2주차에 한다.** 화면에 `배경장`·`백분위`·`상위 13%`가 나오는데
"싫어할 것: 전문용어"와 긴장 관계다. 문서상으로는 결론이 안 나므로
견본 A·B를 실제 휴대폰 화면으로 띄워놓고 판단한다(§16).
`열유입`은 직관적이지만 `배경장`은 해양학 용어라 우선 검토 대상이다.

---

## 5. 화면 구조

```
        ┌─────────────────────────────┐
        │        ③ 정보               │  특보·위기경보 기준
        └──┬───────────────────┬──────┘
           │ ▲                 │ ▲
           │ │ 뒤로            │ │ 뒤로
           ▼ │                 ▼ │
  ① 지도 ────────클릭────────▶ ② 설명
        ◀────────뒤로─────────
```

**①과 ② 모두에서 정보 화면으로 갈 수 있고, 정보 화면에서는 왔던 화면으로 돌아간다.**
진입 요소는 두 화면 오른쪽 위의 `ⓘ 특보란?` 버튼이다. 목업에 표시했다.

### ① 지도 — 충남 서해 연안 관측점

| 지점 | ID | 좌표 | 해역(잠정) | 상태 |
|---|---|---|---|---|
| **서산 창리** | fsch6 | 36.6163, 126.3717 | **천수만** | **설명 제공** |
| 서산 지곡 | sj086 | 36.8935, 126.3524 | **가로림만** | 설명 준비 중 |
| 태안 안면도 | btai5 | 36.5369, 126.2981 | 천수만 | 설명 준비 중 |
| 태안 내포 | btni5 | 36.4672, 126.4394 | 천수만 | 설명 준비 중 |
| 태안 고남 | br001 | 36.4158, 126.4333 | 천수만 | 설명 준비 중 |
| 보령 삽시도 | bbsi5 | 36.3714, 126.3383 | 천수만 입구 | 설명 준비 중 |

**해역 분류는 아직 확정이 아니다.** 지곡을 가로림만으로 본 근거는
"태안군 내리 여도 서단 ~ 서산시 지곡 비경도 동단 = 충남 가로림만"이라는 해역 정의인데,
이는 **저수온** 특보 해역 고시에서 가져온 것이고 고수온 해역 구분이 같다는 보장이 없다.
출처와 고수온 적용 여부를 §16에서 확인한다.

**확인 전까지 지도에 해역명을 단정적으로 쓰지 않는다.**
`가로림만(추정)` 형태로 표시하고, 1주차 확인 후 괄호를 뗀다.
"검증하지 않은 숫자를 띄우지 않는다"는 원칙은 이름에도 똑같이 적용된다.

**여섯 곳 모두에 붙인다. 창리도 예외가 아니다.**
창리의 천수만 소속은 §0의 속보 인용이라는 근거가 있다. 그러나 한 곳만 괄호를 떼면
**"창리는 확인됐고 나머지는 아니다"라는 구분을 화면이 주장하게 된다.**
해역 구분 자체가 1주차 확인 대상이므로 전부 붙이고 한꺼번에 뗀다.

**설명 화면 머리말과 불일치가 하나 남는다.** §11의 머리말은
`충남 천수만 · 서산 창리`로 단정형이라, 같은 지점이 지도에서는 `천수만(추정)`,
설명 화면에서는 `충남 천수만`으로 보인다. 1주차 확인이 끝나면 저절로 사라지지만,
**확인이 늦어져 시연을 먼저 하게 되면 §11 머리말도 함께 고친다.**

**설명하지 않는 지점에는 수온을 표시하지 않는다.** 검증하지 않은 숫자를 화면에 띄우면
그 숫자에 책임이 생긴다. **시간이 남아도 하지 않는다** — 원칙이지 시간 문제가 아니다.

### 지도 배경 — 개념도에서 실제 지형으로

**7/31 프로토타입은 개념도를 쓴다.** 저작권이 확인되지 않은 이미지를 넣지 않는다.

**출처와 좌표는 1주차에 확정하고, 교체 작업은 3주차에 여유가 있으면 한다.**
좌표→픽셀 변환은 어긋나면 바로 보이는 작업이라 시간이 든다.
**자를 순서 1순위이므로 여유가 없으면 개념도를 유지한다.**
대상 사용자가 중장년 어민이므로 개념도로는 자기 양식장 근처를 알아보기 어렵다는 것이
교체를 시도하는 이유다.

| 방식 | 채택 |
|---|---|
| 지도 서비스 실시간 호출 | **안 한다.** API 키 필요, 오프라인에서 화면 사망 |
| **정적 이미지 1장을 저장소에 커밋** | **채택.** 키 불필요, 오프라인에서도 뜸 |

**저작권 조건.** 카카오맵·네이버지도·구글맵 화면 캡처를 공개 저장소에 올리는 것은
재배포에 해당해 이용약관 위반이다. 출처가 깨끗한 것만 쓴다.

- 브이월드(국토교통부 공간정보 오픈플랫폼) — 출처 표시 조건
- OpenStreetMap — ODbL, 출처 표시
- 국립해양조사원 / 국토지리정보원 공개 자료

**화면에 출처를 표기한다**(§11).

**좌표 → 픽셀 변환.** 이미지의 네 모서리 위경도(bounding box)를 §16에 못 박고
변환식을 고정한다. 개념도는 대충 배치해도 됐지만 실제 지도는 어긋나면 바로 보인다.

### ② 설명 화면

```
← 지도로                              ⓘ 특보란?

충남 천수만 · 서산 창리
2021년 7월 31일 일평균

28.5℃
평년보다 2.2℃ 높습니다
[ 주의보 기준 도달 ]

수온: 7월 31일 일평균 (48회 관측)
      7월 31일까지 2일 연속
요인: 7월 30일까지 자료

지금 평년을 벗어난 것

열유입
  일사량 (7일)              상위 13%
  ├──────────────────●─┊────┤
               기준 상위 10%
  기온 (7일)                 상위 9%
  ├───────────────────●┊────┤
               기준 상위 10%

배경장
  판정 기준에 못 미칩니다

함께 보는 조건
  ● 조석   소조기 — 바닷물 섞임이 약해지는 시기입니다

  ⓘ 가로 위치는 평년 분포에서 얼마나 바깥인지를
    나타냅니다. 원인 기여도가 아닙니다.

[쉬운 말] [자세히]
  …

이 서비스가 답하지 않는 것
· 며칠 더 갈지 예측하지 않습니다
· 안전한지 위험한지 판정하지 않습니다
· 공식 특보를 대신하지 않습니다

평년값: 최근 4년 · 표본 172일
기후평년값(30년) 기준에는 못 미칩니다
관측점 한 곳의 자료이며 해역 전체를 대표하지 않습니다
공식 특보는 국립수산과학원 발표를 따르십시오
```

**연속일수 줄은 `stage`가 `advisory` 또는 `warning`일 때만 나온다**(§11).
위 목업은 견본 A(`advisory`, 2일)이므로 표시된다.

### 요인 막대 — 채우는 바가 아니라 트랙 위의 점 하나

진행 바를 쓰면 "일사량이 13% 기여했다"로 읽힌다. 화면이 상시 노출하는
`원인 기여도가 아닙니다`를 **그림이 정면으로 반박하게 된다.**

| 요소 | 사양 |
|---|---|
| 스케일 | 백분위 **0~100**을 트랙 폭에 선형 대응 |
| 마커 | 백분위 위치에 점 하나. 트랙을 채우지 않는다 |
| 임계선 | `adoption_threshold.percentile` 위치에 세로선 하나 |
| 임계선 라벨 | `기준 {상위 N%}` (§11) |
| 오른쪽 | `상위 N%` |
| 아래 | 요인 값 + 단위 |

**스케일을 50~100으로 확대하지 않는다.** 견본 A의 87.3과 90.7은 3.4밖에
차이 나지 않아 막대에서 거의 붙어 보이는데, 그게 정직하다.
**판별은 임계선과 숫자가 하고, 막대는 인상만 준다.**

**임계선은 마커보다 위에 그리고 마커 밖으로 빼야 한다.**
견본 A의 기온은 백분위 90.7로 임계선(90)에서 0.7% 떨어져 있다.
폭 374px 화면에서 **2.6px**이므로 지름 12px짜리 마커가 선을 통째로 덮는다.
**임계선을 그렸는데도 §9가 견본 A를 고른 이유가 화면에서 사라진다.**
2026-07-31 프로토타입에서 실제로 발생했고 픽셀 좌표로 확인했다(§14 #16).

### ③ 정보 화면 — 특보와 위기경보

어민이 "왜 이런 거지?"를 묻기 전에 "이게 뭐지?"가 하나 더 있다.
특보와 위기경보는 발령 주체도 단계 이름도 다른데 같은 날 함께 보도된다.

| | 고수온 특보 | 고수온 위기경보 |
|---|---|---|
| 발령 | **국립수산과학원** | **해양수산부** |
| 단계 | 예비특보 → 주의보 → 경보 | 관심 → 주의 → 경계 → 심각 |
| 기준 | 수온 (25℃ / 28℃ / 28℃ 3일) | 특보 발령 해역 수. **'경계'는 고수온 주의보 해역 4개 이상**. 나머지 단계는 §16에서 확인 |
| 대상 | 해역 | 국가 대응 수준 |

**아래는 화면에 그대로 나가는 문구다. 개발 메모나 미정 표시를 넣지 않는다.**
1주차에 채우지 못한 항목은 줄 자체를 빼고, `기준 확인일`은 실제 날짜를 넣는다.

```
← 뒤로

특보와 위기경보

고수온 특보는 국립수산과학원이,
위기경보는 해양수산부가 발표합니다.

┌────────────────────────────┐
│ 고수온 특보 · 국립수산과학원   │
├────────────────────────────┤
│ 예비특보   수온 25℃ 도달     │
│ 주의보     수온 28℃ 도달     │
│ 경보       28℃ 이상 3일 지속 │
└────────────────────────────┘

┌────────────────────────────┐
│ 고수온 위기경보 · 해양수산부   │
├────────────────────────────┤
│ 관심                        │
│ 주의                        │
│ 경계   주의보 해역 4개 이상   │
│ 심각                        │
└────────────────────────────┘

※ 두 체계는 단계 수도 기준도 다릅니다.
  서로 짝지어지지 않습니다.
  주의보 하나만 발표돼도 위기경보는
  '주의'일 수도 '경계'일 수도 있습니다.

기준 확인일: 2026-08-07

※ 이 기준은 현재 기준입니다.
  보고 계신 날짜에는 다른 기준이
  적용됐을 수 있습니다.

공식 특보는 국립수산과학원 발표를
따르십시오 → (링크)
```

**설계 조건 다섯.**

1. **전부 정적 텍스트다.** AI가 생성하지 않으므로 §10 검사기 대상이 아니다.
   여기서 AI를 쓰면 "숫자만 쓰고 단정하지 마라" 원칙이 기준값 설명과 정면충돌한다.
2. **기준값은 설정 파일에 둔다.** 예비특보 25℃는 2024년 6월 기준이고 바뀔 수 있다.
   화면에 `기준 확인일: YYYY-MM-DD`를 함께 띄운다.
3. **적용 시기를 함께 적는다.** 개발용 구간(2020~2021)은 25℃ 기준 이전이라
   화면에서 `preliminary`가 뜨지 않는다(§8). 정보 탭은 **지금 기준**을 설명하는 곳임을
   명시하고, 과거 날짜를 보고 있을 때는 그 사실을 안내한다.
4. **출처 링크와 면책을 넣는다.** 정보 탭이 생기는 순간 "여기가 특보를 설명하는 곳"으로
   읽힐 위험이 커진다. `공식 특보는 국립수산과학원 발표를 따르십시오`를 이 탭에도 넣는다.
5. **문구의 출처를 문서에 남긴다.** 화면에서 유일하게 §10 검사기를 안 거치는 부분이라
   근거 관리가 별도로 필요하다(§16).

---

## 6. 범위

### 만드는 것
지도 화면 · 설명 화면 · **정보 화면** · 자료 처리 · **갱신 파이프라인** ·
AI 설명 문장 · 문장 검사기 · 과거 재현 채점

### 설계상 하지 않는 것 (판이 바뀌어도)

| 항목 | 사유 |
|---|---|
| **알림·경보** | §0 설계 원리와 충돌. 우리는 설명만 한다 |
| **예측 기능** | 지속 기간을 예측하지 않는다 |
| **안전 판정** | 안전한지 위험한지 말하지 않는다 |
| 로그인·서비스 내 즐겨찾기 | 브라우저 북마크로 충분 |

### M1 범위 밖 (다음 판에서 가능)

**"안 한다"가 아니라 "무엇이 채워지면 되는가"로 적는다. 이 오른쪽 열이 확장 설계다.**

| 항목 | 왜 이번엔 안 하나 | 무엇이 있으면 되나 |
|---|---|---|
| **다른 지점의 설명** | 지점별 평년 분포를 새로 만들어야 함 | **스키마가 이미 `station` 단위다.** 지점별 `evidence.json` 파일만 추가하면 화면은 그대로 돈다 |
| **저수온 확장** | 겨울 현상이라 검증 구간이 다름 | 설정 파일에 임계값과 작용 방향 자리가 이미 있다. 겨울 평년값만 산출하면 된다 |
| 조석 자동 계산 | 천문조위 계산이 무겁다 | 3분류(`spring`/`mid`/`neap`) 판정 규칙만 정하면 된다 |
| 성층·바람 요인 | **자료가 없다** (§15) | 저층 수온 관측소, 또는 내파수도 부이 5년치(2030년경) |

**지점 확장 구조.** `evidence.json`은 **지점 하나에 파일 하나**다.
`station`은 단일 객체이고 배열이 아니다. 지점이 늘면 파일이 늘고, 지도가 그 파일을 가리킨다.
한 파일에 여러 지점을 담지 않는다 — 실시간 갱신 시 지점마다 갱신 시각이 다르기 때문이다.

### 시간이 남으면
최근 7일 수온 그래프 (창리만)

---

## 7. 요인 구조

| 그룹 | 요인 | 변수 | 출처 | 창 |
|---|---|---|---|---|
| **열유입** | 일사 | **일 합계 일사량의 7일 평균** | ASOS 129 | 7일 |
| | 기온 | **일 최고기온의 7일 평균** | ASOS 129 | 7일 |
| **배경장** | 누적 편차 | 수온 30일 누적 평년편차 | 창리 | 30일 |
| *(조건)* | *조석 위상* | *대조 / 중조 / 소조* | *M1은 손입력* | *—* |

### 조석은 3분류다

| 코드 | 화면 문구 |
|---|---|
| `spring` | 대조기 — 바닷물이 잘 섞이는 시기입니다 |
| `mid` | 조석이 중간 정도인 시기입니다 |
| `neap` | 소조기 — 바닷물 섞임이 약해지는 시기입니다 |

**이상도를 계산하지 않으므로 평년 분포도 창도 없다.** 라벨만 판정한다.
3분류 판정 규칙은 3주차에 확정한다(M1은 손입력).

### 채택 판정

**그룹 대표 백분위 = 그룹 안 요인 백분위 중 최대값.** 이 값이 **90 이상이면 채택.**
사전 고정이며 결과를 보고 바꾸지 않는다.

**3일 창은 쓰지 않는다.** 판정에 쓰이는 값을 3개(일사7·기온7·누적편차)로 유지한다.

### 요인 창은 대상일 전날까지다

수온 대상일을 D라 할 때 **ASOS 요인의 7일 창은 D−7 ~ D−1**이다.
견본 A에서 수온이 7/31이고 `solar.as_of`가 7/30인 것이 이 규칙의 결과다.

**왜 하루 전인가.** 일사량은 하루가 끝나야 확정된다. 오후 2시에 "최근 7일 일사"를
계산하면 오늘치가 없다. 수온은 30분마다 들어오는데 요인은 어제까지다.

**이건 결함이 아니라 이 분야의 성질이다.** 바다는 열이 쌓여 뜨거워지므로
며칠치 누적이 원인이고, 오늘 한나절 일사는 애초에 답이 아니다.
다만 **화면이 그 사실을 정직하게 말해야 한다.**

> 수온: 7월 31일 일평균 · 요인: 7월 30일까지 자료

배경장(수온 30일 누적편차)은 수온 자료라 당일까지 쓸 수 있다.
그래서 `cum_anomaly.as_of`만 대상일과 같다.

### 평년값 — 롤링 5년, 최소 3년

| 보는 해 | 평년값 구간 | 연수 | 판정 |
|---|---|---|---|
| 2017~2019 | — | 0~2 | 근거 파일 만들지 않음 |
| 2020 | 2017~2019 | 3 | 가능 |
| 2021 | 2017~2020 | 4 | 가능 |
| 2022 | 2017~2021 | 5 | 가능 |
| 2023 | 2018~2022 | 5 | 가능 |
| 2024 | 2019~2023 | 5 | 가능 |
| 2025 | 2020~2024 | 5 | 가능 |

**창은 대상일 ±21일 = 43일.** 5년이면 215일, 4년이면 172일, 3년이면 129일.

**창의 경계 처리는 아직 정해지지 않았다(§16, 1주차).**
대상일이 5/15면 창은 4/24~6/5이고, 9/30이면 9/9~10/21이다. 즉 **분석 기간 밖 자료를
쓰는지**가 정해져야 한다. 안 쓰면 경계 근처는 43일이 안 되어 215/172/129가 성립하지 않고,
쓰면 §2의 "분석 기간은 우리가 정한 것"이라는 규정과 어긋난다.
**어느 쪽이든 골든 케이스의 표본 172일이 그 규칙 아래서도 맞는지 재확인해야 한다.**

같은 문제가 개시 연도에도 있다. 창리는 2017-06-13 개시이므로
2020년을 판정할 때 **5월~7월 초 구간은 실질 2년치**다. "3년 미만이면 판정 안 함"이
연 단위인지 일 단위인지도 정해야 한다.

**수온과 ASOS는 같은 연도 창을 쓴다.** 창리가 2017년 개시이므로 2020년을 볼 때는
ASOS도 2017~2019만 쓴다(ASOS에 2016 자료가 있어도 쓰지 않는다).
두 변수가 다른 기간을 기준으로 하면 비교가 성립하지 않는다.

**표본 크기는 변수마다 다를 수 있다.** 견본 A의 경우 기온 172일, 일사 **158일**이다
(ASOS 일사에 결측이 있어 7일 평균이 산출되지 않는 날이 있다).
`factors[].clim_sample_size`에 변수별로 기록한다.

### 7일 창 안에 결측이 있으면 값을 만들지 않는다

보간 금지 원칙이다. 2021-08-12 ASOS 결측 하루가 **판정 대상일 8/13 ~ 8/19**
일곱 날의 7일 평균을 무효화했다.

> **날짜 규약.** 창이 D−7 ~ D−1이므로, 8/12가 창에 들어가는 대상일은
> D−7 ≤ 8/12 ≤ D−1, 즉 **8/13 ≤ D ≤ 8/19**다.
> 3.4까지 이 구간을 8/12~8/18로 적었다(§14 #12). 일수는 7일로 같아 §13의
> 271·250은 그대로지만, **어느 날이 빠졌는지가 하루 밀렸으므로
> 이상군 일수는 재검산이 필요하다**(§13, §16).

이 보간 금지가 실제로 비용을 치렀다는 사실은 §13에 그대로 적는다.

---

## 8. 근거 파일 (`evidence.json`)

화면은 이 파일만 읽는다. 계산은 이 파일만 쓴다.

| 형태 | 내용 | 쓰임 |
|---|---|---|
| **입력본** | `explanation`·`daily_max_sst` **없음** | AI에게 이것만 준다 |
| **완성본** | 입력본 + `explanation` + `daily_max_sst` | 화면과 채점이 읽는다 |

### `status.stage` — 상태 코드 (enum)

| 코드 | 조건 (일평균 기준) | 화면 배지 |
|---|---|---|
| `normal` | 25℃ 미만 | **`특보 기준 미만`** |
| `preliminary` | 25℃ 이상 28℃ 미만 | `예비특보 기준 도달` |
| `advisory` | 28℃ 이상, 연속 3일 미만 | `주의보 기준 도달` |
| `warning` | 28℃ 이상이 **3일 연속** | `경보 기준 도달` |
| `withheld` | 판정 보류 | `판정 보류` |

`warning` 판정에는 `status.days_over_advisory`(28℃ 이상 연속 일수, **일평균 기준**)를 쓴다.
이 값이 3 이상이면 `warning`이다.

**보류일은 연속을 끊는다.** 판정하지 않은 날을 연속에 포함시키면 없는 관측을
있는 것처럼 세는 셈이 된다. §7의 보간 금지 원칙과 같은 이유다.

**`days_over_advisory`는 완료된 날의 일평균으로만 센다.** 실시간에서 오늘은
아직 끝나지 않았으므로 포함하지 않는다. 경보 판정이 하루 늦어지지만 그게 정직하다.

### 보류일에 담는 것과 담지 않는 것

`stage`가 `withheld`인 날은 판정을 하지 않았다. 그런데 평년값처럼
**계산은 되는** 값들이 있다. **담아두면 "왜 있는데 안 쓰냐"는 질문이 생기므로
담지 않는다.** §7의 보간 금지와 같은 취지다.

| 필드 | 보류일 | 왜 |
|---|---|---|
| `groups` | **`[]`** (빈 배열) | 요인을 판정하지 않았다 |
| `conditions` | `[]` | 같은 이유 |
| `status.current_sst` / `_raw` | **`null`** | 결측률이 기준을 넘은 값을 대표값으로 띄우지 않는다 |
| `status.climatology_mean` / `_raw` | **`null`** | 판정하지 않은 날의 평년값은 쓰지 않는다 |
| `status.anomaly` / `_raw` | **`null`** | 위 둘의 차이이므로 함께 `null` |
| `status.days_over_advisory` | `0` | 보류일은 연속을 끊는다 |
| `status.daily_max_sst` / `_raw` | **담는다** | 채점 분류용이며 화면·문장이 쓰지 않는다 |
| 최상위 `climatology` 블록 | **담는다** | 아래 참조 |

**최상위 `climatology` 블록만 예외인 이유.** 한계 블록 7줄의
`평년값: 최근 {n}년 · 표본 {n}일`이 `years`와 `sample_size`를 쓰는데,
그 일곱 줄은 **`stage`와 무관하게 항상 노출**이다(§11).
이 블록은 "그날의 평년값"이 아니라 "평년값을 어떻게 산출하는가"라
판정 여부와 성격이 다르다.

**`status.stage_caveat`는 보류일 화면에 띄우지 않는다.**
"이 값은 관측점 재계산치입니다"인데 **화면에 값 자체가 없다.**
필드는 남기되 렌더러가 `withheld`에서 건너뛴다.

**`normal`의 배지를 "평년 범위"에서 "특보 기준 미만"으로 바꾼 이유.**
조건은 절대 수온인데 문구는 평년 대비 개념이라, 수온 22℃·평년 대비 +2.5℃인 날에
`평년보다 2.5℃ 높습니다` 바로 아래 `평년 범위`가 붙어 서로 반박했다.

**예비특보 25℃는 2024년 6월 이후 기준이다.** 그 이전 기준값은 미확인이다(§16).

| 시기 | 25~28℃ 구간의 `stage` |
|---|---|
| 2024년 6월 이후 | `preliminary` |
| **그 이전** | **`normal`** (배지 `특보 기준 미만`) |

개발용 구간(2020~2021)은 후자다. `normal` 배지를 절대 기준 문구로 바꿔놨기 때문에
25~28℃를 `normal`로 떨어뜨려도 화면이 모순되지 않는다.

### 배지와 §0의 관계 (2주차에 재검토)

화면 중앙에 `[ 경보 기준 도달 ]`이 굵게 뜨는 것은 "경보를 울리지 않는다"는 §0과
긴장 관계다. 게다가 실제 특보 문구는 "28℃에 도달하였거나 **도달할 것으로 예상되는** 경우"인데
이 서비스는 예측을 하지 않으므로, 배지의 "기준 도달"은 **공식 특보와 다른 것을 재현**한다.
`stage_caveat`가 이 차이를 설명하지만 작은 글씨다.

배지 자체를 빼는 안, 색을 쓰지 않는 안, `우리 관측점 재계산: 경보 기준` 같은
한정 문구를 붙이는 안을 실제 화면에서 비교한다(§16).

### `sst_basis` — 수온의 정의 (enum)

| 코드 | 뜻 | 언제 |
|---|---|---|
| `daily_mean` | 그날 48회 관측의 **일평균** | 과거 재현 |
| `rolling_24h` | **최근 24시간 이동평균** (48회) | 실시간 |

**"오늘 00시~현재 평균"을 쓰지 않는다.** 평년값은 일평균의 분포인데
오전 8시의 00~08시 평균은 구조적으로 낮다. 시간대에 따라 평년 대비가 편향되고,
결측률 분모를 48로 두면 오전에는 항상 80% 넘는 결측으로 상시 보류가 된다.
최근 24시간 이동평균은 언제 계산해도 24시간 분량이라 평년 분포와 성질이 같다.

**48이라는 만점은 30분 간격 관측을 전제한다.** §16의 "30분 → 1시간 변환"을
채택하면 만점이 24가 되고 **결측률 정의·20% 임계·견본 C의 39.6%가 전부 재계산된다.**
변환 여부를 3주차가 아니라 **1주차에 결정한다**(§16).

### 항목별 뜻

| 항목 | 뜻 |
|---|---|
| `schema_version` | 스키마 판 번호. **문서 버전과 별개 체계다.** 현재 `1.0` |
| `generated_at` | 이 파일을 만든 시각 |
| `station.*` | 관측점 이름·해역·좌표 |
| `status.observed_at` | 수온 기준 시각. 과거는 날짜, 실시간은 날짜+시각 |
| `status.sst_basis` | 위 enum |
| `status.current_sst` / `_raw` | 수온 (소수 2자리 반올림값 / 원값). **화면 표시값은 렌더러가 만든다** |
| **`status.daily_max_sst` / `_raw`** | **그날 일 최고수온.** 이상군/대조군 분류에만 쓴다. 화면·문장에 쓰지 않으며 입력본에서 제외한다 |
| `status.climatology_mean` / `_raw` | 평년값 |
| `status.anomaly` / `_raw` | 평년 대비. **부호 있음** |
| `status.stage` / `stage_label` | 상태 코드 / 화면 배지 문구 |
| `status.days_over_advisory` | 28℃ 이상 연속 일수 (일평균 기준, 보류일에서 끊김) |
| `status.stage_caveat` | 공식 특보가 아니라는 안내 |
| `data_quality.sst_missing_rate` | 결측 **비율(0~1)**. 화면은 %로 변환 |
| `data_quality.obs_count` | 실제 관측 횟수. 과거는 그날, 실시간은 최근 24시간. 만점 48 |
| `data_quality.judgment_withheld` / `withheld_reason` | 보류 여부 / 사유 |
| `groups[].display_order` | 화면 순서. 값에 따라 바뀌지 않음 |
| `groups[].adopted` | 채택 여부. **자리표시자 화이트리스트를 만드는 기준**(§10) |
| `groups[].representative_percentile` | **그룹 안 요인 백분위의 최대값** |
| `factors[].as_of` | 이 요인이 며칠까지의 자료인지 |
| `factors[].value` / `value_raw` / `unit` | 측정값 2자리 반올림값 / 원값 / 단위 |
| `factors[].percentile` | 백분위 |
| `factors[].clim_sample_size` | 이 변수의 평년 표본 일수 |
| `factors[].window` / `quality` | 창 길이 / `ok`·`partial` |
| `conditions[].label` | `spring`·`mid`·`neap` |
| `adoption_threshold` | 채택 기준값과 동결 기록 |
| `climatology.method` | `rolling_5y_min3` |
| `climatology.years` / `sample_size` / `window_days` / `is_climate_normal` | 연수 / 수온 평년 표본 일수 / 창 반경 / 항상 false |
| `explanation.*` | 생성 문장(자리표시자 원문과 렌더 결과)과 검사 결과 |
| `disclaimer` | 면책 문구 |

### 파일 형태 (완성본)

```json
{
  "schema_version": "1.0",
  "generated_at": "2026-07-31T14:00:00+09:00",
  "station": { "id":"fsch6","name":"서산 창리","area":"충남 천수만",
               "lat":36.6163,"lon":126.3717 },

  "status": {
    "observed_at":"2021-07-31", "sst_basis":"daily_mean",
    "current_sst":28.47,      "current_sst_raw":28.4708,
    "daily_max_sst":29.00,    "daily_max_sst_raw":29.0000,
    "climatology_mean":26.25, "climatology_mean_raw":26.2451,
    "anomaly":2.23,           "anomaly_raw":2.2257,
    "stage":"advisory", "stage_label":"주의보 기준 도달",
    "days_over_advisory":2,
    "stage_caveat":"공식 특보는 해역 단위이며 전망을 포함합니다. 이 값은 관측점 재계산치입니다."
  },

  "data_quality": {
    "sst_missing_rate":0.0, "obs_count":48,
    "judgment_withheld":false, "withheld_reason":null
  },

  "groups": [
    { "id":"heat_input","name":"열유입","display_order":1,
      "adopted":true,"representative_percentile":90.7,
      "factors":[
        {"id":"solar","name":"일사량","window":"7일","as_of":"2021-07-30",
         "value":23.14,"value_raw":23.1429,"unit":"MJ/m²",
         "percentile":87.3,"clim_sample_size":158,"quality":"ok"},
        {"id":"airtemp","name":"기온","window":"7일","as_of":"2021-07-30",
         "value":34.37,"value_raw":34.3714,"unit":"℃",
         "percentile":90.7,"clim_sample_size":172,"quality":"ok"}]},
    { "id":"background","name":"배경장","display_order":2,
      "adopted":false,"representative_percentile":61.0,
      "factors":[
        {"id":"cum_anomaly","name":"30일 누적편차","window":"30일","as_of":"2021-07-31",
         "value":12.40,"value_raw":12.3971,"unit":"℃·일",
         "percentile":61.0,"clim_sample_size":172,"quality":"ok"}]}
  ],

  "conditions":[{"id":"tidal_phase","label":"neap",
                 "display_text":"소조기 — 바닷물 섞임이 약해지는 시기입니다"}],

  "adoption_threshold":{"percentile":90,"unit":"group_representative_max",
                        "decided_at":"2026-07-30",
                        "note":"사전 고정. 결과를 보고 바꾸지 않음"},

  "climatology":{"method":"rolling_5y_min3","years":4,"sample_size":172,
                 "window_days":21,"is_climate_normal":false,
                 "note":"기후평년값(30년) 기준에 미달합니다"},

  "explanation":{
    "plain_template":"지금 수온은 {current_sst}로 {anomaly_phrase} …",
    "plain":"지금 수온은 28.5℃로 평년보다 2.2℃ 높습니다 …",
    "detailed_template":"…", "detailed":"…",
    "verification":{"passed":true,"retries":0,"blocked":false}},

  "disclaimer":"이 서비스는 국립수산과학원의 공식 특보를 대신하지 않습니다."
}
```

**입력본은 위에서 `explanation` 블록과 `daily_max_sst` · `daily_max_sst_raw`를 뺀 것이다.**

`daily_max_sst`는 채점 분류에만 쓴다. AI에게 주면 최고수온을 문장에 쓰려 하고,
그 시도는 §10에서 막히지만 **재시도 한 번을 헛되이 쓴다.** 애초에 주지 않는다.
완성본에는 남기되, 화면이 쓰지 않는 값이 공개 저장소에 있는 이유를
`daily_max_sst`의 필드 설명에 명시해 둔다.

---

## 9. 화면 견본

전부 **개발용 구간(2020~2021) 실측**이다. 합성 견본은 없다.

| 견본 | 날짜 | 상태 | 확보 |
|---|---|---|---|
| A | 2021-07-31 | 요인 채택 **있음** | 완료 |
| B | 2021-08-05 | 요인 채택 **없음** | 완료 |
| C | 2021-08-11 | 판정 **보류** | 완료 |
| **D** | **(미정)** | **실시간(`rolling_24h`) 형태** | **2주차. 8/3 API 확인 성공에 의존** |
| **E** | **(미정)** | **`anomaly` 음수** | **1주차. 아래 참조** |

### 견본 E가 필요한 이유

§10의 `anomaly` 부호·방향어 규칙과 §11의 `평년과 비슷합니다` 분기를
**검증할 견본이 없다.** A·B는 모두 양수(+2.23, +1.96)이고 C는 보류다.

대조군이 231일이므로 음수 anomaly인 날은 얼마든지 있다.
**1주차에 개발용 구간에서 하나를 골라 원값과 함께 이 절에 채운다.**
`|anomaly|`가 0.5 미만인 날까지 고르면 세 분기가 모두 닫힌다.
못 고르면 §16에 미실행으로 기록한다.

### 견본 A — 요인 채택 있음

2021년 천수만 고수온 특보 기간 안이다.
(2021년 전국 특보 기간은 7.15~8.26이나, **천수만은 7.23 주의보 확대 이후**다.
7.15는 전남 함평만·득량만·가막만 발령일이므로 천수만 기간으로 쓰면 틀린다. §14 #11)

| 항목 | 표기값 | 원값 · 부가 |
|---|---|---|
| 일평균 수온 | 28.47℃ | 28.4708 |
| 평년값 | 26.25℃ | 26.2451 (4년 / 172일) |
| 평년 대비 | +2.23℃ | 2.2257 |
| **일 최고수온** | 29.00℃ | 채점 분류용. 화면·문장에는 안 씀 |
| `stage` / 연속일수 | `advisory` / **2일** | 7/30부터 28℃ 연속 |
| 관측 횟수 | 48회 | 결측 0 |
| 일사 7일 평균 | 23.14 MJ/m² | 23.1429 · 백분위 87.3 (상위 13%) · 표본 158 |
| 기온 7일 평균 | 34.37℃ | 34.3714 · 백분위 90.7 (상위 9%) · 표본 172 |
| **열유입 대표** | **90.7 = max(87.3, 90.7) → 채택** | |
| 배경장 | 미채택 (61.0) | |

**표기값끼리 빼면 2.22, 원값으로 빼면 2.23이다.** 검산은 원값으로 한다.

**이 견본이 좋은 이유:** 일사 하나만 보면 90에 못 미치는데(87.3) 기온이 넘어서(90.7)
그룹으로는 채택된다. 그룹 판정이 실제로 어떻게 도는지 보여준다.

**다만 임계에 민감하다.** 기온이 **0.7 차이로** 채택을 만든다.
그리고 §14가 기록했듯 5년 창(2016~2020)으로 계산하면 일사 90.0 / 기온 92.6이 되어
**채택 요인 구성이 달라진다.** "일사는 못 미치는데 기온이 넘어서"라는 논지 자체가
창 선택에 의존한다는 뜻이다. 3주차 검산 때 창 ±1년 민감도를 함께 적는다(§16).

### 견본 B — 요인 채택 없음 (시연에 가장 중요)

| 항목 | 표기값 | 원값 · 부가 |
|---|---|---|
| 일평균 수온 | 28.62℃ | 28.6250 |
| 평년값 | 26.67℃ | 26.6657 (4년 / 172일) |
| 평년 대비 | +1.96℃ | 1.9593 |
| 일사 7일 평균 | 19.57 MJ/m² | 19.5686 · 백분위 **71.5** (상위 29%) |
| 기온 7일 평균 | 31.90℃ | 31.9000 · 백분위 **68.6** (상위 31%) |
| **일 최고수온** | 29.10℃ | 채점 분류용 |
| `stage` / 연속일수 | **`warning`** / **7일** | 7/30~8/5 연속 |
| **열유입 대표** | **71.5 = max(71.5, 68.6) → 미채택** | |

**경보 기준(28℃ 3일 연속)에 도달했는데 요인은 하나도 평년을 벗어나지 않았다.**
억지로 원인을 만들지 않고 "평년을 크게 벗어난 요인이 없습니다"를 출력한다.

**시연에서 쓸 근거 세 가지.**

**첫째, 그해 가장 뜨거운 날이다.** 일 최고 29.10℃는 §13 표의 2021년 최댓값 29.1℃와 같다.
**가장 뜨거웠던 날인데 요인은 전부 평범했다**는 뜻이라 논지가 한 단계 세진다.

**둘째, 우리 재계산이 공식 발령과 독립적으로 맞물린다.**
우리 계산은 7/30부터 연속이므로 8/1에 3일이 되고, 공식 천수만 경보는 8월 4일 발령됐다.
사흘 차이가 나는데 이건 오차가 아니라 **관측점 단위와 해역 단위의 차이**다.
특보는 해역 전체를 보고 전망까지 반영하므로 관측점 하나보다 늦거나 이를 수 있다.
`stage_caveat`가 이미 설명하는 내용이며, **우리가 더 빠르다는 주장이 아니다.**

**셋째, 더미 A′와 갈리는 화면이다.**
더미 A′는 "일평균 28℃ 초과일에 일사+기온 출력"이므로 이 날 **발동한다.**
본 시스템은 미채택이다. "이 더미랑 뭐가 다르냐"는 질문에 화면 한 장으로 답할 수 있다.
§13의 **침묵 일치율**이 재려는 것이 정확히 이 차이다.

### 견본 C — 판정 보류

| 항목 | 값 |
|---|---|
| 일 최고수온 | 28.60℃ |
| `stage` | **`withheld`** |
| 관측 횟수 | **29회** (48회 중) |
| 수온 결측률 | **39.6%** → 기준(20%) 초과 |
| 판정 | **보류** |

**원래 2021-08-18로 잡았다가 바꿨다.** 8/13~8/19는 ASOS 일사 결측으로
7일 평균이 산출되지 않아, 그날은 "수온 결측으로 보류"가 아니라 "요인 자체가 없는" 날이다.
(3.4까지 이 구간을 8/12~8/18로 적었다. 8/18은 두 표기 모두에서 제외 구간이므로
견본 C를 옮긴 결정 자체는 유효하다. §14 #12)

---

## 10. AI 문장과 검사기

### 이 절이 3.5에서 통째로 바뀐 이유

3.1~3.4에 걸쳐 "AI가 자유 문장을 쓰고 검사기가 정규식으로 항목을 특정한다"는
접근을 정교화했다. 그 결과 **규칙을 더할 때마다 정상 문장이 하나씩 차단됐다.**

| 판 | 추가한 규칙 | 그래서 깨진 것 |
|---|---|---|
| 3.3 | 우선순위 0 (특보 기준값 먼저) | — |
| 3.4 | 우선순위 0에 "항목명이 있으면 우선 1" 단서 | `수온이 2일 연속 28℃를 넘었습니다`가 차단됨 |
| 3.4 | 절 경계에 `~으로` 추가 | `23.1 MJ/m²로 상위 13%`가 차단됨 |

두 번째 것은 진단이 명확하다. `~이고`·`~하며`·`~지만`은 **연결어미**이고
`~으로`는 **부사격 조사**다. 성격이 다른 것이 목록에 섞였고, 절이 끊기면서
`일사량 … 상위 N%`처럼 **항목명을 요구하는 패턴**이 항목명과 분리됐다.

**문제는 개별 규칙이 아니라 접근이다.** 한국어는 어순이 자유롭고 조사가 다양해서,
"문장에서 숫자를 읽어 어느 항목인지 알아낸다"를 정규식으로 완전하게 만들 수 없다.
270줄까지 늘어난 명세를 사람이 읽어 모순을 다 잡는 것도 이미 불가능했다.

**그래서 숫자를 읽지 않기로 한다. AI가 숫자를 쓰지 않게 한다.**

### 새 구조

```
AI는 자리표시자만 쓴다 → 검사기는 자리표시자와 표현만 본다 → 렌더러가 값을 넣는다
```

숫자 오기·단위 오류·백분위 뒤바뀜·반올림 불일치·부호 오류가
**검사로 잡는 대상이 아니라 구조적으로 발생 불가능한 것**이 된다.

### 주는 것

**입력본** 근거 파일만. `temperature = 0`. **모델은 3주차에 확정한다.**
입력본에는 `explanation`과 `daily_max_sst`가 없다(§8).

### 자리표시자 — AI가 쓸 수 있는 유일한 값

**화이트리스트는 근거 파일에서 생성한다.** 채택되지 않은 그룹(`adopted:false`)의
요인에는 자리표시자를 만들지 않는다. **화이트리스트가 곧 항목별 격리다.**

| 자리표시자 | 근거 필드 | 렌더 결과 (견본 A) |
|---|---|---|
| `{current_sst}` | `status.current_sst_raw` | `28.5℃` |
| `{climatology_mean}` | `status.climatology_mean_raw` | `26.2℃` |
| `{anomaly_phrase}` | `status.anomaly_raw` | `평년보다 2.2℃ 높습니다` |
| `{climatology_years}` | `climatology.years` | `4년` |
| `{stage_label}` | `status.stage_label` | `주의보 기준 도달` |
| `{days_over_advisory}` | `status.days_over_advisory` | `2일` |
| `{observed_at}` | `status.observed_at` | `7월 31일` |
| `{obs_count}` | `data_quality.obs_count` | `48회` |
| `{missing_rate}` | `data_quality.sst_missing_rate` | `39.6%` |
| `{factor_as_of}` | `factors[].as_of` (ASOS 공통) | `7월 30일` |
| `{solar_window}` | `factors[solar].window` | `7일` |
| `{solar_value}` | `factors[solar].value_raw` + `unit` | `23.1 MJ/m²` |
| `{solar_top}` | `factors[solar].percentile` | `상위 13%` |
| `{airtemp_window}` | `factors[airtemp].window` | `7일` |
| `{airtemp_value}` | `factors[airtemp].value_raw` + `unit` | `34.4℃` |
| `{airtemp_top}` | `factors[airtemp].percentile` | `상위 9%` |

**단위가 렌더 결과에 포함된다.** AI가 단위를 잘못 붙일 방법이 없다.
**견본 A에는 `{cum_anomaly_*}`가 없다.** 배경장이 미채택이기 때문이다.

### 렌더 규칙

| 대상 | 규칙 |
|---|---|
| 수치 (`current_sst`·`climatology_mean`·`{*_value}`) | **원값**을 소수 첫째 자리 **사사오입** + 단위 |
| `{*_top}` | `상위 N%`, **N = round(100 − percentile)** |
| `{anomaly_phrase}` | `|raw|`를 첫째 자리 반올림해 **0.5 이상**이면 `평년보다 N℃ 높습니다/낮습니다`(부호로 방향 결정), **0.5 미만**이면 `평년과 비슷합니다` |
| `{missing_rate}` | `round(rate × 100, 1)` + `%` |
| `{days_over_advisory}` | `N일` |
| 날짜 | `M월 D일` |

**표기값(`current_sst`=28.47)이 아니라 원값(28.4708)에서 반올림한다.**
표기값을 다시 반올림하면 26.25 → 26.3이 되어 원값 기준(26.2)과 어긋난다.

**렌더러는 §11 화면 문구와 같은 함수를 쓴다.** 화면이 `28.5℃`를 쓰는데
문장이 `28.47℃`를 쓰는 일이 구조적으로 생기지 않는다.
`anomaly_raw = 0.48`일 때 화면과 문장이 갈리던 문제도 같은 이유로 사라진다.

### 지시문

```
아래 파일을 보고 설명 문장을 써.

숫자를 직접 쓰지 마. 아라비아 숫자를 한 글자도 쓰지 마.
값이 필요한 자리에는 아래 목록의 자리표시자를 그대로 넣어.
목록에 없는 자리표시자를 만들지 마.

  {current_sst} {climatology_mean} {anomaly_phrase} {climatology_years}
  {stage_label} {days_over_advisory} {observed_at} {obs_count} {missing_rate}
  {factor_as_of} {solar_window} {solar_value} {solar_top}
  {airtemp_window} {airtemp_value} {airtemp_top}

{factor_as_of}를 반드시 한 번 넣어. 요인 자료가 수온보다 이전 날짜라는 사실을
    밝히지 않으면 안 된다.

앞으로 어떻게 될지 쓰지 마.
안전한지 위험한지 판단하지 마.
"때문에", "원인은" 같은 말로 인과를 단정하지 마.
어느 요인이 더 중요한지 말하지 마.
파일에서 adopted가 false인 묶음의 이름을 쓰지 마.

쉬운 말은 3~4문장, 자세히는 6~8문장.
판정 보류일 때는 보류 사실만 1~2문장으로 쓴다.
```

### 검사 항목 — 여섯 줄로 끝난다

| # | 검사 | 규칙 |
|---|---|---|
| 1 | **자리표시자 화이트리스트** | 목록에 없는 `{...}`가 있으면 차단 |
| 2 | **숫자 리터럴 금지** | 자리표시자 밖에 아라비아 숫자가 **하나라도** 있으면 차단 |
| 3 | **필수 자리표시자** | `{factor_as_of}`가 없으면 차단 |
| 4 | **미채택 그룹 언급** | `adopted:false`인 그룹 이름(예: `배경장`)이 문장에 있으면 차단 |
| 5 | **금지 표현** | 전망, 예상, 예측, 될 것, 이어질, 안전, 우려 없음, 괜찮 / 때문에, 원인은, 탓에, ~로 인해 / 가장 큰, 주된, 주요 원인, 결정적, 기여도 |
| 6 | **문장 수** | 쉬운 말 3~4, 자세히 6~8, 보류 1~2 |

**2번이 이 설계의 핵심이다.** 숫자를 한 글자도 허용하지 않으므로
반올림·단위·백분위·부호·표본·한글 수사를 각각 검사할 필요가 없다.
`이레`, `일주일` 같은 우회도 4번이 아니라 **2번의 취지에 따라 금지 표현에 넣어 둔다**
— 다만 자리표시자가 이미 `7일`을 제공하므로 AI가 이를 쓸 유인이 거의 없다.

**3번이 3.4까지 없던 검사다.** 지시문에는 "요인 날짜를 밝혀"라는 필수 요구가
있었는데 검사 표에는 이를 확인하는 줄이 없어, 빼먹어도 통과했다.

### 재시도와 차단

문장이 검사를 통과하지 못하면 **실패한 검사 번호와 규칙 문구를 프롬프트에 덧붙여
다시 만든다. 세 번 실패하면 문장을 표시하지 않고 `verification.blocked = true`로
기록한다.** 이 비율이 "문장 차단율"이다.

> **`temperature = 0`인데 재시도가 의미가 있는가.**
> 있다. **입력이 달라지기 때문이다.** 실패 사유를 되먹이지 않으면 같은 입력에
> 같은 출력이 나와 재시도 3회가 사실상 1회가 된다. 3.4까지 이 되먹임이
> 문서에 없었다(§14 #14).

**문장 차단율의 분모는 「생성 시도 대상 문장 수」다.**
하루에 쉬운 말·자세히 두 개를 만들므로 하루당 2다. 보류일은 하나다.

### 시험 문장 — 이것이 곧 테스트 케이스다

**차단되어야 함**

| # | 문장 | 걸리는 검사 |
|---|---|---|
| 1 | `지금 수온은 28.5℃입니다` | 2 (숫자 리터럴) |
| 2 | `최근 7일 일사량이 상위 13%입니다` | 2 (자리표시자를 안 씀) |
| 3 | `오늘 최고수온은 {daily_max_sst}입니다` | 1 (입력본에 없는 자리표시자) |
| 4 | `배경장은 {cum_anomaly_top}입니다` | 1 + 4 (미채택 그룹) |
| 5 | `배경장도 평년을 벗어났습니다` | 4 |
| 6 | `폭염 때문에 수온이 올랐습니다` | 5 (인과 단정) |
| 7 | `일사량이 가장 큰 요인입니다` | 5 (순위 표현) |
| 8 | `당분간 이 상태가 이어질 것으로 보입니다` | 5 (예측) |
| 9 | `아직은 안전한 수준입니다` | 5 (안전 판정) |
| 10 | `지금 수온은 {current_sst}로 {anomaly_phrase}.` (요인 날짜 없음) | 3 (필수 자리표시자 누락) |
| 11 | 쉬운 말인데 6문장 | 6 |
| 12 | `7월 30일부터 이레째 넘고 있습니다` | 5 (한글 수사) |

**통과해야 함**

| # | 문장 | 무엇을 검사하나 |
|---|---|---|
| 13 | `지금 수온은 {current_sst}로 {anomaly_phrase}.` + `요인 값은 {factor_as_of}까지의 자료입니다.` | 기본형 |
| 14 | `최근 {solar_window} 일사량이 평년 {solar_top} 수준입니다.` | 자리표시자 조합 |
| 15 | `{days_over_advisory} 연속으로 주의보 기준을 넘었습니다.` | 숫자 없이 연속일수 표현 |
| 16 | `수온 자료의 {missing_rate}가 비어 있어 이번 시각은 판정하지 않았습니다.` | 보류형 |

**렌더 검사 (문장 검사와 별도)**

| # | 확인 | 견본 |
|---|---|---|
| 17 | `{anomaly_phrase}`가 양수에서 `높습니다` | A |
| 18 | `{anomaly_phrase}`가 **음수에서 `낮습니다`** | **E 필요** |
| 19 | `|anomaly|` 0.5 미만에서 **`평년과 비슷합니다`** | **E 필요** |
| 20 | `{solar_top}` = `상위 13%` (백분위 87.3) | A |
| 21 | `{current_sst}` = `28.5℃` (원값 28.4708 사사오입) | A |
| 22 | 실시간에서 `{observed_at}`에 시각이 붙는가 | **D 필요** |

**핵심은 2·3·4번이다.**
2번이 뚫리면 이 설계 전체가 무의미해지고, 3번은 3.4까지 없던 누락 검사이며,
4번은 "억지로 원인을 만들지 않는다"는 §0의 두 번째 주장을 지킨다.
**5번(금지 표현)은 3.2에서 통째로 사라진 적이 있으므로 회귀 감시 대상이다.**

### 좋은 예 / 나쁜 예

**쉬운 말 (3~4문장)**

| | |
|---|---|
| ✅ 원문 | 지금 수온은 `{current_sst}`로 `{anomaly_phrase}`. 최근 `{solar_window}` 일사량이 평년 `{solar_top}` 수준이고, 같은 기간 기온은 `{airtemp_top}`입니다. 요인 값은 `{factor_as_of}`까지의 자료입니다. |
| ✅ 렌더 | 지금 수온은 28.5℃로 평년보다 2.2℃ 높습니다. 최근 7일 일사량이 평년 상위 13% 수준이고, 같은 기간 기온은 상위 9%입니다. 요인 값은 7월 30일까지의 자료입니다. |
| ❌ | 폭염 **때문에** 수온이 올랐습니다. 당분간 **이어질 전망**입니다. **아직은 안전한** 수준입니다. |

**자세히 (6~8문장)**

| | |
|---|---|
| ✅ 원문 | 현재 수온은 `{current_sst}`입니다. 최근 `{climatology_years}` 같은 시기 평균인 `{climatology_mean}`보다 높아 `{anomaly_phrase}`. 열유입 묶음이 판정 기준을 넘었습니다. 최근 `{solar_window}` 일사량은 `{solar_value}`로 평년 `{solar_top}`입니다. 같은 기간 일 최고기온 평균은 `{airtemp_value}`로 `{airtemp_top}`입니다. 요인 값은 `{factor_as_of}`까지의 자료입니다. 이 수치는 평년에서 벗어난 정도이며 원인 기여도가 아닙니다. |
| ❌ | 일사량이 **가장 큰 요인**이며 기여도는 약 60%로 추정됩니다. |

**보류 (1~2문장. 쉬운 말 3~4문장 규정의 예외)**

| | |
|---|---|
| ✅ 원문 | 수온 자료의 `{missing_rate}`가 비어 있어 이번 시각은 판정하지 않았습니다. |
| ❌ | 자료가 부족하지만 **대체로 평년 수준으로 보입니다**. |

**보류일에는 `{factor_as_of}` 필수 검사를 적용하지 않는다.**
요인 자체를 판정하지 않았으므로 밝힐 날짜가 없다.

---

## 11. 화면 문구 전체

### 지도
- 제목 `충남 서해 연안 관측점`
- 안내 `점을 누르면 설명을 볼 수 있습니다`
- 범례 `설명 제공` / `설명 준비 중`
- 준비 중 클릭 `{지점명} ({해역}) — 이 지점은 설명을 준비 중입니다.`
- **정보 화면 진입** 오른쪽 위 `ⓘ 특보란?`

### 정보 화면
**§5의 목업을 그대로 쓴다.** 아래는 목업에 없는 항목만 적는다.
- 되돌아가기 `← 뒤로` (왔던 화면으로)
- 하단 `공식 특보는 국립수산과학원 발표를 따르십시오` + 링크

### 지도 출처 표기
- 지도 하단 작게 `배경지도: {출처}` (1주차에 확정)

### 설명 머리말
- `← 지도로`
- **정보 화면 진입** 오른쪽 위 `ⓘ 특보란?`
- `충남 천수만 · 서산 창리`
- 과거 `{날짜} 일평균` / 실시간 `{날짜} {시각}까지 24시간 평균`

### 상태 배지
§8의 `stage` enum 표를 그대로 쓴다.

### 평년 대비
**§10의 `{anomaly_phrase}` 렌더러와 같은 함수를 쓴다.**
분기 판정은 원값이 아니라 **절댓값을 소수 첫째 자리로 반올림한 값**으로 한다.

| 조건 (반올림 절댓값 기준) | 문구 |
|---|---|
| ≥ 0.5 이고 원값이 양수 | `평년보다 {n}℃ 높습니다` |
| ≥ 0.5 이고 원값이 음수 | `평년보다 {n}℃ 낮습니다` |
| **< 0.5** | **`평년과 비슷합니다`** |

`{n}`은 **원값의 절댓값을 소수 첫째 자리로 사사오입한 값**이다(견본 A는 `2.2`).
원값 기준으로 분기하면 `anomaly_raw = 0.48`일 때 화면은 "비슷합니다"인데
문장은 반올림해 "0.5℃ 높습니다"가 되어 갈린다. 같은 함수를 쓰면 그럴 수 없다.

### 자료 시각
- 과거 `수온: {날짜} 일평균 ({n}회 관측)`
- 실시간 `수온: {날짜} {시각}까지 24시간 평균 ({n}회 관측)`
- 연속일수 `{날짜}까지 {n}일 연속` — **`stage`가 `advisory` 또는 `warning`일 때만 표시.**
  `normal`·`preliminary`·`withheld`에는 표시하지 않는다
- 공통 `요인: {날짜}까지 자료`

### 요인 영역
- 제목 `지금 평년을 벗어난 것`
- 채택 없음 `평년을 크게 벗어난 요인이 없습니다.`
- 그룹 미채택 `판정 기준에 못 미칩니다`
- 요인 이름 `{요인명} ({창})` — 예: `일사량 (7일)`
- 요인 백분위 `상위 {n}%` — n = round(100 − `percentile`)
- **임계선 라벨** `기준 {상위 n%}` — n = round(100 − `adoption_threshold.percentile`).
  임계가 90이면 `기준 상위 10%`
- 요인 값 `{값}{단위}` — 원값을 소수 첫째 자리 사사오입(§10 렌더 규칙과 같은 함수)
- 안내 `ⓘ 가로 위치는 평년 분포에서 얼마나 바깥인지를 나타냅니다. 원인 기여도가 아닙니다.`

**임계선 라벨을 `90`이라고 쓰지 않는 이유.**
막대 오른쪽에 이미 `상위 13%`가 적혀 있는데, **백분위 90과 상위 13%는
숫자가 커지는 방향이 반대다.** 백분위는 클수록 바깥이고 상위%는 작을수록 바깥이다.
`90`과 `상위 13%`를 나란히 놓으면 어느 쪽이 바깥인지 읽히지 않는다.
같은 눈금으로 적으면 이렇게 읽힌다.

| 요인 | 화면 | 읽히는 것 |
|---|---|---|
| 일사량 | `상위 13%` vs `기준 상위 10%` | 13은 10보다 크다 → **못 미친다** |
| 기온 | `상위 9%` vs `기준 상위 10%` | 9는 10보다 작다 → **넘었다** |

### 요인 영역 표시 규칙

| 상황 | 화면 |
|---|---|
| 채택된 그룹이 **하나라도 있다** | 모든 그룹을 `display_order` 순으로 그린다. 채택 그룹은 막대를, 미채택 그룹은 `판정 기준에 못 미칩니다`만 |
| 채택된 그룹이 **하나도 없다** | `평년을 크게 벗어난 요인이 없습니다.` **한 줄만** 쓴다. 그룹을 나열하지 않는다 |
| `stage`가 `withheld` | 요인 영역 전체를 **판정 보류 문구로 대체**한다 |

**채택이 하나도 없을 때 그룹을 나열하지 않는 이유.**
`평년을 크게 벗어난 요인이 없습니다.`와 `판정 기준에 못 미칩니다`가
**같은 말을 두 번 하게 된다.** 견본 B는 시연에서 이 서비스가 보여줄 화면이므로
한 줄로 끝내는 편이 세다.

**`ⓘ` 안내는 막대가 그려진 화면에서만 띄운다.**
견본 B·C에는 막대가 없어 안내할 대상이 없다.

### 조건 영역
- 제목 `함께 보는 조건`
- §7의 조석 3분류 문구를 그대로 쓴다.

### 설명 영역
- 탭 `쉬운 말` / `자세히`
- 차단 시 `이번 시각의 설명 문장을 만들지 못했습니다. 위의 수치는 그대로 유효합니다.`

### 판정 보류
`자료 결측으로 이번 시각은 판정을 보류했습니다. (수온 결측률 {n}%)`

### 한계 블록 (항상 노출, 아래 7줄이 최종)
```
이 서비스가 답하지 않는 것
· 며칠 더 갈지 예측하지 않습니다
· 안전한지 위험한지 판정하지 않습니다
· 공식 특보를 대신하지 않습니다

평년값: 최근 {n}년 · 표본 {n}일
기후평년값(30년) 기준에는 못 미칩니다
관측점 한 곳의 자료이며 해역 전체를 대표하지 않습니다
공식 특보는 국립수산과학원 발표를 따르십시오
```

---

## 12. 일정

| 시점 | 할 일 |
|---|---|
| **7/31** | **프로토타입** — 지도(개념도) + 설명 화면 + **정보 화면** + 견본 3종 |
| 8/3~8/7 | ① **실시간 API 확인** ② 환경·GitHub ③ **스키마 확정** ④ 천수만 특보 이력 ⑤ 정보 탭 기준값·문구 출처 ⑥ 지도 배경 출처·좌표 ⑦ **평년값 창 경계 규칙** ⑧ **30분→1시간 변환 여부** ⑨ 견본 E 선정 |
| 8/10~8/14 | 화면 완성, 골든 케이스, 견본 D(실시간), **갱신 파이프라인**, **검사기·렌더러** |
| 8/17~8/21 | 자료 처리 + **검산** + 창 민감도 + **문서 정리** (+ 여유가 있으면 지도 배경 교체) |
| 8/24~8/26 | **설명층(모델 호출) + 채점** + 문서 마무리 |
| **8/27 (목)** | **제출** |

**1주차가 아홉 항목이다.** 3.4까지 여섯이라고 적었으나 §16과 대조하면 아홉이었다.
⑦⑧은 이번 판에서 새로 올라온 것이고, 둘 다 **뒤에 있는 계산의 전제**라
3주차로 미룰 수 없다. ⑧을 3주차로 두면 결측률 정의가 바뀌면서
견본 C와 §13 보류율을 다시 계산해야 한다.

**7/31 프로토타입의 스키마는 잠정이다.** 8/7 확정 때 바뀌면 프로토타입도 고친다.
**대표님이 짬 날 때 검사하시므로 화면은 항상 돌아가는 상태를 유지한다.**

### 이미 계산이 끝난 것

§13의 검산표(2017~2021), 판정 대상일 250일, 채택률 14.0%/12.6%/31.6%,
보류율 8.5%는 **이미 산출된 값**이다. 3주차의 "자료 처리 + 검산"은
새로 계산하는 일이 아니라 **코드가 이 값을 재현하는지 확인하는 일**이다.
§14가 기록했듯 같은 원자료에서 다른 숫자가 나온 적이 두 번 있어서 이 확인이 필요하다.

### `evidence.json`은 누가 언제 갱신하는가

**3.4까지 이 절이 없었다.** 배포가 GitHub이고 화면이 `evidence.json`만 읽는데,
그 파일을 갱신하는 주체가 문서 어디에도 없었다.

| 항목 | 결정 |
|---|---|
| 실행 주체 | **GitHub Actions (스케줄 워크플로)** |
| 주기 | **1시간** (30분 간격 관측이지만 요인은 어제까지라 더 자주 돌 이유가 없다) |
| 하는 일 | 실시간어장정보 API 호출 → 24시간 이동평균 계산 → `evidence.json` 갱신 → AI 문장 생성·검사 → 커밋 |
| 실패 시 | **이전 파일을 그대로 둔다.** 화면 머리말의 `{날짜} {시각}` 이 갱신되지 않으므로 사용자가 오래된 값임을 알 수 있다. 3회 연속 실패하면 워크플로가 이슈를 연다 |
| 커밋 폭증 | 같은 파일을 덮어쓰므로 이력만 늘어난다. 필요하면 `--amend` 대신 **매일 1커밋으로 squash**하는 정리 작업을 주 1회 돌린다 |
| AI 호출 비용 | 1시간마다 문장 2개 × 24시간. **3주차 모델 확정 시 단가를 곱해 §16에 적는다** |

**이 파이프라인은 8/3 API 확인이 성공해야 만들 수 있다.** 실패하면 아래 후퇴선을 탄다.

### 1주차 첫 작업 — 실시간 자료 확인

공공데이터포털 **실시간어장정보 API**가 서산 창리를 제공하는가.
활용신청(자동승인) → 인증키 → 호출 → 창리가 목록에 있는가, 어떤 항목이 오는가.

| 상황 | 조치 |
|---|---|
| API가 창리를 준다 | 계획대로 |
| **API가 창리를 주지 않는다** | **지점을 바꾸지 않는다.** 실시간을 M1 범위 밖으로 내리고 **과거 재현만 제출**한다 |

**지점을 바꾸면 §7 평년값, §9 견본 A·B·C, §13 숫자가 전부 무효가 된다.**
실시간을 포기하는 비용이 훨씬 싸다. **이 판단은 8/3 당일에 한다. 미루지 않는다.**

### 실시간 철회 체크리스트

실시간을 포기할 경우 지워야 할 곳이다. 흩어져 있어서 목록으로 둔다.

| # | 위치 | 지울 것 |
|---|---|---|
| 1 | §3 | 「운영 검증 2026」 행 |
| 2 | §6 | 만드는 것에서 「갱신 파이프라인」 |
| 3 | §8 | `sst_basis`의 `rolling_24h` 행과 그 설명 |
| 4 | §11 | 설명 머리말·자료 시각의 **실시간** 문구 |
| 5 | §10 | 렌더 검사 22번, 자리표시자 `{observed_at}`의 시각 표기 |
| 6 | §12 | 갱신 아키텍처 절 전체 |
| 7 | §9 | 견본 D |
| 8 | §16 | 견본 D 항목 → **미실행으로 기록** |

**그리고 그 사실을 §14 정정 이력에 적는다.** 조용히 지우지 않는다.

### 인증키는 세 종류다

| 키 | 용도 | 언제 |
|---|---|---|
| 공공데이터포털 | 실시간 수온 | **8/3** (자동승인) |
| 기상청 API 허브 | 기상 자료 | 확보됨 |
| AI 모델 | 문장 생성 | 8/21 |

**첫 커밋으로 `.gitignore`를 만들고 `.env`를 등록한다.**
GitHub Actions에서 쓰는 키는 저장소 Secrets에 넣는다.

### 자를 순서
문서 축소 → **지도 배경 교체 취소(개념도 유지)** → **요인 일치율을 2025년만**
→ 조석 영역 생략 → 화면 반응형 축소

**정보 화면은 자르지 않는다.** 정적 텍스트라 비용이 거의 없고,
§0이 규정한 서비스 정체성("왜 이런 거지"에 답한다)의 앞단이기 때문이다.

**대조군·이상군 채택률은 2022~2025 전체를 유지한다.** 정답지가 필요 없어 비용이 거의 없고
실패 기준의 주력이기 때문이다. **문장 검사기·검산·더미 비교는 자르지 않는다.**

**요인 일치율을 2025년만으로 자르면 표본이 1개 연도로 줄어든다.**
그 경우 실패 기준의 "더미 대비 개선 없음"을 **판정하지 않고 참고치로만 보고한다.**
소수 건수로 성공/실패를 가르지 않는다.

### 마지막 주 부하

**마감이 8/29(토)에서 8/27(목)로 당겨져 마지막 주가 5일에서 3일이 됐다.**
원래 이 주에 설명층·검사기·채점·문서 넷이 몰려 있었다.
AI 모델 인증키가 8/21에 확보되므로 **설명층은 사흘 안에 만들고 돌려야 한다.**

**§10을 자리표시자 방식으로 바꾼 것이 이 부하를 줄인다** — 정규식 명세와
디버깅이 사라지고 검사 여섯 줄만 구현하면 된다. 이 변경이 없었다면
3일은 불가능했다.

그래도 사흘은 빠듯하므로 **선제적으로 앞으로 당긴다.**

| 옮기는 것 | 어디로 | 왜 가능한가 |
|---|---|---|
| **문서 정리** | 3주차(8/17~8/21) | 자를 순서 1순위라 어차피 줄일 항목이다. 검산 결과만 나중에 채워 넣으면 된다 |
| **검사기 구현** | 2주차(8/10~8/14) | 검사 여섯 줄은 근거 파일 스키마(8/7 확정)만 있으면 만들 수 있다. **AI 모델과 무관하다** |
| **렌더러 구현** | 2주차 | §11 화면 문구와 같은 함수이므로 화면 완성과 동시에 만든다 |

**마지막 주에 반드시 남는 것은 설명층(모델 호출)과 채점 둘뿐이다.**
검사기와 렌더러를 2주차로 당기면, 8/21에 인증키가 나온 뒤 하는 일은
**프롬프트를 붙여 돌려보는 것**뿐이다.

**그래도 밀리면 문서를 줄이되 검사기·검산·더미 비교는 자르지 않는다.**

---

## 13. 검산과 채점

### 검산 — 코드가 맞는지 코드 밖에서 확인

**대조 기준 (평년값 재료 + 개발용 구간만)**

| 연도 | 관측일 | 일평균의 평균 | 일 최고의 최댓값 | 일 최고 28℃ 이상 |
|---|---|---|---|---|
| 2017 | 110 | 25.59℃ | 29.5℃ | 21일 |
| 2018 | 139 | 23.43℃ | 30.6℃ | 26일 |
| 2019 | 139 | 23.75℃ | 29.2℃ | 24일 |
| 2020 | 139 | 23.00℃ | 27.8℃ | **0일** |
| 2021 | 137 | 23.69℃ | 29.1℃ | 27일 |

2017년은 6월 13일 개시라 5~6월이 빠져 평균이 높다. 추세 근거로 쓰지 않는다.

**골든 케이스 2021-07-31 / 보조 2021-08-05** — §9의 원값 표를 그대로 쓴다.
**두 날짜를 다 맞춰야 한다.** 하나는 채택, 하나는 미채택이라 판정 로직까지 검증된다.

### 채택률 — 분모 정의

**분모는 「판정 대상일」이다.** ASOS 7일 평균이 산출되고 보류되지 않은 날.

| 개발용 구간(2020~2021) | 일수 |
|---|---|
| 분석 기간 139일 × 2년 | 278 |
| − ASOS 일사 결측으로 7일 평균 미산출 (**8/13~8/19**) | −7 |
| = ASOS 산출 가능일 | 271 |
| − 수온 결측·부재로 보류 | −21 |
| **= 판정 대상일** | **250** |

| 구분 | 일수 | 채택 | 비율 |
|---|---|---|---|
| 전체 | 250 | 35 | **14.0%** |
| **대조군** (일 최고 < 28℃) | 231 | 29 | **12.6%** |
| **이상군** (일 최고 ≥ 28℃) | 19 | 6 | **31.6%** |

**이상군 31.6% > 대조군 12.6%** — 방향이 맞다.

### 이상군이 27일에서 19일로 줄어든 이유 (3주차에 확정)

검산표의 2021년 「일 최고 28℃ 이상」은 **27일**인데(2020년은 0일),
판정 대상일 기준 이상군은 **19일**이다. 차이 **8일**은 제외 구간에서 왔다.

| 제외 사유 | 이상군에 해당한 일수 |
|---|---|
| ASOS 일사 결측 (8/13~8/19, 7일) | (3주차 확정) |
| 수온 결측·부재로 보류 (21일) | (3주차 확정) |
| **합** | **8** |

**이 표를 채우는 것이 중요하다.** 8/13~8/19는 2021년 한여름 한복판이므로
제외된 날 상당수가 이상군일 가능성이 높다. 그렇다면 이런 문장이 성립한다.

> **가장 뜨거웠던 시기의 자료가 요인 결측으로 판정에서 빠졌고, 그래서 이상군이 얇다.**

이건 숨길 게 아니라 §7의 보간 금지 원칙이 실제로 치른 비용의 기록이다.
지금처럼 27과 19만 있고 설명이 없으면 "계산이 틀린 것 아니냐"로 읽힌다.

**날짜 규약 정정에 따른 재검산.** ASOS 제외 구간을 8/12~8/18에서 8/13~8/19로
정정했으므로(§7, §14 #12), **하루가 밖으로 나가고 하루가 안으로 들어왔다.**
일수 7일은 같아 271과 250은 그대로지만, **그 두 날이 이상군인지 대조군인지에 따라
19·231·6·29가 각각 ±1까지 움직일 수 있다.** 3주차 검산에서 확인하고,
바뀌면 §14에 적는다.

### 개발용에서는 유의성을 판정하지 않는다

**개발용 이상군은 19일뿐이라 얇다.** 하루만 달라져도 26%~37%로 흔들린다.
12.6% vs 31.6%의 차이가 우연인지 아닌지를 여기서 주장하지 않으며,
이 숫자는 **발동률이 합리적 범위인지 확인한 값**으로만 쓴다.

**성능과 유의성은 채점용(2022~2025, 556일)에서만 말한다.**
다만 **채점용 이상군이 몇 일인지가 아직 문서에 없다.** 표본 크기는 성능 정보가
아니므로 1주차에 세어 §3 접촉 기록 #7에 올리고, 그 수를 보고 **어떤 검정을 쓸지
(피셔 정확검정 / 카이제곱 / 부트스트랩) 채점 전에 정해 커밋한다**(§16).
2020년처럼 이상군이 0일인 해가 채점 구간에도 있을 수 있다.

**개발용을 늘리려면 채점용에서 떼오는 수밖에 없다**(2017~2019는 롤링 3년 미달이라
근거 파일 자체를 만들 수 없다). 채점이 이 프로젝트의 주 산출물이므로 떼오지 않는다.

### 보류율

| | |
|---|---|
| 분석 기간 총합 (139 × 5년, 2017~2021) | 695일 |
| 관측 자체가 없던 날 | 31일 |
| 관측일 중 결측률 20% 초과 | 28일 |
| **보류율 (2017~2021)** | (28+31) / 695 = **8.5%** |
| 참고: 개발용 구간(2020~2021)만 | 21 / 278 = **7.6%** |

**실패 기준의 「보류율 > 40%」는 채점용 구간(2022~2025)에 적용한다.**
위 두 값은 개발용·평년값 재료 구간의 참고치다.
**보류율 임계 20%(결측률)의 근거는 아직 없다**(§16).
채택 임계 90퍼센타일은 사전 고정 근거와 이론 상한까지 계산해 두었으면서
보류를 가르는 20%에는 근거가 없어, "임계를 사전 고정한다"는 원칙의 일관성이 약하다.

### 더미

| 모델 | 동작 |
|---|---|
| **더미 A′** | 일평균이 28℃를 넘은 날에만 "일사 + 기온" 출력. 이상도 계산 없음 |
| **본 시스템** | 그룹 대표 백분위 90 이상일 때만 채택 요인 출력 |

**더미 A′는 두 지표에서 정반대다.**

| 지표 | 더미 A′ |
|---|---|
| 대조군 채택률 | **항상 0%** (28℃ 미만에서 발동하지 않음) → 본 시스템에 완승 |
| 요인 일치율 | **매우 높음** (항상 열유입을 출력하고, 속보 원인 서술도 대개 열유입 계열) |
| **침묵 일치율** | **항상 0%** (28℃를 넘으면 무조건 발동하므로 침묵하지 않음) |

**3.4까지 침묵 일치율이 없었기 때문에, 더미가 이기게 설계된 구조였다.**
비교 지표가 "채택했을 때 맞나"뿐이었는데, 이 서비스의 주장은
"없을 때 없다고 말한다"였다. 지표가 주장을 재지 않았다.

### 채점 지표

| 지표 | 정의 | 구간 | 쓰임 |
|---|---|---|---|
| 대조군 채택률 | 일 최고 28℃ 미만인 판정 대상일 중 채택 비율 | 2022~2025 전체 | **보고** |
| 이상군 채택률 | 일 최고 28℃ 이상인 판정 대상일 중 채택 비율 | 2022~2025 전체 | 실패 기준 |
| **요인 일치율** | 채택했을 때 속보 원인 서술과 맞는 비율 | 특보 이력·정답지 확보 연도 | 실패 기준 |
| **침묵 일치율** | **원인이 특정되지 않은 날에 시스템도 채택하지 않은 비율** | 특보 이력 확보 연도 | 실패 기준 |

### 일치율 — '맞다'의 판정 규칙

**① 그룹 단위로 판정한다. 요인 단위가 아니다.**
속보는 "폭염 지속"이라고만 쓰고 일사와 기온을 구분하지 않는다.
요인 단위로 채점하면 속보가 쓰지 않은 구분을 요구하는 셈이 된다.

**② 표현–그룹 대응표를 채점 전에 확정하고 커밋한다.**

| 속보 표현 | 그룹 |
|---|---|
| 폭염, 고온, 북태평양고기압·티베트고기압 확장 | 열유입 |
| 태양복사 증가, 일사량 증가, 맑은 날씨 | 열유입 |
| 장마 소멸·소강 | 열유입 |
| 누적된 고수온, 이전부터 높은 수온 | 배경장 |
| 소조기, 조석 | (조건 변수. 채점 대상 아님) |
| 장마전선 정체, 강수, 태풍, 한기 유입 | (수온 하강 요인. 채점 대상 아님) |

**대응표가 열유입 쪽으로 기울어 있다.** 열유입 3줄, 배경장 1줄이다.
속보가 배경장 계열 서술을 드물게 쓴다면 배경장 채택일은 구조적으로 불일치가 된다.
1주차에 속보를 훑을 때 **배경장 계열 표현이 실제로 얼마나 쓰이는지 세어
대응표를 보강한다.** 채점 결과를 보기 전에 끝낸다.

**③ 판정은 네 갈래다.** (3.4까지 세 갈래였고, 가장 흔한 경우가 빠져 있었다)

| 판정 | 시스템 | 속보 | 들어가는 지표 |
|---|---|---|---|
| **일치** | 채택 | 원인 서술 있음, 그룹 대응 | 요인 일치율 분자 |
| **불일치** | 채택 | 원인 서술 있음, 그룹 안 맞음 | 요인 일치율 분모 |
| **정당한 침묵** | 미채택 | **원인 서술 없음 / 대응표 밖 표현만** | **침묵 일치율 분자** |
| **놓침** | 미채택 | 원인 서술 있음 | **침묵 일치율 분모** |
| (제외) | 채택 | 원인 서술 없음 | 어느 분모에도 넣지 않고 **건수만 보고** |

> **요인 일치율 = 일치 / (일치 + 불일치)**
> **침묵 일치율 = 정당한 침묵 / (정당한 침묵 + 놓침)**

**개발용에서 이상군 채택률이 31.6%였으므로 미채택이 다수다.**
3.4까지는 이 다수가 어느 분모에도 정의되지 않아, 채점 당일에 정해야 했다.
**그건 결과를 보고 기준을 정하는 것이고, §3이 막으려던 바로 그 상황이다.**

**④ 더미와 비교할 때는 분모를 맞춘다.**
요인 일치율은 **양쪽 모두 채택한 날**만, 침묵 일치율은 **양쪽 모두 판정 가능한 날**만
공통 분모로 쓴다. 분모가 다르면 비교가 아니다.

**⑤ 사람이 채점하고 신뢰도를 잰다.**
작성자가 채점하되 **1인에게 10건 이상을 독립 채점시켜 일치도를 산출·보고한다.**
불일치가 잦으면 대응표가 모호한 것이므로 대응표를 고치고 재산출하며 이력을 남긴다.
독립 채점자를 구하지 못하면 **그 사실을 결과에 명시하고 일치율을 참고치로 낮춘다.**

**⑥ 대응표를 채점 결과를 보고 고치지 않는다.**
②에서 커밋한 이후에는 ⑤의 신뢰도 문제로만 고칠 수 있다.

**대조군·이상군 채택률은 관측값 기준이라 특보 이력이 필요 없다.**
특보가 필요한 것은 요인 일치율과 침묵 일치율뿐이다.

### 실패 기준 (사전 확정)

| 조건 | 뜻 |
|---|---|
| **이상군 채택률 ≤ 대조군 채택률** | 신호와 잡음을 구분하지 못함. **임계값 없이 발동하는 주력 기준** |
| **더미 A′ 대비 요인 일치율과 침묵 일치율이 둘 다 개선 없음** | 이상도 계산이 불필요 |
| 판정 보류율 > 40% (채점용 구간) | 자료 품질이 서비스 될 수준이 아님 |
| 문장 차단율 > 10% | AI가 제약을 못 지킴 |

**대조군 채택률 20%를 게이트에서 내렸다.** 이유는 둘이다.

**첫째, 이론 상한이 상관 구조에 따라 크게 움직인다.**

| 가정 | "하나라도 90 초과"할 확률 상한 |
|---|---|
| 판정값 3개가 완전 독립 | 1 − 0.9³ = **27.1%** |
| 일사·기온이 완전 상관 + 배경장 독립 | 1 − 0.9² = **19.0%** |
| 셋 다 완전 상관 | **10.0%** |

일사 7일 평균과 기온 7일 평균은 명백히 강한 상관이다.
**개발용 실측 대조군이 12.6%라는 것은 실효 독립 검정 수가 1.3개 수준이라는 뜻**이고,
그렇다면 현실적 상한은 19%에 가깝다. **20%는 30%와 마찬가지로 거의 발동하지 않는다.**
"30%는 이론 상한보다 높아 안 되니 20%로 낮췄다"는 3.4까지의 논거는
상관을 넣으면 20%에도 그대로 적용된다.

**둘째, 온난화와 구분되지 않는다.**
§3이 "기후 추세는 확립된 전제"라고 썼다면, 채점용에서 대조군 채택률이 높게 나와도
그것이 시스템 결함인지 기후 신호인지 알 수 없다. §3의 롤링 민감도 측정 결과가
나오기 전에는 절대 임계로 합격/불합격을 가를 수 없다.

**그래서 이렇게 한다.**
- 대조군 채택률은 **보고 지표**로 낸다.
- 개발용 실측(12.6%) 대비 **2배를 넘으면 조사 대상**으로 삼고 원인을 적는다 —
  같은 임계 아래 다른 결과라는 뜻이므로.
- 합격/불합격은 **임계값이 없는 첫 번째 기준**(이상군 ≤ 대조군)이 맡는다.

**실패해도 그만두지 않는다.** 왜 실패했는지를 결과물로 낸다.

---

## 14. 정정 이력

계산 오류와 설계 오류를 발견할 때마다 여기 적는다. **원자료를 고친 적은 없다.**

| # | 항목 | 처음 | 고침 | 원인 |
|---|---|---|---|---|
| 1 | 견본 B 열유입 대표 | 68.6 | **71.5** | 최대값이 아니라 기온 값을 그대로 씀 |
| 2 | 견본 B 일사 백분위 | 72.7 (상위 27.3%) | **71.5 (상위 29%)** | 초기·최종 스크립트의 창 처리가 달랐음 |
| 3 | 2020년 평년값 창 | 2016~2019 | **2017~2019** | 창리는 2017년 개시. ASOS만 2016을 쓰고 있었음 |
| 4 | 채택일 / 채택률 | 39일 / 14.4% | **35일 / 14.0%** | ③ 반영 + 보류일을 분모에서 제외 |
| 5 | 채택률 분모 | 271 (ASOS 기준) | **250 (판정 대상일)** | 보류일은 판정하지 않으므로 분모에서 뺌 |
| 6 | 수온 28℃ 이상 + 채택 | 2일 | **6일** | ③④ 반영 |
| 7 | 보류율 분모 | 664 (관측일) | **695 (분석 기간)** | 관측이 없던 날도 보류다 |
| 8 | 견본 C | 2021-08-18 (합성) | **2021-08-11 (실측)** | 8/18은 일사 자체가 없는 날 |
| 9 | 견본 A `days_over_advisory` | 1 | **2** | 7/30부터 연속. 세지 않고 적었음 |
| 10 | 견본 B `stage` | 미기재 | **`warning`** | 7/30~8/5 일평균 28℃ 연속 7일 |
| **11** | **견본 A 특보 기간** | **2021 천수만 7.15~8.26** | **7.23 주의보 확대 이후** | 7.15~8.26은 전국 기준. 7.15는 전남 함평만·득량만·가막만 발령일 |
| **12** | **ASOS 결측 무효 구간** | **8/12~8/18** | **8/13~8/19** | 창이 D−7~D−1이므로 8/12를 포함하는 대상일은 8/13부터다. 일수 7일은 같으나 어느 날이 빠졌는지가 하루 밀린다 |
| **13** | **§10 검사기 접근** | **문형 정규식으로 항목 특정** | **자리표시자 + 렌더러** | 아래 참조 |
| **14** | **재시도 규칙** | `temperature=0`에서 3회 재생성 | **실패 사유를 프롬프트에 되먹인 뒤 재생성** | 입력이 같으면 출력이 같아 재시도 3회가 사실상 1회였다 |
| **15** | **§8 "시각이 두 개인 이유"** | 3.0~3.2에 있었음 | **3.5에서 §7로 복원** | 3.3~3.4에서 소실. 화면 문구 `요인: {날짜}까지 자료`의 유일한 근거였다 |
| **16** | **§5 목업의 요인 막대** | 두 마커가 **모두** 임계선 오른쪽 | **일사는 왼쪽, 기온은 오른쪽** | 일사 87.3은 임계 90에 못 미친다. **§9가 견본 A를 고른 이유와 그림이 반대였다.** 아래 참조 |
| **17** | **임계선 라벨** | 명세 없음 | **`기준 {상위 N%}`** | 백분위 90과 화면의 `상위 13%`는 커지는 방향이 반대라 `90`으로는 비교가 안 된다 |
| **18** | **보류일 필드 규약** | 문서에 없음 | **§8에 명시** | 견본 C를 만들 때 `groups`·`current_sst`·`climatology`를 어떻게 둘지 정해진 바가 없었다 |

**정정 #3의 영향 범위 (확인 완료).**
#3은 **2020년 창(2016~2019 → 2017~2019)만** 고친 것이다. 2021년은 처음부터
4년(2017~2020)이었으므로 **견본 A의 백분위 87.3 / 90.7은 영향을 받지 않았다.**
참고로 5년(2016~2020)으로 계산하면 일사 90.0 / 기온 92.6이 나온다 — 값이 달라지므로,
2021년이 4년이었다는 사실이 중요하다. 재계산으로 확인했다.

**정정 #16에 대해 — 그림이 논지를 반박하고 있었다.**

3.1~3.5의 §5 목업은 이랬다.

```
일사량 (7일)              상위 13%
├──────────────┊───●──────┤
기온 (7일)                 상위 9%
├──────────────┊──────●───┤
```

`┊`가 무엇인지 정의가 없었고, **두 마커가 모두 `┊` 오른쪽에 그려져 있었다.**
그런데 §9는 견본 A를 고른 이유를 이렇게 적어놨다.

> 일사 하나만 보면 90에 못 미치는데(87.3) 기온이 넘어서(90.7) 그룹으로는 채택된다.

**목업이 이 문장과 반대로 그려져 있었다.** 시연의 핵심 장면을 그림이 반박하는 상태였다.

그리고 2026-07-31 프로토타입에서 두 번째 문제가 드러났다. 목업대로 임계선을 그려도
**기온 마커가 선을 통째로 덮었다** — 백분위 차이 0.7%가 화면에서 2.6px인데
마커 지름이 12px이다. 임계선을 마커보다 위에 그리고 위아래로 빼서 해결했고,
마커·임계선의 실제 픽셀 좌표를 재서 확인했다(§5 요인 막대 사양).

**교훈 — 목업은 사양이 아니다.** ASCII 목업은 위치를 대충 그려도 읽히지만,
그 대충이 논지와 반대일 수 있다. **숫자가 들어가는 그림은 값으로 위치를 정하고,
만든 뒤 좌표로 확인해야 한다.**

**정정 #13에 대해 — 왜 접근을 바꿨나.**

3.1부터 3.4까지 네 판에 걸쳐 검사기를 정교화했다. 문형 패턴표, 우선순위 0/1/2,
같은 우선순위 경합 규칙, `…`의 범위와 탐색 방향, 절 경계, 단위 검사, 표본 금지,
한글 수사 금지, `anomaly` 절댓값 예외까지 270줄이 됐다.

**그런데 규칙을 더할 때마다 정상 문장이 하나씩 차단됐다.**
3.4에서 넣은 "항목명이 있으면 우선 1을 적용한다"는 단서는, 같은 절에서
그 단서의 존재 이유로 든 문장(`수온이 2일 연속 28℃를 넘었습니다`)을 차단했다.
같은 판에서 절 경계에 넣은 `~으로`는 통과해야 할 시험 문장 두 개를 차단했다.

**개별 규칙의 실수가 아니라 접근의 한계다.** 한국어는 어순이 자유롭고 조사가 많아
"문장에서 숫자를 읽어 어느 항목인지 알아낸다"를 정규식으로 완전하게 만들 수 없다.
그리고 270줄짜리 명세를 사람이 읽어 모순을 다 잡는 것도 이미 불가능했다.

**그래서 숫자를 읽지 않기로 했다. AI가 숫자를 쓰지 않게 했다.**
검사는 여섯 줄로 줄었고, 반올림·단위·백분위·부호·표본은 검사 대상이 아니라
**발생 불가능한 것**이 됐다. 지운 규칙은 이 항목에 기록으로 남긴다.

**교훈.** 같은 원자료에서 다른 숫자가 나온 적이 두 번(#2·#3) 있었고,
규칙을 더해서 규칙을 고치려다 실패한 적이 한 번(#13) 있었다.
전자는 **계산 스크립트를 하나로 고정**해서, 후자는 **검사 대상을 줄여서** 해결했다.
스크립트는 3주차에 저장소에 커밋해 고정한다.

---

## 15. 확인하고 접은 것 — 해상 풍속

| 지점 | 번호 | 개시 | 판정 |
|---|---|---|---|
| 내파수도 | 22446 | 2025-06-12 | 기록 1년. 이전엔 파고부이라 풍속 자체가 없음 |
| 풍도 | 22303 | 미확인 | 다른 만. 대표성 없음 |
| 외연도 | 22108 | 2009-10-21 | 기록 16년이나 외해. 반폐쇄 내만을 대표 못 함 |
| 덕적도 | 22101 | 1996-07-01 | 기록 30년이나 거리가 멂 |

가까운 곳은 기록이 짧고, 기록이 긴 곳은 멀다. 성층 요인을 조사했을 때와 같은 패턴이다.
내파수도는 2030년경 5년치가 쌓이면 쓸 수 있다.

---

## 16. 미확정 항목

### 1주차에 결정할 것 (§12 일정 ①~⑨)

| 항목 | 왜 미룰 수 없나 |
|---|---|
| **실시간 API가 창리를 주는가** | 8/3. 후퇴선이 여기서 갈린다 |
| **2022~2025 천수만 특보 발령 이력** | §2 표와 §13 일치율의 재료 |
| **평년값 창의 경계 처리** | 분석 기간 밖 자료 사용 여부. 215/172/129와 골든 케이스가 걸림 |
| **"3년 미만이면 판정 안 함"이 연 단위인가 일 단위인가** | 2020년 5~7월 초는 실질 2년치 |
| **30분 → 1시간 변환 여부** | 채택하면 `obs_count` 만점이 24가 되어 결측률·20% 임계·견본 C가 재계산됨 |
| **결측률 20% 임계의 근거** | 채택 임계 90은 근거를 적어놨는데 이쪽은 없다 |
| **지도 배경 이미지 출처와 네 모서리 좌표** | 교체는 3주차지만 출처·좌표는 먼저 |
| **정보 탭 기준값·확인일·문구 출처** | 화면에서 유일하게 §10 검사기를 안 거치는 부분 |
| **위기경보 관심·주의·심각 단계의 발령 기준** | 확인 안 되면 "공개된 기준 없음"이라고 적는다 |
| **위기경보 '심각' 단계의 세분화 여부** | 확인 전까지 목업에 '심각'으로만 적는다 |
| **견본 E — `anomaly` 음수인 날** | §10 렌더 검사 18·19를 돌릴 근거가 없다 |
| **채점용 이상군 일수** | §13 유의성 검정 선택에 필요. 표본 크기만 세고 성능은 보지 않는다 |
| **속보의 배경장 계열 표현 빈도** | §13 대응표가 열유입으로 기울어 있는지 확인 |

### 2주차

| 항목 | |
|---|---|
| **견본 D — 실시간(`rolling_24h`) 형태 1건** | 8/3 API 확인 성공에 의존. 실패하면 §10 렌더 검사 22번을 미실행으로 기록 |
| **갱신 파이프라인 구현** | §12 아키텍처 표대로 |
| **전문용어 재검토** | `배경장`·`백분위`를 실제 화면에서 판단 |
| **배지와 §0의 긴장 재검토** | 배지 제거 / 색 미사용 / 한정 문구 세 안을 화면에서 비교 |

### 3주차

| 항목 | |
|---|---|
| 조석 3분류 판정 규칙 | M1은 손입력 |
| **평년값 창 ±1년 민감도** | 견본 A의 채택이 창 선택에 얼마나 의존하는지 |
| **롤링 평년값의 추세 편향 측정** | §3. 실패 기준 해석의 전제 |
| **유의성 검정 방법 확정 후 커밋** | 채점 전에. 결과를 보고 고르지 않는다 |
| AI 모델과 단가 | 1시간 주기 × 문장 2개 기준 월 비용 |
| 계산 스크립트 저장소 커밋·고정 | §14 교훈 |

### 출처를 문서에 병기해야 할 것

| 항목 | 현재 근거 |
|---|---|
| 속보의 천수만 행에 창리가 쓰이는 방식 | 2017년 제1호·2022년 제65호 고수온속보 → **인용문 병기 필요** |
| 2024년 표층수온 관측 사상 최고 | 수산과학원 발표 → **제목·일자 각주 필요** |
| **가로림만 해역 정의** | **저수온** 특보 해역 고시 → **출처 병기 + 고수온에도 같은 구분이 적용되는지 확인** |
| 지점 6곳의 해역 분류 | 좌표 기반 판단 → 확인 전까지 지도에 `(추정)` 표기. **창리 포함 여섯 곳 모두**(§5) |
| **2024년 이전 예비특보 기준값** | **미확인.** 확인 안 되면 그 시기 `preliminary` 미표시 |
| **온배수 영향** | 어업인 증언 보도뿐. **문헌 근거를 못 찾으면 화면 상시 노출에서 빼고 잔차 분석 결과로만 언급한다** |
| **수산과학원 대책기간이 매년 5/15~9/30인가** | 미확인. §2에서 "우리가 정한 분석 기간"으로 재규정해 의존을 끊음 |
