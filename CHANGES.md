# 반영 대기 (PRD 미반영)

화면이 PRD와 어긋나게 될 때마다 여기에 한 줄씩 쌓는다.
**PRD 파일(`03_PRD_3.6.md`)은 승인 전까지 고치지 않는다.**
이 목록이 다음 판(3.7)에 한꺼번에 반영할 후보다.

## 2026-07-31

| # | 절 | 무엇 | 왜 |
|---|---|---|---|
| 1 | §11 | 머리말을 두 줄로 — `{지점명}` / `{해역} 해역(확인 중)` (표기는 9번을 따른다) | 지도와 설명이 같은 지점을 다르게 불렀음. 2026-07-31 프로토타입에서 확인 |
| 2 | §11 | 연속일수 줄을 자료시각 → 상태배지로 | 배지 판정의 근거이므로. 2026-07-31 프로토타입에서 확인 |
| 3 | §11 | 배지 옆에 "국립수산과학원 기준" 한 줄 + 화면 아래(ⓘ 안내 뒤)에 근거 박스 "기준: 국립수산과학원 고수온 특보" | 어느 기관 기준인지 화면에서 알 수 없었음. 자세한 설명을 배지 옆에 두면 우리 계산과 공식 특보가 짝지어진 것처럼 읽혀 아래로 내림. 2026-07-31 프로토타입에서 확인 |
| 4 | §5·§16 | 지도 bbox **확정: 북 36.95 / 남 36.15 / 서 125.95 / 동 126.90** (충남 서해안 전체, 85 × 89km). 1주차 배경지도를 이 범위로 캡처한다 | §5 가 "네 모서리 위경도를 §16 에 못 박고 변환식을 고정한다"고 한 그 값. §16 미확정 항목에서 내려온다. 2026-07-31 프로토타입에서 확인 |
| 5 | §5 | 개념도에 해역명 라벨 `가로림만 해역` · `천수만 해역` 추가. 지도 라벨에는 `(확인 중)` 을 붙이지 않는다 | 지곡이 창리와 다른 만이라는 것이 지도에서 보여야 함. 확인 대상은 만의 위치가 아니라 "각 관측점이 어느 해역에 속하는가"이고, 붙이면 45px 폭 만에 라벨이 들어가지 않는다. 2026-07-31 프로토타입에서 확인 |
| 6 | §5·§11 | 준비 중 지점을 누르면 **모달**로 안내한다. §11 의 한 줄 응답 문구(`{지점명} ({해역}) — 이 지점은 설명을 준비 중입니다.`)를 대체한다. 지도의 점과 접힌 목록 양쪽에서 열린다. **수온을 비롯한 어떤 값도 넣지 않는다** | 한 줄 응답은 접힌 목록 구조에서 놓을 자리가 없었고, 어디에 떴는지도 알기 어려웠다. 검증하지 않은 지점이므로 값은 넣지 않는다. 2026-07-31 프로토타입에서 확인 |
| 7 | §11 | 지도 출처 표기를 "배경지도 없음(개념도)" 으로 | 외부 배경지도를 쓰지 않아 적을 출처가 없음. 2026-07-31 프로토타입에서 확인 |
| 8 | §10 | `{factor_as_of}` 를 factors 중 가장 이른 `as_of` 로 계산 | 명세는 "ASOS 공통"인데 파일에 ASOS 표시가 없어 식별 불가. 견본 A 에서는 결과가 같음(7/30). 2026-07-31 프로토타입에서 확인 |
| 9 | §5·§11 | 해역 표기를 `{해역}(추정)` → `{해역} 해역(확인 중)` 으로 | 화면만 보면 뭐가 추정인지 알 수 없고, 수온이 추정치라는 오해 소지가 있었음. 2026-07-31 프로토타입에서 확인 |
| 10 | §5 | 요인 막대: 임계선을 **점선(1.5px dashed, 간격 3px)** 으로 명시. 마커는 **10px** | 실선은 마커에 덮여 안 보였고, "기준선"이라는 의미도 점선이 더 맞다. 2026-07-31 프로토타입에서 확인 |
| 11 | §6 | **M1 범위 밖에 추가** — 지점 선택 없이 확장. `station.html?station={id}` 로 준비해 두었다. 지점이 늘면 창리 전용 버튼을 없애고 지도의 점과 목록 항목을 직접 링크로 바꾼다 | 지금은 설명하는 곳이 한 곳뿐이라 선택 UI 가 필요 없다. 구조만 열어 두었다. 2026-07-31 프로토타입에서 확인 |
| 12 | §6 | **M1 범위 밖에 추가** — 지도 확대·축소·지역 확장. 정적 이미지 1장으로는 불가능하다. 축척별 이미지 여러 장이나 타일 서비스가 필요한데, 타일은 API 키가 필요해 §5 의 오프라인 원칙과 충돌한다. `geo.js` 의 좌표 변환은 bbox 를 인자로 받게 해 두었다 | 확대·축소를 하려면 bbox 가 바뀌어야 하는데 변환식이 상수에 묶여 있었다. 함수는 그대로 두고 bbox 만 갈아끼울 수 있게 고쳤다. 2026-07-31 프로토타입에서 확인 |
| 13 | §5 | **정보 화면 특보 단계에 관행 색**(노랑 · 주황 · 빨강) 도입. 제도 설명이라 "지금 위험하다"를 주장하지 않는다. 위기경보는 **회색 농도** — 같은 계열이면 `경보`와 `경계`가 짝지어져 보이고, 4단계 중 기준을 아는 것은 `경계` 하나뿐이라 색으로 단정할 수 없다. 형태도 **원 / 사각**으로 나눠 흑백에서도 구분되게 했다. **설명 화면 배지는 회색 유지.** `--danger` 등 토큰은 여전히 금지 | 정보 화면은 제도를 설명하는 곳이고 설명 화면은 오늘을 말하는 곳이라, 같은 규칙을 쓰면 한쪽이 어색해진다. 색은 `app.css` 의 정보 화면 전용 변수(`--app-info-*`)에 가둬 두고 "상태 표시에 쓰지 않는다"를 주석으로 못 박았다. 2026-07-31 프로토타입에서 확인 |
| 14 | §11 | **한계 블록 7줄을 두 박스로 분리** — `이 서비스가 답하지 않는 것`(3줄) / `이 자료의 한계`(4줄). 문구와 줄 수는 그대로 | "서비스가 하지 않는 일"과 "자료의 한계"는 성격이 다르다. 한 박스에 담으면 어느 쪽을 읽고 있는지 흐려진다. 두 번째 박스의 제목 `이 자료의 한계` 만 §11 에 없는 새 문구다. 2026-07-31 프로토타입에서 확인 |

| 15 | §11 | **미채택 그룹도 요인 값과 막대를 표시한다.** 채택 그룹과 같은 형식(이름·상위 N%·막대·값·임계선 라벨) + 그 아래 `판정 기준에 못 미칩니다`. 구분은 그룹 이름·요인 이름을 한 단계 흐리게(`--wv-text-muted`)만 하고 막대·마커·임계선·숫자는 그대로. **채택 그룹이 없을 때 그룹을 나열하지 않던 규칙도 함께 뒤집는다** — `평년을 크게 벗어난 요인이 없습니다.` 를 먼저 짧게 두고 그 아래에 모든 그룹을 같은 형식으로 나열한다 | `판정 기준에 못 미칩니다` 한 줄만으로는 왜 못 미치는지 알 수 없다. 견본 A 의 배경장은 백분위 61.0(상위 39%)인데 그 숫자가 화면에 없었다. 나열하지 않으면 우리가 아무것도 안 본 것처럼 읽혀 무책임해 보인다. 2026-07-31 프로토타입에서 확인 |

| 16 | §8 | **스키마에 `adopted: null` 규약 추가 필요** — `false`("봤는데 못 미쳤다")와 `null`("아직 못 봤다")은 다르다. 화면은 `null` 일 때 막대를 그리지 않고 `값을 아직 확인하지 못했습니다` 로 표시한다 | `null` 을 `false` 로 뭉치면 확인하지 않은 것을 확인한 것처럼 주장하게 된다. 감추면 견본 B 의 "셋 다 평범했다"가 둘만 보여 반쪽이 된다. 2026-07-31 프로토타입에서 확인 |
| 17 | §9 | **견본 표에 미확보 필드 목록을 명시할 것.** 지금은 `확보: 완료` 로만 적혀 있어 무엇이 빠졌는지 파일을 열어야 안다 | 견본 B·C 를 만들면서 §9 표에 없는 값이 여럿 나왔다(아래 목록). 3주차 원자료 재계산 전까지는 `null` 이다. 2026-07-31 프로토타입에서 확인 |

### 견본 B·C 미확보 값 — 3주차 원자료 재계산 대상

지어내지 않고 `null` 로 두었다. 각 파일 상단 `_unconfirmed` 필드에도 같은 목록이 있다.

| 견본 | 미확보 필드 |
|---|---|
| **B** (2021-08-05) | 배경장 30일 누적편차 값·백분위 · 관측 횟수 · 결측률 · 조석 조건 · 요인 `as_of` · 평년 표본 수(일사·기온) |
| **C** (2021-08-11) | `climatology.years` · `climatology.sample_size` · `withheld_reason` |

화면 처리 — B 의 배경장은 `값을 아직 확인하지 못했습니다`,
C 의 한계 블록은 `평년값: 최근 N년 · 표본 N일` 줄만 감추고 나머지 세 줄은 그대로 노출한다.

`as_of` 는 §7 규칙(ASOS 7일 창 = D−7~D−1)으로 2021-08-04 가 나오지만 §9 가 명시하지 않아 `null` 로 두었다.
`clim_sample_size` 는 날마다 다시 세는 값이라 견본 A 의 158/172 를 옮겨 쓸 수 없다.

**견본 B 의 배경장 그룹 이름을 `이전부터 높았던 수온` 으로 적었다.** 견본 A 는 아직 `배경장` 이다 —
둘 중 하나로 통일해야 한다.

### 배경 — 4번 bbox 가 세 번 바뀐 경위

| | 1안 | 2안 | **확정** |
|---|---|---|---|
| 범위 | 북 36.94 / 남 36.33 / 서 126.13 / 동 126.61 | 북 36.66 / 남 36.35 / 서 126.15 / 동 126.60 | **북 36.95 / 남 36.15 / 서 125.95 / 동 126.90** |
| 실거리 | 42.9 × 67.8 km | 40.3 × 34.5 km | **85.0 × 88.9 km** |
| 가로:세로 | 1 : 1.58 | 1 : 0.86 | **1 : 1.05** (거의 정사각형) |
| 개념도 높이 | 552 px | 299 px | **366 px** |
| 지곡 | 안 | 밖 | **안** |
| 접은 이유 | 지곡 하나 때문에 세로가 두 배 | 천수만에 너무 붙어 어디인지 모름 | — |

2안에서 쓰던 "지도 밖 지곡 화살표 줄"은 철회했다. 지곡이 다시 지도 안으로 들어왔고,
같은 사실(다른 만이다)을 해역명 라벨이 대신 말한다.
`geo.js` 의 `distanceKm()` · `placeOffMapNotes()` 는 남겨 두었다 — 지금은 쓰는 곳이 없다.

### 1주차 배경지도 캡처 지침

확정 bbox 가 곧 캡처 범위다. 지침은 `assets/geo.js` 머리말에도 적어 두었다.

| | |
|---|---|
| 범위 | 북 **36.95** / 남 **36.15** / 서 **125.95** / 동 **126.90** |
| 비율 | 가로 : 세로 = **0.956 : 1** (85.0 × 88.9 km). 이 비율로 자른다 |
| 여백 | 없이 자른다. 이미지 가장자리가 곧 bbox 경계다 |
| 출처 | 브이월드 · OpenStreetMap · 국립해양조사원 / 국토지리정보원. 카카오·네이버·구글 캡처는 재배포 금지 (PRD §5). 화면에 출처 표기 (§11) |

비율이 어긋나면 `toPixel()` 이 어긋나 관측점이 육지에 찍힌다.
교체할 때 고칠 곳은 `index.html` 의 `<path class="app-land">` 두 개뿐이고,
점의 좌표는 다시 찍지 않는다.

---

## 결정을 기다리는 것

PRD 와 어긋나지는 않지만 확인이 필요한 것들. 확정되면 위 표로 옮기거나 지운다.

| 절 | 내용 |
|---|---|
| §16 | 국립수산과학원 고수온 특보 페이지의 정확한 주소. 지금은 기관 대표 주소(`www.nifs.go.kr`)를 링크하고 있다 |
| §16 | 해역 구분 확정. 확정되면 여섯 곳의 `(확인 중)` 을 한꺼번에 뗀다 (표기는 9번, 고칠 곳은 `geo.js` 의 `STATIONS` 한 군데) |
| §11 | 한계 블록 `관측점 한 곳의 자료이며 해역 전체를 대표하지 않습니다` 가 414px 폭에서 두 줄로 감긴다(402px / 374px). "7줄이 최종"인데 화면에서는 여덟 행이 된다. **블록을 만들어 실제로 확인함**. 그대로 두거나 / 그 블록만 14px 로 내리거나 / §11 문구를 줄이거나 |
| §5 | 지도에 창리의 수온을 표시할지. §11 「지도」 문구 목록에 수온 표기 형식이 없어 지금은 표시하지 않는다 |

---

## 디자인적용지침과 어긋난 것

PRD 가 아니라 `디자인적용지침.md` 기준이다.

| 장 | 무엇 | 왜 |
|---|---|---|
| 1장 | 타이포 스케일 전 단계 +2px (`--wv-text-md` 16→18) | 중장년·야외 기준으로 기본 스케일이 작음. `waves-design-system.css` 는 수정하지 않고 `app.css` 에서 토큰만 덮어씀 |
| 2장 | 수온을 `--wv-text-5xl`(52px)로 | 지침은 "`.wv-display`(50px)가 아니라 `.wv-title` 정도가 적당"인데, 설명 화면에 들어왔을 때 가장 먼저 보여야 한다는 판단으로 더 키웠다 |
| 2장 | 모든 박스를 컨테이너 안(374px)에 두고 모서리를 `--wv-radius` 로 둥글림 | 한때 여백 밖으로 빼내 전폭(414px)으로 썼으나, 모서리를 둥글리면 곡선이 화면 가장자리에 붙어 잘린 것처럼 보인다. 요인 카드·근거 박스·한계 박스·기준 박스가 모두 같은 폭·같은 모서리다. 요인 막대 트랙 폭은 374 → **334px** 로 되돌아갔다 |
| 2장 | 육지 색을 `--wv-surface-raised` → `--wv-neutral-100` | 라이트 테마에서 `--wv-surface-raised` 가 순백이라 카드 배경과 같아 육지가 보이지 않음 |
