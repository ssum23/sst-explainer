# 반영 대기 (PRD 미반영)

화면이 PRD와 어긋나게 될 때마다 여기에 한 줄씩 쌓는다.
**PRD 파일(`03_PRD_3.7.md`)은 승인 전까지 고치지 않는다.**
이 목록이 다음 판에 한꺼번에 반영할 후보다.

| # | 절 | 무엇 | 왜 | 근거 |
|---|---|---|---|---|
| 1 | §8 | `sst_basis` 만점 **48 → 24**. 과거 재현도 **1시간(H1 정시)** 으로 내린다 | §16 ⑧ 결정 1. 실시간이 하루 24점으로 이미 강제되어 있고(① API 확인), 사전 기준 판정이 **9.0%(57/635) ≥ 5%** 로 충족됐다 | ⑧ |
| 2 | §8 | `obs_count` 「만점 48」 → **24** | 1과 같음 | ⑧ |
| 3 | §8 | **`daily_max_sst`는 30분 기준을 유지**한다고 명시 | 채점 분류 전용이고 화면·문장·자리표시자가 읽지 않는다(코드 사용처 0건). 함께 내리면 이상군/대조군 분류가 바뀐다(2017-07-21 등). **필드마다 시간 해상도가 다르다는 사실이 문서에 없다** | ⑧ |
| 4 | §8 | 완성본 설명 「입력본 + `explanation` + `daily_max_sst` — 화면과 채점이 읽는다」(`03_PRD_3.7.md:742`) | 화면이 `daily_max_sst`를 읽는 것으로 오해된다. 화면이 읽는 것은 완성본이고 그 필드는 채점만 쓴다 | ⑧ |
| 5 | §9 | 견본 C 관측 횟수 **29 → 14**, 결측률 **39.6% → 41.7%** | 결정 1. 판정은 **보류 그대로**라 견본의 역할은 유지된다 | ⑧ |
| 6 | §9 | 견본 A 관측 횟수 **48 → 24** · 평년값 **26.25 → 26.19**(26.1937) · 평년 대비 **+2.23 → +2.27**(2.2729) · 평년 표본 **172 → 168** | ⑧ H1 + #38 적용 확정값. 일평균 표기값 28.47은 바뀌지 않는다(28.4708 → 28.4667). **화면(1자리)은 평년값 26.2로 그대로인데 평년 대비가 2.2 → 2.3으로 바뀐다** — #39 | ⑧⑦ |
| 7 | §9 | 견본 B 관측 횟수 **→ 24** · 평년값 **26.67 → 26.62**(26.6244) · 평년 대비 **+1.96 → +2.00**(1.9964) · 평년 표본 **172 → 168** | ⑧ H1 + #38 적용 확정값. 일평균 표기값·일 최고 29.10은 안 바뀐다. **화면(1자리)은 평년값이 26.7 → 26.6으로 바뀌고 평년 대비는 2.0으로 그대로다** | ⑧⑦ |
| 8 | §9 | ~~견본 B 일평균 표기값 28.62 → 28.63~~ **철회 — ⑧ 확정으로 근거가 사라졌다** | 30분 원값이 정확히 **28.6250**이라 파이썬 `round()`의 은행가 반올림 값 28.62가 사사오입 28.63과 갈렸다. **H1에서는 원값이 28.6208이 되어 어느 방식으로도 28.62다.** 버그가 실물로 나타난 사례는 사라졌지만 **규칙(#9·#10)은 그대로 유효하다** | ⑧ |
| 9 | §10 | 사사오입 **구현 주의** 추가 — 파이썬 `round()` 금지, **1/10 단위 정수 산술**로 계산 | §10:1144-1145가 쓴 「표기값을 다시 반올림하면 **26.25 → 26.3**」이 `round()`로는 **26.2**가 되어, 문서가 든 예시 자체가 재현되지 않는다. 그 26.25는 견본 A `climatology_mean`에 실물로 있다. 0.05~400.95 경계값 전수 검사 — **파이썬 `round()` 2005건 오답 / JS `Math.round(x*10)/10` 0건**. `assets/format.js`는 이미 맞고, 위험한 곳은 **갱신 파이프라인·채점 스크립트(파이썬)** 다 | ⑧ |
| 10 | §10 | 평균을 **float으로 낸 뒤 반올림하지 말 것** | 9와 다른 자리다. 2017-06-17 창리 일평균은 정확히 22.25℃인데 48개를 float으로 더하면 22.2499…가 되어 22.3이 22.2로 내려간다. **반올림 함수를 어떻게 짜든 들어가는 값이 이미 틀린다** | ⑧ |
| 11 | §11 | 판정 보류 문구 `{전체}` 설명의 「48」 → **24** | `{전체}` = `obs_count ÷ (1 − 결측률)`. §11이 「48을 적어 두면 조용히 틀린다」고 경고해 둔 자리다. `station.js:447`은 이미 나눗셈으로 만들고 있어 **코드 수정은 없다** | ⑧ |
| 12 | §13 | 보류율 표에 「관측 자체가 없던 날 31일」의 **내역 병기** — **29일은 2017-05-15~06-12 관측 개시 전**, 2일은 **2021-07-05 · 2021-09-05** | §16 ⑧ 결정 2. 분자·분모는 안 「가」로 **지금 그대로 둔다**(CHANGES #7이 분모를 695로 정한 근거 「관측이 없던 날도 보류다」와 일관). 다만 8.5% 중 **4.2%p가 관측소가 없던 기간**이라 병기하지 않으면 성능으로 오독된다 | ⑧ |
| 13 | §13 | 보류율 표 값(**695 / 31 / 28 / 8.5%**)은 **변경 없음 — 재현 확인** | `scripts/verify.py hourly`가 네 값을 그대로 낸다. 시간 해상도를 바꿔도 8.5% → 8.6%로 ±0.1%p다 | ⑧ |
| 14 | §16 | ⑧ **닫힘** | `data/verify/2026-08-03_시간해상도.md` | ⑧ |
| 15 | §16 | 결측률 임계 20% 항목에 ① 「**만점이 24가 되면 허용 결측이 9회 → 4회**」 ② **이 임계가 가르는 곳 셋** 을 명시 | ①은 ⑧. ②는 #38의 결과 — 임계 하나가 **판정 보류(§8) · 배경장 30일 창(#29) · 평년 표본(#38)** 셋을 가른다. **임계에 근거가 없다는 문제의 영향 범위가 셋으로 넓어졌다** | ⑧⑦ |
| 16 | — | **견본 JSON 3종 재생성 — 완료** (2026-08-03) | 확정 규칙 전부 적용. `python3 scripts/verify.py build` 로 만들고 `evidence` 로 검증한다 — **세 견본 모두 코드가 재현한다.** 바뀐 필드는 견본 A 15개 · B 20개 · C 4개 | ⑦⑧ |
| 17 | §1 | RISA와 실시간어장정보는 **「별개 창구」가 아니다** | id가 문자 그대로 `risaList`다. 같은 시스템의 API 창구다 | ① |
| 18 | §12 | 인증키 발급처가 **공공데이터포털이 아니라 국립수산과학원**(`nifs.go.kr`) | 「인증키는 세 종류다」 표 정정. 4종 신청·전부 승인(2026-08-03), 활용기간 ~2027-08-03. 우리가 쓰는 것은 **실시간어장정보 키 하나**(`.env`의 `NIFS_KEY`) | ① |
| 19 | §12 | **API 주소·리다이렉트·인코딩을 명시** — `https://www.nifs.go.kr/api/OpenAPI_json` · 활용가이드 PDF 주소는 **302 리다이렉트**라 `-L` 없이는 **0바이트** · JSON도 **EUC-KR** 선언 | 갱신 파이프라인 구현의 전제다. 문서 안에서 철자도 어긋난다 — 항목표 `repaire_gbn`·`resultMng` ↔ 실제 응답 `repair_gbn`·`resultMsg` | ① |
| 20 | §12 | 갱신 파이프라인에 **`obs_dat` 최신성 검사** 추가 | `risaList` 응답에 `obs_dat`가 `2026-04-16`인 관측점이 섞여 있다. **4월 이후 갱신이 멈춘 지점이 있다**는 뜻이다 | ① |
| 21 | §5 | 지점 표에 **실시간 제공 여부 열** | 실시간은 여섯 곳 중 **셋뿐**이다 — 창리·지곡·고남 있음 / 안면도·내포·삽시도 없음. 과거 zip에는 여섯 곳이 다 있어 **목록의 범위가 다르다.** 화면은 설명하지 않는 지점에 수온을 띄우지 않으므로(§5) **지금 그대로 두어도 된다** | ① |
| 22 | §6·§15 | 「저층 자료가 없다」는 맞으나 **중층은 온다** | 창리 `bot_tmp_yn=N`이고 **서해 관측소 전체에 저층이 없다** — §6이 성층·바람을 M1 밖으로 둔 근거는 오히려 강해졌다. 그런데 `risaList` 실측에 **`obs_lay=2`가 온다**(29.0℃). 표층 29.7 − 중층 29.0 = **0.7℃**가 실제로 계산된다. **「자료가 아예 없다」는 서술이 정확하지 않다** | ① |
| 23 | §13 | 실패 기준의 `12.6%` · 「실효 독립 검정 수 **1.3개**」가 **3.6 값**이다 | **1.3개의 출처를 찾았다** — 12.6%(옛 창 대조군) → k=1.27이다(#34). 확정 규칙에서 대조군은 **열유입만 10.8%(k 1.09)** · **하나라도 11.3%(k 1.14)** 다 | ①⑦ |
| 24 | §13 | 침묵 일치율의 「개발용에서 이상군 채택률이 **31.6%**」가 **3.6 값**이다 | 3.7은 **44.4%**. 31.6%도 옛 창(D−6~D) 값이며, `verify.py rates`가 두 창을 나란히 찍어 확인된다 | ① |
| 25 | §13 | 배경장 — 「빼지 않고 남긴다」고 적어놓고 **대표 표는 뺀 값(4.1배)** 을 쓴다 | 남긴다면 대표 표가 **2.4배**여야 하고 분자 정의도 「하나라도 채택」이어야 한다. 다만 §13이 **사전 고정한 배경장 판단 기준은 배경장 단독 채택률**을 요구하므로 표에 **세 줄**이 필요하다 — 열유입 단독 / 배경장 단독 / 하나라도 | ① |
| 26 | §7 | 창 경계를 **A1「분석 기간 밖 자료를 쓴다」로 확정** | ⑦ 결정 A. **측정 없이 규칙만으로 정해졌다** — A2는 2020년 대상일 5/15에서 평년 표본 **44일**을 만드는데(2017년 기여 0일, 개시 6/13), §7 표가 판정 가능의 하한으로 적어 둔 **「3년 = 129일」의 3분의 1**이라 최소 3년 규칙과 정면 충돌한다 | ⑦ |
| 27 | §7 | §2와의 긴장에 **답을 본문에 적는다** | §7:702-704가 「A1은 §2와 어긋난다」고 적었으나 어긋나지 않는다. **분석 기간은 판정을 내리는 날의 범위**이고 **평년 표본은 그 날을 재기 위한 참조 분포**다. 9월 20일을 재려고 10월 초를 참조하는 것은 10월을 판정한다는 뜻이 아니다 |  ⑦ |
| 28 | §7 | **배경장 30일 창에 §7 보간 금지를 그대로 적용한다 — 확정** | 예외를 두려면 「왜 7일 창은 엄격하고 30일 창은 느슨한가」에 답이 있어야 하는데 없다. **30/N 환산은 없는 날을 평균으로 메우는 것과 수학적으로 같으므로 보간이다** — §7이 금지한 바로 그것이다. §7:720이 「7일 창」으로만 적어 둔 것을 30일 창까지 넓혀 쓴다 | ⑦ |
| 29 | §7·§8 | **결측률 20% 초과일은 배경장 창에서도 결측으로 본다 — 확정** | §8이 그런 날의 `current_sst`를 `null`로 두고 판정 보류로 선언했다. 화면에 「이 날의 수온은 믿을 수 없다」고 말해놓고 그 일평균을 다른 계산에 쓰면 모순이다. **평년값도 같은 집합에서 만든다** — 신뢰 가능한 일평균은 1588/1651일 | ⑦ |
| 30 | §9·§14 | **견본 A 배경장 `12.3971` / `61.0` / `172` / `quality:"ok"` → 전부 `adopted: null`** | 어느 규칙으로도 12.3971이 나오지 않는다(산출 불가 / 25.9329 / 26.8271 / 24.9847 — 전부 그 두 배쯤). §12:1496 「그 값을 낸 코드가 어디에도 없었다」·§14 #2·#3과 같은 자리다. **원인 추적은 멈추고 새 규칙으로 재산출해 §14에 정정으로 남긴다.** 확정 규칙에서 견본 A는 창 30일 중 **5일**이 결측이라 산출 불가다 | ⑦ |
| 31 | §7 | 「골든 케이스의 표본 172일이 그 규칙 아래서도 맞는지 재확인해야 한다」(§7:705)는 **A를 검증하지 못한다** | 세 견본의 ±21일 창이 전부 분석 기간 안이라 **172는 A1이든 A2든 같다.** A는 경계에 걸리는 날을 따로 만들어야 확인된다 | ⑦ |
| 32 | §16 | ⑦ **닫힘** — A1 · #28·#29·#38 · B·C(일 단위 · C2) | `data/verify/2026-08-03_평년창경계.md` | ⑦ |
| 33 | §13 | **§13 검산표를 확정 규칙으로 재산출** — 판정 대상일 **250 → 221**(평년 3년 미만 −30 줄 신설), 대조군/이상군 **231/19 → 203/18**, 배경장 채택 **17 → 1**, 대조군 「하나라도」 **11.3%**(k 1.14) | ⑧ H1 + #28·#29·#38 + ⑦ C2 를 모두 적용한 결과. **대조군 열유입 10.8%(k 1.09)·이상군 44.4%(k 5.58)는 그대로이고 실패 기준 1은 통과한다** | ⑧⑦ |
| 34 | §13 | **실효 독립 검정 수의 식을 §13에 적는다** — `k = ln(1 − p) / ln(0.9)` | §13의 「1 − 0.9^k」를 k에 대해 푼 것. **검산 k=1 → 10.0% · k=2 → 19.0% · k=3 → 27.1%로 §13 표와 일치한다.** `verify.py`의 `effective_tests()`에 넣었고 `rates`가 열로 찍는다 — **식이 코드에 있으므로 §12:1496이 되풀이되지 않는다.** k는 **원 비율**에서 계산한다(반올림된 %에서 다시 계산하는 것은 §10:1144가 금지한 「표기값을 다시 반올림」과 같은 구조다) | ⑦ |
| 35 | §8 | 배경장 미산출일의 결과를 **명시** — `adopted: null`, 판정 보류가 **아니다** | 새 규칙에서 개발용 판정 대상일의 **47%(117/249)** 가 여기 해당한다. ASOS 요인은 그대로 판정되고 §13 분모도 안 바뀐다. §8의 「`adopted`는 세 가지다」 규약이 여기 쓰인다. `station.js:255`가 이미 분기하므로 **렌더러 수정은 없다** | ⑦ |
| 36 | §9 | **견본 B 표에 배경장 행을 신설한다** — `35.4819` ℃·일 · 표본 **155일** · 백분위 **69.7 → 미채택** | 지금 §9 견본 B 표에 배경장 행이 아예 없고 `sample-b.json`은 전부 `null`이었다(§9:964). 새 규칙에서 산출되므로 **순수한 추가**다. 견본 B가 **열유입 69.2 · 배경장 69.7로 두 그룹 모두 값을 가진 채 미채택**이 되어 §14 #26의 「미채택 그룹도 값과 막대를 보여준다」 화면을 맡는다. **35.4810이 아니라 35.4819다** — 앞 값은 30분 기준이었다 | ⑦ |
| 37 | §16 | ⑨(견본 E 선정)에서 **「미채택 그룹 값·막대」 견본을 따로 만들 필요가 없다** | #36이 채운다. 검토 대상에서 내린다 | ⑦ |
| 38 | §7·§8 | **#29를 §8 `climatology_mean`에도 확장 — 확정.** 원칙 한 줄로 둔다: **「판정 보류일의 일평균은 어떤 계산에도 쓰지 않는다」** | 범위를 배경장 창에만 한정하면 「배경장에서는 못 믿는 값을 평년 표본에서는 믿는다」를 설명해야 하는데 답이 없다. 비용은 표본 **172 → 168**(2.3%)로 작고 §7의 3년 하한 129일에서 한참 위다. **보류일 판정은 H1(만점 24) 기준으로 한다** — ⑧에서 결측률 정의가 바뀌었다. **반대 논거도 기록에 남겼다**(결측률 20% 초과여도 24회 중 19회 이하일 뿐이라 일평균 자체는 쓸 만하고, 「그날을 주장하지 않는 것」과 「참조 분포에서 빼는 것」은 다른 결정일 수 있다 — 단일 원칙의 단순함을 택한 것이지 반론이 틀려서가 아니다) | ⑦ |
| 40 | §7 | **「3년 미만이면 판정 안 함」은 일 단위이며 기준은 C2(43일 창에 22일 이상 기여한 해만 1년)** | ⑦ B·C 확정. 사전 기준(개발용 이상군이 18일에서 15일 미만으로 줄면 연 단위로 후퇴)을 숫자 보기 전에 고정했고, **C1·C2·C3 모두 이상군 18일로 불변**이라 기본안 C2를 그대로 택했다. 판정 불가가 되는 날은 전부 5~6월 초인데 이상군은 한여름이라 겹치지 않는다(2020년 이상군 0일 · 2021년 27일) | ⑦ |
| 41 | §7 | 연 단위를 쓰면 **표본 85일(실질 2년치)로 판정하는 날이 생긴다**는 사실을 명시 | 2020-05-15~05-17은 **2017년 기여 0일** · 2018년 43 · 2019년 42로 85일이다. **A2를 물리친 것과 완전히 같은 문제** — 「최소 3년」이 이름만 남는다. C2는 22일 이상 기여한 해만 세므로 3년이면 실제로 3년이고, 표본 최소가 **107**로 올라간다 | ⑦ |
| 42 | §7 | **C3(43일 전부)는 쓸 수 없다** | 판정 대상일이 249 → **79**로 무너진다(173일 판정 불가). 개시일이 아니라 **산발 보류일** 때문이다 — 창 어딘가에 결측이 하루만 있어도 그 해가 통째로 탈락한다. **보간 금지는 값을 만드는 규칙이지 표본을 세는 규칙이 아니다** | ⑦ |
| 43 | §13 | **「배경장 개발용 17일 채택」의 정체** — 13일 중 **12일이 2020-05-15~05-26**이고 백분위가 **전부 정확히 100.0**이다 | 그 구간의 평년 연도는 2017·2018·2019인데 **2017년이 하루도 기여하지 않아** 누적편차 기준선이 두 해로만 만들어진다. **기준선을 만든 두 해의 누적편차는 정의상 0 근처에 모이므로 대상 연도가 조금만 따뜻해도 백분위가 100으로 튄다** — 구조적 산물이다. C2에서 그 구간이 빠지면 배경장 채택은 **1일**만 남는다 | ⑦ |
| 44 | §13·§16 | **배경장 그룹이 개발용에서 사실상 발동하지 않는다**(221일 중 1일)는 사실을 기록 | #43의 결과. 배경장 산출률 자체도 53%다(#35). §13의 배경장 관련 지표(단독 채택률·대표 표)를 이 조건에서 어떻게 해석할지 §13·§16에 적어야 한다 | ⑦ |
| 45 | §3 | **(예정)** 채점 구간(2022~2025)의 산발 결측이 C2에 걸리는지 확인 — **접촉 기록 #8**로 올린다 | 표본 규모이므로 성능 정보가 아니다(§3 원칙, #2와 같은 성격). **개시일로 인한 판정 불가는 규칙만으로 배제됐다** — 2022는 평년 연도가 5년이라 2017이 빠져도 4년이 남고, 2023 이후는 2017을 아예 쓰지 않는다. 이 부분은 자료를 열지 않고 확인했다 | ⑦ |
| 39 | §5·§8·§10·§11 | **견본 A의 평년 대비 `2.2` → `2.3`** — PRD 다섯 곳에 박혀 있다 | #38의 결과. 평년값이 내려가면 평년 대비는 그만큼 올라간다(2.2232 → 2.2729). **§5:398 화면 목업 · §8:934 `explanation.plain` · §10:1111 `{anomaly_phrase}` 렌더 예시 · §10:1262 좋은 예 ✅ 렌더 문장 · §11:1355 「견본 A는 `2.2`」** 전부 바뀐다. `data/fsch6.json`의 `anomaly`·`anomaly_raw`와 `explanation`도 같다 | ⑦ |

| 46 | §8·§9 | **견본 A·B·C의 바뀐 값 전부** — §8 완성본 JSON 9줄(889·891·892·899·914·917·918·928·934) · §9 견본 A 5줄(991·992·993·996·1000) · 견본 B 3줄(1020·1021·1022) · 견본 C 2줄(1054·1055) | `data/verify/2026-08-03_견본재생성.md` 6절에 줄 단위로 정리했다. 견본 A 평년 **26.25 → 26.19** · 평년대비 **2.23 → 2.27** · 관측 **48 → 24** · 배경장 전부 `null` · 표본 **172 → 168**. 견본 B 평년 **26.67 → 26.62** · 평년대비 **1.96 → 2.00**. 견본 C **29회/39.6% → 14회/41.7%** | ⑦⑧ |
| 47 | §5·§9·§10 | **값이 바뀌어 문장이 틀리게 되는 곳 7줄** — 401(`48회 관측`) · 432(`표본 172일`) · 976(`+2.23, +1.96`) · 1116(`{obs_count}` = `48회`) · 1117(`{missing_rate}` = `39.6%`) · 1144·1247(원값 `28.4708`) | #46의 파급. 값만 고치면 되고 논지는 그대로다 | ⑦⑧ |
| 48 | §11 | **§11:1403의 백분위 예시가 성립하지 않게 된다** | 「배경장은 백분위 **61.0**(상위 39%)인데 그 숫자가 화면에 없었다」가 요인 영역 설계의 근거인데, **견본 A의 배경장이 이제 `adopted: null`** 이라 백분위 자체가 없다. 예시를 **견본 B(69.7)로 옮기거나** 문장을 고쳐야 한다 | ⑦ |
| 49 | §10 | **§10:1145의 「26.25 → 26.3」이 실물 근거를 잃는다** | 26.25는 견본 A `climatology_mean`에 실제로 있던 값이었는데 **재생성으로 26.19가 됐다.** 이중 반올림 금지 규칙 자체는 유효하나 예시가 가상이 된다. 규칙을 유지하되 「한때 견본 A에 있던 값」임을 밝히거나 다른 실물 예시로 바꾼다 | ⑦⑧ |
| 50 | §8 | §8:841 「§16의 30분→1시간을 **채택하면** … 견본 C의 39.6%가 전부 재계산된다」를 **사실로 고쳐 쓴다** | 채택했고 재계산됐다(14회 / 41.7%). 예고문이 그대로 남으면 미결로 읽힌다 | ⑧ |
| 51 | — | `assets/station.js:304` 주석의 견본 A 값이 **3.6 옛 창 값**이다 — 일사 `87.3` · 기온 `90.7` | 현재는 **88.6 / 92.4**다(§14 #19로 창 규칙이 D−7~D−1로 바뀌면서). 코드 주석이라 화면에는 안 나오지만, 읽는 사람이 옛 값을 사실로 받는다 | ⑧ |
| 52 | §8 | **`withheld_reason` enum이 정의되지 않았다** | 견본 C가 보류일인데 `withheld_reason`이 `null`이다. §11 판정 보류 문구가 결측 항목을 밝히기로 했으므로(3.7 #28) 코드값이 필요하다 | ⑦ |

| 53 | §2 | **「이 기간이 실제 특보를 다 담는가」 표를 채운다 — 9년 전부 확정됐다** | 천수만 한정. 2017 `7/31~8/29` · 2018 `7/26~9/4` · 2019 `8/8~8/29` · **2020 발령 없음** · 2021 `7/23~8/26` · **2022 `7/8~9/7`** · 2023 `7/28~9/20` · **2024 `7/24~10/2`** · **2025 `7/29~9/23`** | ④ |
| 54 | §2 | **답 — 첫 특보는 9년 전부 담고, 해제는 8년 중 7년을 담는다. 2024년만 놓친다** | 2024년 천수만 해제가 **10/2 14:00** 로 9/30을 **이틀 초과**한다. 2023년은 10일 · 2025년은 7일 여유로 들어왔다. **「특보 기간을 다 담는다」고 쓸 수 없다.** 앞쪽 경계는 여유가 크다 — 가장 이른 첫 특보가 **2022년 7/8** 로 5/15보다 54일 늦다 | ④ |
| 55 | §0·§16 | **창리가 특보 구역을 정의하는 지명으로 원문에 쓰인다 — §16 첫 줄을 닫는다** | 2017·2018 에 더해 **2021 제9호 · 2022 제3호**에서도 발령 구역이 「**서산시 창리 남측 횡단** ~ …」로 시작한다. §0 이 「`천수만(창리)` 형태의 공식 병기가 있는지 확인되지 않았다」고 적어 둔 자리다. **병기는 아니지만 그보다 강하다.** 다만 「대표 관측점」 단정은 계속 쓰지 않는다 — 구역 경계를 정의하는 것과 해역을 대표하는 것은 다르다 | ④ |
| 56 | §14 #11 | 2021년 7/23 은 **원문에 「천수만」 표기가 없다** | 제9호 원문은 「서해 내만·연안(서산시 창리 남측 횡단 ~ 신안군 장산도 남측 횡단…)」이다. **「천수만 7/23 발령」으로 굳히면 안 된다.** 다만 **구역 시작점이 창리라 우리 관측점이 발령 구역에 포함된 것은 원문 근거가 있다** — 3.7 의 「정황상 일치하나 원문 확인은 아니다」에서 한 칸 나아갔다 | ④ |
| 57 | §13 | **속보 원인 서술 4년치(2022~2025)가 전부 열유입이고 배경장 계열은 0건이다** | §13 이 「대응표가 열유입 쪽으로 기울어 있다」고 걱정하며 1주차에 세어 보라고 한 것. 「북태평양 고기압」·「폭염」·「장마 소강/소멸」뿐이고 「누적된 고수온」·「이전부터 높은 수온」 표현이 하나도 없다. **배경장 채택일은 구조적으로 전부 불일치가 된다** — 대응표 보강이 필요하다. #44(배경장이 개발용 221일 중 1일만 채택)와 같은 방향이다 | ④⑦ |
| 58 | §13 | **2020년 천수만 특보 없음이 검산표 「일 최고 28℃ 이상 0일」과 독립적으로 일치한다** — **속보 원문 22건 전수 확인** | 2020년 속보 22건 중 「천수만」이 나오는 9건은 **전부 금후전망의 「주의가 필요함」 문구**이고 특보상황 절에는 한 번도 없다. 관측값에서 나온 0일과 특보 이력에서 나온 발령 없음이 **서로 다른 두 경로인데 같은 답**이다. §13 이 「이상군은 특보 발령 여부로 정의하지 않는다」고 갈라 둔 두 정의가 2020년에서 같은 결론에 도달한다 — **관측값 기준 이상군 정의가 특보와 어긋나지 않는다는 증거** | ④ |
| 59 | §12 | **2022년 정답지가 채워졌다** | 조사 당시 2022년만 첫 발령일·해제일을 모두 확인하지 못해 **채점 구간 4년 중 첫 해가 정답지 없이 남을** 뻔했다. 원문으로 확정했으므로 §12 「자를 순서」 3순위(요인 일치율을 2025년만)를 자르기 전에 3년으로 줄어드는 일은 없다 | ④ |
| 60 | §3 | 접촉 기록 #5 「**(예정)** 1주차에 2022~2025 특보 발령 이력 확보」 → **완료**로 갱신 | 성능 정보가 아니다(발령 이력). 2022~2025 네 해 전부 천수만 기준으로 확정했다 | ④ |
| 61 | §16 | **고수온속보 게시판 접근 방법을 적어 둔다** | 3.7 은 「PDF 첨부만 있고 내려받기 주소가 자바스크립트로 감춰져 있다」고 적었으나 **목록 HTML 의 「원본」 칸에 `FILE_ID` 가 그대로 있다.** `/cmmnFile/fileDownloadStat.do?FILE_ID=` 로 받는다. 쪽 넘김은 **`selectPage`**(`pageIndex` 아님). **게시판은 UTF-8** — RISA API 의 EUC-KR(#19)과 다르다 | ④ |

| 62 | §8 | **`schema_version` `1.0` → `2.0`** · 판번호 기준을 §8에 적는다 | ③ 결정 6. **기준: 필드가 늘기만 하면 minor, 기존 필드의 뜻·값 범위가 바뀌면 major.** `obs_count` 만점 **48 → 24**(⑧)는 48을 가정한 소비자가 조용히 틀리므로 major다. `climatology.sample_size` 정의(#38)·`adopted:null`의 실제 사용도 같은 성격 | ③ |
| 63 | §8 | **`stage: "withheld"` 는 수온을 못 믿는 경우만이다** — 평년 3년 미만·ASOS 미산출은 `stage`를 정상값으로 두고 해당 값만 `null`, 그룹은 `adopted:null` | ③ 결정 1. §8:783-791의 보류일 규약(`current_sst` 등 전부 `null`)은 **수온 결측에만 맞는다** — ASOS 미산출인 날은 수온·평년 대비가 멀쩡한데 `null`로 덮으면 있는 자료를 감춘다. `stage`는 일평균 **절대값**(25/28℃)이라 평년이 없어도 낼 수 있다. 확인: 2021-08-13(ASOS 미산출)은 수온 28.41·평년 27.06·편차 1.35가 다 남는다 | ③ |
| 64 | §8 | **`withheld_reason` enum 신설** — `sst_missing`(20일) · `sst_absent`(2일) · `station_not_started` | ③ 결정 2. §13 보류율 표가 이미 세 갈래로 세는데(31 / 28, 그중 29가 개시 전 — #12) 필드가 하나면 표를 근거 파일로 재현할 수 없다. **`station_not_started`는 근거 파일에 나타나지 않는다 — §13 집계 전용**이며, 이 주석이 없으면 다음 사람이 「왜 이 코드는 한 번도 안 나오나」로 막힌다. `check_schema()`가 근거 파일에서 나오면 실패시킨다 | ③ |
| 65 | §8 | **겹칠 때는 수온이 먼저다** — 2020-05-27·28은 `sst_missing` | ③ 결정 3. 수온이 없으면 평년을 따질 것도 없다. 평년이 모자랐다는 사실은 `climatology.years`로 드러난다 | ③ |
| 66 | §8 | **`factors[].quality` 정의** — `ok`(값·백분위 둘 다) · `partial`(값만, 백분위 없음) · `null`(값도 없음) | ③ 결정 4. §8:870이 `ok`·`partial`이라 적어 두고 **언제 `partial`인지 정의하지 않았다.** 평년 3년 미만인 날이 정확히 `partial`이다 | ③ |
| 67 | §8 | **`judgment_withheld` 는 `stage == "withheld"` 의 파생값**임을 명시 | ③ 결정 5. 같은 사실을 두 필드가 들고 있어 어긋날 수 있었다(`station.js:394`가 `\|\|`로 읽어 드러나지도 않는다). 지우는 쪽이 깔끔하나 **없애는 것은 깨는 변경**이라, 동치를 `check_schema()`로 강제한다 | ③ |
| 68 | §8 | **실시간 필드 규칙** — `sst_basis == "rolling_24h"` ⟹ **`daily_max_sst == null`** | ③ 결정 7. 「최근 24시간 최고값」은 일 최고수온이 **아니라 다른 물건**이다. 같은 필드에 다른 정의를 담으면 §2가 경계한 「같은 28℃를 섞는」 상황이 재발한다. ⑧에서 `daily_max_sst`만 30분(만점 48)으로 남겼으므로 실시간에서 만들면 24점의 max라 **과거와 성질이 다르고** §14 #19가 그대로 걸린다. `check_schema()`가 검사한다 | ③⑧ |
| 69 | §11 | **모든 그룹이 `adopted:null` 인 날의 문구가 없다 — 그리고 화면이 거짓을 말한다** | `station.js:374`가 `groups.filter(g => g.adopted)`로 세는데 **`null`은 falsy**라 `adopted.length === 0`이 되어 **「평년을 크게 벗어난 요인이 없습니다.」를 출력한다.** 보지도 않고 평범했다고 말하는 것 — **§8:770이 금지한 바로 그 동작**이다. §11 요인 영역은 「채택 없음」과 「그룹 미채택」 둘뿐이고 **셋째 경우가 없다.** 지금은 잠복이나 **평년 3년 미만 28일이 렌더되면 드러난다.** §0의 「원인이 없으면 없다고 말한다」를 **「판정할 자료가 없으면 없다고 말한다」**로 확장해 **「요인을 판정할 자료가 부족합니다.」** 한 줄이 필요하다. 채택 0·일부만 `null`인 경우도 같은 문제다 | ③ |
| 70 | §8·§11 | **그룹이 `adopted:null` 이면 아는 요인 값까지 감춰진다** | 2021-08-13(ASOS 미산출)은 일사가 `null`인데 **기온은 `ok`이고 백분위까지 있다.** 그런데 `station.js:316`이 `isUnknown(g)`에서 카드를 통째로 조기 반환해 기온 33.0571을 그리지 않는다. §8:769 규약을 따른 것이라 코드가 틀린 것은 아니나, §14 #26의 「미채택 그룹도 값과 막대를 보여준다」 취지와 **같은 종류의 손실**이다. **`adopted`는 그룹 단위인데 `quality`는 요인 단위**라, 화면이 어느 쪽을 따를지가 §8·§11에 없다 | ③ |

**근거** — `⑧` `data/verify/2026-08-03_시간해상도.md` (§16 ⑧ 시간 해상도) · `⑦` `data/verify/2026-08-03_평년창경계.md` (§16 ⑦ 평년값 창 경계) · `①` `data/api/2026-08-03_API확인.md` (§12 일정 ① 실시간 API 확인) · `④` `data/api/2026-08-03_특보이력.md` + `data/api/고수온속보/` PDF 6건 (§12 일정 ④ 천수만 특보 이력) · `③` `data/verify/2026-08-03_스키마확정.md` (§12 일정 ③ 스키마 확정). 다섯 다 2026-08-03.
**①에서 올라왔다가 여기 없는 것 둘.** ① 기록 §5의 「§8 `rolling_24h` 48회 → 실시간 24회」는
**#1에 흡수**됐고(과거까지 24로 내리기로 했으므로), 「§16 ⑧ 질문이 바뀜」은
**⑧이 닫혀서 #14로 대체**됐다.

---

## 반영 이력

| 판 | 날짜 | 건수 | |
|---|---|---|---|
| **3.7** | 2026-07-31 | **28건** | 프로토타입 완성 + 원자료 재계산. §14 #19~#28 로 들어갔다. 그중 #19(창 규칙)·#20(채택률 분자)은 문서 안에서는 찾을 수 없었고 원자료로 내려가서야 나왔다 |

---

## 디자인적용지침과 어긋난 것

PRD 가 아니라 `디자인적용지침.md` 기준이다. **아직 지침에 반영하지 않았다.**

| 장 | 무엇 | 왜 |
|---|---|---|
| 1장 | 타이포 스케일 전 단계 +2px (`--wv-text-md` 16→18) | 중장년·야외 기준으로 기본 스케일이 작음. `waves-design-system.css` 는 수정하지 않고 `app.css` 에서 토큰만 덮어씀 |
| 2장 | 수온을 `--wv-text-5xl`(52px)로 | 지침은 "`.wv-display`(50px)가 아니라 `.wv-title` 정도가 적당"인데, 설명 화면에 들어왔을 때 가장 먼저 보여야 한다는 판단으로 더 키웠다 |
| 2장 | 모든 박스를 컨테이너 안(374px)에 두고 모서리를 `--wv-radius` 로 둥글림 | 전폭(414px)으로 두면 둥근 모서리가 화면 가장자리에 붙어 잘린 것처럼 보인다. 요인 막대 트랙 폭은 334px |
| 2장 | 육지 색을 `--wv-surface-raised` → `--wv-neutral-100` | 라이트 테마에서 `--wv-surface-raised` 가 순백이라 카드 배경과 같아 육지가 보이지 않음 |
| 2장 | 카드 배경을 반투명으로 (`color-mix` 로 `--wv-surface-solid` 75%) | `.wv-card--glass` 가 라이트 테마에서 불투명이라 유리로 보이지 않는다 |
