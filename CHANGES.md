# 반영 대기 (PRD 미반영)

화면이 PRD와 어긋나게 될 때마다 여기에 한 줄씩 쌓는다.
**PRD 파일(`03_PRD_3.6.md`)은 승인 전까지 고치지 않는다.**
이 목록이 다음 판(3.7)에 한꺼번에 반영할 후보다.

## 2026-07-31

| # | 절 | 무엇 | 왜 |
|---|---|---|---|
| 1 | §11 | 머리말을 두 줄로 — `{지점명}` / `{해역} 해역(확인 중)` (표기는 9번을 따른다) | 지도와 설명이 같은 지점을 다르게 불렀음. 2026-07-31 프로토타입에서 확인 |
| 2 | §11 | 연속일수 줄을 자료시각 → 상태배지로 | 배지 판정의 근거이므로. 2026-07-31 프로토타입에서 확인 |
| 3 | §11 | 배지 옆에 "국립수산과학원 기준" 한 줄 + 화면 아래(ⓘ 안내 뒤)에 근거 박스 "기준: 국립수산과학원 고수온 특보" | 어느 기관 기준인지 화면에서 알 수 없었음. 자세한 설명을 배지 옆에 두면 우리 계산과 공식 특보가 짝지어진 것처럼 읽혀 아래로 내림. 2026-07-31 프로토타입에서 확인 |
| 4 | §5·§16 | 지도 bbox **확정: 북 36.95 / 남 36.15 / 서 125.95 / 동 126.90** (충남 서해안 전체, 85 × 89km). 1주차 배경지도를 이 범위로 캡처한다 | §5 가 "네 모서리 위경도를 §16 에 못 박고 변환식을 고정한다"고 한 그 값. §16 미확정 항목에서 내려온다. 2026-07-31 프로토타입에서 확인 |
| 5 | §5 | 개념도에 해역명 라벨 `가로림만 해역` · `천수만 해역` 추가. 지도 라벨에는 `(확인 중)` 을 붙이지 않는다 | 지곡이 창리와 다른 만이라는 것이 지도에서 보여야 함. 확인 대상은 만의 위치가 아니라 "각 관측점이 어느 해역에 속하는가"이고, 붙이면 45px 폭 만에 라벨이 들어가지 않는다. 2026-07-31 프로토타입에서 확인 |
| 6 | §5·§11 | 준비 중 지점을 누르면 **모달**로 안내한다. §11 의 한 줄 응답 문구(`{지점명} ({해역}) — 이 지점은 설명을 준비 중입니다.`)를 대체한다. 지도의 점과 접힌 목록 양쪽에서 열린다. **수온을 비롯한 어떤 값도 넣지 않는다** | 한 줄 응답은 접힌 목록 구조에서 놓을 자리가 없었고, 어디에 떴는지도 알기 어려웠다. 검증하지 않은 지점이므로 값은 넣지 않는다. 2026-07-31 프로토타입에서 확인 |
| 7 | §11 | 지도 출처 표기를 "배경지도 없음(개념도)" 으로 | 외부 배경지도를 쓰지 않아 적을 출처가 없음. 2026-07-31 프로토타입에서 확인 |
| 8 | §10 | `{factor_as_of}` 를 factors 중 가장 이른 `as_of` 로 계산 | 명세는 "ASOS 공통"인데 파일에 ASOS 표시가 없어 식별 불가. 견본 A 에서는 결과가 같음(7/30). 2026-07-31 프로토타입에서 확인 |
| 9 | §5·§11 | 해역 표기를 `{해역}(추정)` → `{해역} 해역(확인 중)` 으로 | 화면만 보면 뭐가 추정인지 알 수 없고, 수온이 추정치라는 오해 소지가 있었음. 2026-07-31 프로토타입에서 확인 |
| 10 | §5 | 요인 막대: 임계선을 **점선(1.5px dashed, 간격 3px)** 으로 명시. 마커는 **10px** | 실선은 마커에 덮여 안 보였고, "기준선"이라는 의미도 점선이 더 맞다. 2026-07-31 프로토타입에서 확인 |
| 11 | §6 | **M1 범위 밖에 추가** — 지점 선택 없이 확장. `station.html?station={id}` 로 준비해 두었다. 지점이 늘면 창리 전용 버튼을 없애고 지도의 점과 목록 항목을 직접 링크로 바꾼다 | 지금은 설명하는 곳이 한 곳뿐이라 선택 UI 가 필요 없다. 구조만 열어 두었다. 2026-07-31 프로토타입에서 확인 |
| 12 | §6 | **M1 범위 밖에 추가** — 지도 확대·축소·지역 확장. 정적 이미지 1장으로는 불가능하다. 축척별 이미지 여러 장이나 타일 서비스가 필요한데, 타일은 API 키가 필요해 §5 의 오프라인 원칙과 충돌한다. `geo.js` 의 좌표 변환은 bbox 를 인자로 받게 해 두었다 | 확대·축소를 하려면 bbox 가 바뀌어야 하는데 변환식이 상수에 묶여 있었다. 함수는 그대로 두고 bbox 만 갈아끼울 수 있게 고쳤다. 2026-07-31 프로토타입에서 확인 |
| 13 | §5 | **정보 화면 특보 단계에 관행 색**(노랑 · 주황 · 빨강) 도입. 제도 설명이라 "지금 위험하다"를 주장하지 않는다. 위기경보는 **회색 농도** — 같은 계열이면 `경보`와 `경계`가 짝지어져 보이고, 4단계 중 기준을 아는 것은 `경계` 하나뿐이라 색으로 단정할 수 없다. 형태도 **원 / 사각**으로 나눠 흑백에서도 구분되게 했다. **설명 화면 배지는 회색 유지.** `--danger` 등 토큰은 여전히 금지 | 정보 화면은 제도를 설명하는 곳이고 설명 화면은 오늘을 말하는 곳이라, 같은 규칙을 쓰면 한쪽이 어색해진다. 색은 `app.css` 의 정보 화면 전용 변수(`--app-info-*`)에 가둬 두고 "상태 표시에 쓰지 않는다"를 주석으로 못 박았다. 2026-07-31 프로토타입에서 확인 |
| 14 | §11 | **한계 블록 7줄을 두 박스로 분리** — `이 서비스가 답하지 않는 것`(3줄) / `이 자료의 한계`(4줄). 문구와 줄 수는 그대로 | "서비스가 하지 않는 일"과 "자료의 한계"는 성격이 다르다. 한 박스에 담으면 어느 쪽을 읽고 있는지 흐려진다. 두 번째 박스의 제목 `이 자료의 한계` 만 §11 에 없는 새 문구다. 2026-07-31 프로토타입에서 확인 |

| 15 | §11 | **미채택 그룹도 요인 값과 막대를 표시한다.** 채택 그룹과 같은 형식(이름·상위 N%·막대·값·임계선 라벨) + 그 아래 `판정 기준에 못 미칩니다`. 구분은 그룹 이름·요인 이름을 한 단계 흐리게(`--wv-text-muted`)만 하고 막대·마커·임계선·숫자는 그대로. **채택 그룹이 없을 때 그룹을 나열하지 않던 규칙도 함께 뒤집는다** — `평년을 크게 벗어난 요인이 없습니다.` 를 먼저 짧게 두고 그 아래에 모든 그룹을 같은 형식으로 나열한다 | `판정 기준에 못 미칩니다` 한 줄만으로는 왜 못 미치는지 알 수 없다. 견본 A 의 `이전부터 높았던 수온` 은 백분위 61.0(상위 39%)인데 그 숫자가 화면에 없었다. 나열하지 않으면 우리가 아무것도 안 본 것처럼 읽혀 무책임해 보인다. 2026-07-31 프로토타입에서 확인 |

| 16 | §8 | **스키마에 `adopted: null` 규약 추가 필요** — `false`("봤는데 못 미쳤다")와 `null`("아직 못 봤다")은 다르다. 화면은 `null` 일 때 막대를 그리지 않고 `값을 아직 확인하지 못했습니다` 로 표시한다 | `null` 을 `false` 로 뭉치면 확인하지 않은 것을 확인한 것처럼 주장하게 된다. 감추면 견본 B 의 "셋 다 평범했다"가 둘만 보여 반쪽이 된다. 2026-07-31 프로토타입에서 확인 |
| 17 | §9 | **견본 표에 미확보 필드 목록을 명시할 것.** 지금은 `확보: 완료` 로만 적혀 있어 무엇이 빠졌는지 파일을 열어야 안다 | 견본 B·C 를 만들면서 §9 표에 없는 값이 여럿 나왔다(아래 목록). 3주차 원자료 재계산 전까지는 `null` 이다. 2026-07-31 프로토타입에서 확인 |

| 18 | §11 | **판정 보류 문구에 결측 항목과 관측 횟수를 명시.** `자료 결측으로 이번 시각은 판정을 보류했습니다` + `수온 자료의 {n}%가 비어 있습니다 ({전체}회 중 {n}회 관측)`. §11 원문은 `… 보류했습니다. (수온 결측률 {n}%)` 한 줄이었다 | `자료 결측`만으로는 무엇이 비었는지 알 수 없었다. 현재는 수온만 밝힐 수 있다 — 요인별 결측 정보가 근거 파일에 없다. **§8 에 요인별 결측 필드 추가를 검토할 것(3주차).** 만점(48회)은 파일에 없어 `obs_count ÷ (1 − 결측률)`로 되돌린다 — §16 의 30분→1시간 변환을 채택하면 만점이 24가 되는데 48을 적어 두면 그때 조용히 틀린다. 2026-07-31 프로토타입에서 확인 |
| 19 | §7·§8·§9·§11 | **그룹·요인 이름을 전부 새 이름으로 고칠 것.** PRD 원문은 아직 `배경장`·`30일 누적편차` 다. §8 파일 형태 예시, §9 견본 A 표, §13 대응표 포함 | 화면과 데이터는 이미 `이전부터 높았던 수온`·`지난 30일 누적` 으로 바꿨다. §5 가 "`배경장` 은 해양학 용어라 우선 검토 대상"이라고 적어 둔 그 항목이다. **§13 대응표는 이미 `누적된 고수온, 이전부터 높은 수온` 을 쓰고 있어 새 이름과 같은 계열이다.** 2026-07-31 프로토타입에서 확인 |
| 20 | §11 | **한계 블록에 채택 기준과 사전 고정 사실 추가** — `채택 기준: 평년 상위 {N}%` / `이 서비스가 미리 정한 값이며 결과에 따라 바꾸지 않습니다.` 숫자는 `adoption_threshold.percentile` 에서 읽는다. **`이 자료의 한계` 가 4줄 → 6줄이 되어 §11 의 "7줄이 최종"은 9줄이 된다** | 화면에 임계값의 근거가 전혀 없었다. 물리적 근거가 있는 값이 아니라 우리가 미리 정한 값이므로 그 사실 자체를 적는 것이 정직하다. `adoption_threshold.note` 가 "사전 고정. 결과를 보고 바꾸지 않음"이라고 파일에만 적혀 있던 것을 화면으로 올린 것이다. 2026-07-31 프로토타입에서 확인 |
| 21 | §8 | **요인에 평균/누적을 가르는 필드 추가 검토** — 지금은 `window`(`7일`·`30일`)만 있고 그 창을 평균했는지 누적했는지가 없다. `aggregation: "mean" \| "sum"` 같은 필드 | 화면이나 문장에서 `7일 평균` 처럼 쓰려면 이 구분이 필요한데, 파일만 보고는 알 수 없다. `지난 30일 누적` 에 `30일 평균` 이라고 적으면 거짓이 된다. §9 표는 `일사 7일 평균` · `30일 누적편차` 로 구분하고 있으나 근거 파일에는 그 정보가 없다. 2026-07-31 프로토타입에서 확인 |

| 22 | §7 | **채택 임계 90퍼센타일의 근거를 보강할 것.** 지금은 `사전 고정. 결과를 보고 바꾸지 않음` 만 적혀 있어 왜 하필 90인지 답이 없다. 실제로는 국제 표준값이다 — Hobday et al.(2016)의 해양 열파(marine heatwave) 정의가 **계절 변동 90퍼센타일을 5일 이상 초과하는 사건**으로 규정하고 NOAA · Copernicus 등이 채택했다. "정의상 모든 지점이 기후 구간의 약 10%에서 이 임계를 넘는다"는 성질이 **§13 의 이론 상한 1 − 0.9³ = 27.1% 와 같은 이야기**다 | 화면에 임계값의 근거가 전혀 없었다. 아래 「단서」 두 가지를 함께 적는다. 2026-07-31 프로토타입에서 확인 |
| 23 | §11 | 화면 문구 교체 — `이 서비스가 미리 정한 값이며 결과에 따라 바꾸지 않습니다` → **`해양 고수온 연구에서 널리 쓰는 기준이며, 결과에 따라 바꾸지 않습니다`** | 22번의 결과. 앞 문구는 "우리 마음대로 정했다"로 읽혀 근거가 있는데도 없어 보였다. 화면에는 이미 적용했다. 2026-07-31 프로토타입에서 확인 |

| 24 | §11 | **그룹 카드 하단에 판정 사유 한 줄 추가** — 채택 그룹은 `→ {요인}이 기준을 넘어 채택됐습니다`, 미채택 그룹은 기존 `판정 기준에 못 미칩니다`. 임계선 라벨 `기준 상위 N%` 는 그대로 둔다 | 임계선 라벨이 사용자에게 백분위 비교를 시켰다 — `상위 13%` 옆에 `기준 상위 10%` 가 놓이면 13 > 10 인데 `상위` 라서 큰 쪽이 낮다는 것까지 알아야 읽힌다. **요인마다 `기준에 못 미침` 을 붙이는 안은 접었다** — 채택 판정이 그룹 단위라 견본 A(일사 미달 · 기온 초과 → 그룹 채택)에서 왜 채택됐는지가 흐려진다. 그룹 단위 한 줄이 그 관계를 그대로 말한다. 조사(이/가 · 과/와)는 요인 이름의 받침으로 고른다 — 문구에 박아 두지 않는다. 2026-07-31 프로토타입에서 확인 |


| 25 | §8 | **`factors[]` 에 평년 분포 범위 필드 추가 검토 (3주차).** `clim_p10` / `clim_p90` 또는 `clim_min` / `clim_max`. 화면에는 `평년 이맘때 14.2 ~ 24.8 MJ/m²` 형태로 한 줄. 3주차 자료 처리 때 함께 계산한다 | 지금 막대는 **위치만** 보여 주고 평년이 어느 정도인지는 알 수 없다. `상위 13%` 가 몇 도인지, 몇 MJ/m² 인지가 화면에 없다. 백분위는 순위지 크기가 아니다. 2026-07-31 프로토타입에서 확인 |
| 26 | §11 | **요인 값 단위에 ⓘ 설명 추가.** 값 옆 ⓘ 를 누르면 모달로 단위를 설명한다 — `일사량 · MJ/m²` / `하루 동안 1제곱미터에 내리쬔 햇빛 에너지의 양입니다…`. 준비 중 지점 안내와 같은 형태. 전부 정적 텍스트이며 §10 검사기 대상이 아니다 | MJ/m² · ℃·일 은 일반 사용자가 판단할 수 없는 단위다. **설명 문구의 예시 숫자(`한여름 맑은 날은 20~25 정도입니다`, `약 12가 됩니다`)는 근거 파일 값이 아니므로 출처 확인이 필요하다.** 지금은 문장으로 예시임을 드러내고만 있다(`정도입니다` · `약`). 2026-07-31 프로토타입에서 확인 |

| 27 | §7·§9·§12·§13 | **요인 창 규칙과 견본 값이 어긋났다 — 원자료 재계산으로 발견, §7 규칙으로 확정하고 반영 완료.** §7 은 `D−7~D−1` 인데 §9 의 23.1429 / 34.3714 는 `D−6~D` 에서 나온 값이다. §7 규칙으로 계산하면 **23.2443 / 34.7143** 이다. `as_of` 는 `2021-07-30` 으로 §7 을 따르고 있어, **화면이 `요인: 7월 30일까지 자료` 라고 적으면서 실제로는 7월 31일 자료를 쓴다.** §0 이 설계 원리로 내건 정직한 자료 시각 표기가 깨져 있었다 | **§7 규칙(`D−7~D−1`)으로 확정했다** — `data/fsch6.json` · `data/sample-b.json` 을 고쳤고 계산은 `scripts/verify.py` 에 남겼다. 근거 — ① 실시간에서 오늘 일사 합계가 없으므로 `D−6~D` 는 운영 불가 ② 과거 재현과 실시간이 다른 창을 쓰면 채점이 서비스를 보증하지 못한다 ③ 화면 문구가 이미 `D−1` 로 적혀 있다. 2026-07-31 프로토타입에서 확인 |

| 28 | §13 | **채택률의 분자 정의가 문서에 없다.** 분모(판정 대상일)는 §13 에 있는데, 무엇을 "채택"으로 세는지가 없다. 재계산해 보니 **35 / 29 / 6 은 열유입 그룹만 센 값**이다. 배경장까지 넣으면 전체 20.1% · 대조군 18.2% 가 된다 (§7 규칙 기준) | 배경장은 개발용 구간에서 **17일 채택되는데 전부 대조군**이다(이상군 0일). 대조군 채택률이 10.8% → 18.2% 로 올라 실패 기준 해석이 달라질 수 있다. 어느 정의가 맞는지 §13 에 명시해야 한다. 2026-07-31 프로토타입에서 확인 |

### 27번 — 재계산 결과

**원자료 위치.** `~/김수민/원자료/` — ASOS 129 서산 일자료(`OBS_ASOS_DD_*.csv`, 2016~2025),
서해 수온 `total_{연도}.zip` → `ys_{연도}.zip` → 월별 CSV(CP949, 30분 간격).
**계산 스크립트는 남아 있지 않다.**

**재현 확인.** 현행 `D−6~D` 창으로 §9 의 네 값(견본 A 87.3 / 90.7, 견본 B 71.5 / 68.6)과
표본 수(158 / 172)가 **소수점까지 모두 재현됐다.** 한쪽만 틀린 것이 아니라
두 견본이 같은 규칙으로 계산돼 있다. 2021-07-31 일평균 28.4708 · 일최고 29.0000 도 일치.

**§7 규칙(`D−7~D−1`)으로 다시 계산하면:**

| | 현행 §9 (`D−6~D`) | **§7 규칙 (`D−7~D−1`)** |
|---|---|---|
| 견본 A 일사 | 23.1429 → 87.3 | **23.2443 → 88.6** |
| 견본 A 기온 | 34.3714 → 90.7 | **34.7143 → 92.4** |
| 견본 A 열유입 대표 | 90.7 | **92.4** → 채택 |
| 견본 B 일사 | 19.5686 → 71.5 | **19.1529 → 69.0** |
| 견본 B 기온 | 31.9000 → 68.6 | **31.8571 → 69.2** |
| 견본 B 열유입 대표 | 71.5 (일사) | **69.2 (기온)** → 미채택 |

표본 수 158 / 172 는 두 창 모두 유지된다.

**견본을 다시 고를 필요는 없다 — 세 논지가 다 살아 있다.**

1. 견본 A 는 여전히 채택된다 (90.7 → 92.4 로 여유가 오히려 커진다)
2. "일사는 못 미치는데 기온이 넘어서" 구조가 유지된다 (일사 88.6 미달 / 기온 92.4 초과)
3. 견본 B 는 여전히 미채택이다 (69.2, 임계에서 20 이상 떨어짐)

**다만 둘이 바뀐다.** 견본 A 의 일사가 임계에 1.4 밖에 안 남아 §9 의 "임계에 민감하다"가
더 아슬해진다. 견본 B 의 대표 요인이 일사 → 기온으로 바뀐다.

**§13 재계산 완료.** 옛 창(`D−6~D`)으로 문서 값이 **완전히 재현됐다** —
250 / 35 / 231 / 29 / 19 / 6, 14.0% / 12.6% / 31.6%. §7 규칙으로 바꾸면:

| | 옛 창 | **§7 규칙** |
|---|---|---|
| 판정 대상일 | 250 | **249** |
| ASOS 제외 구간 | 2021-08-12 ~ 08-18 | **2021-08-13 ~ 08-19** |
| 수온 보류 | 21 | **22** |
| 전체 | 35 / 14.0% | **33 / 13.3%** |
| 대조군 (231일) | 29 / 12.6% | **25 / 10.8%** |
| 이상군 | 19일 · 6 / 31.6% | **18일 · 8 / 44.4%** |

**실패 기준 1 은 통과하고, 오히려 강해진다** — 이상군 : 대조군이 2.5배 → **4.1배**.

**ASOS 제외 구간이 창 규칙에서 나온다.** §14 #12 가 `8/12~8/18` 에서 `8/13~8/19` 로
정정한 것이 **바로 이 창 차이**였다. 날짜 표기만 고치고 값은 옛 창으로 남아 있었다.
이번 수정으로 둘이 맞는다. §13 「이상군이 27일에서 19일로 줄어든 이유」의
"19·231·6·29 가 ±1 까지 움직일 수 있다"도 실제로 그렇게 움직였다 (19→18, 6→8).

**평년 분포 (§7 규칙, 25번 항목에 쓸 값)**

| 견본 | 요인 | p10 | p50 | p90 = 임계 | 당일 |
|---|---|---|---|---|---|
| A 7/31 | 일사 | 8.70 | 15.59 | **23.68** | 23.24 미달 |
| | 기온 | 27.31 | 29.63 | **34.21** | 34.71 초과 |
| B 8/5 | 일사 | 8.80 | 15.59 | **23.46** | 19.15 미달 |
| | 기온 | 27.31 | 30.03 | **34.21** | 31.86 미달 |

**§12 3주차 서술 정정.** "코드가 이 값을 재현하는지 확인하는 일"이 아니라
**"처음 짜서 문서 숫자와 맞는지 확인하는 일"** 이다. 스크립트가 남아 있지 않았다.
§14 #2 · #3(같은 원자료에서 다른 숫자가 나온 적 두 번)이 이 상황에서 나온 것으로 보인다.


### 24번 — 검토 경위

구현 전(2026-07-31, 판정 사유 줄을 넣기 직전)의 판단을 그대로 남긴다.
3.7 반영 때 "왜 이렇게 됐나"를 다시 세우지 않기 위해서다.

> §11 임계선 라벨 `기준 상위 N%` 는 사용자에게 백분위 비교를 시킨다.
> 13 > 10 인데 `상위` 라 큰 쪽이 낮다는 것까지 알아야 한다.
> 요인마다 `기준에 못 미침` 으로 바꾸는 안을 검토했으나,
> 채택 판정이 그룹 단위라 견본 A(일사 미달·기온 초과 → 그룹 채택)에서
> 왜 채택됐는지가 흐려진다.
> 대안: 그룹 카드 하단에 판정 사유 한 줄
> (`기온이 기준을 넘어 채택됐습니다`). 2주차 검토.

**결과 — 2주차를 기다리지 않고 같은 날 구현했다.** 위 표 24번이 그 결과다.
`기준에 못 미침` 안은 접힌 채로 두고, 임계선 라벨은 `기준 상위 N%` 를 유지했다.

### 22번 — 90퍼센타일을 쓰는 근거와 단서

**단서 둘. 인용할 때 함께 적는다.**

1. **Hobday 는 수온(SST)에 적용하고 우리는 요인(일사·기온·누적편차)에 적용한다.**
   `Hobday 를 따랐다` 가 아니라 **`같은 임계를 요인에 적용했다`** 로 쓴다.
   대상이 다르므로 그 논문의 검증이 우리 쪽으로 그대로 넘어오지 않는다.
2. **90 은 넘기 쉬운 느슨한 기준이라는 지적이 있다.** 그래서 Hobday 계열 연구도
   강도 범주(임계−평년 차이의 2·3·4배)를 따로 뒀다. **우리는 범주를 두지 않으므로
   그 한계를 §16 에 남긴다.**

**95 · 99 를 쓰지 않는 이유 — 표본이 얇다.**

| 임계 | 표본 172일에서 위쪽 개수 | 비고 |
|---|---|---|
| 90퍼센타일 | 17.2개 | 채택 |
| 95퍼센타일 | 8.6개 | 임계 추정이 불안정 |
| 99퍼센타일 | 1.7개 | 사실상 추정 불가 |

일사는 **158일**이라 더 얇고(§7), 3년치인 2020년은 **129일**이다(§7 `5년 215 / 4년 172 / 3년 129`).
§13 이 "이상군 19일은 얇다"고 인정한 것과 **같은 문제가 요인에도 생긴다.**

### 견본 B·C 미확보 값 — 3주차 원자료 재계산 대상

지어내지 않고 `null` 로 두었다. 각 파일 상단 `_unconfirmed` 필드에도 같은 목록이 있다.

| 견본 | 미확보 필드 |
|---|---|
| **B** (2021-08-05) | `이전부터 높았던 수온` 의 값·백분위 · 관측 횟수 · 결측률 · 조석 조건 · 요인 `as_of` · 평년 표본 수(일사·기온) |
| **C** (2021-08-11) | `climatology.years` · `climatology.sample_size` · `withheld_reason` |

화면 처리 — B 의 `이전부터 높았던 수온` 은 `값을 아직 확인하지 못했습니다`,
C 의 한계 블록은 `평년값: 최근 N년 · 표본 N일` 줄만 감추고 나머지 세 줄은 그대로 노출한다.

`as_of` 는 §7 규칙(ASOS 7일 창 = D−7~D−1)으로 2021-08-04 가 나오지만 §9 가 명시하지 않아 `null` 로 두었다.
`clim_sample_size` 는 날마다 다시 세는 값이라 견본 A 의 158/172 를 옮겨 쓸 수 없다.

**그룹·요인 이름 통일 완료.** `data/` 세 파일이 모두 새 이름을 쓴다.
`id` 는 그대로다 (`background` · `cum_anomaly`).

| | 옛 이름 | 새 이름 |
|---|---|---|
| `groups[].name` | 배경장 | **이전부터 높았던 수온** |
| `factors[].name` | 30일 누적편차 | **지난 30일 누적** |

### 배경 — 4번 bbox 가 세 번 바뀐 경위

| | 1안 | 2안 | **확정** |
|---|---|---|---|
| 범위 | 북 36.94 / 남 36.33 / 서 126.13 / 동 126.61 | 북 36.66 / 남 36.35 / 서 126.15 / 동 126.60 | **북 36.95 / 남 36.15 / 서 125.95 / 동 126.90** |
| 실거리 | 42.9 × 67.8 km | 40.3 × 34.5 km | **85.0 × 88.9 km** |
| 가로:세로 | 1 : 1.58 | 1 : 0.86 | **1 : 1.05** (거의 정사각형) |
| 개념도 높이 | 552 px | 299 px | **366 px** |
| 지곡 | 안 | 밖 | **안** |
| 접은 이유 | 지곡 하나 때문에 세로가 두 배 | 천수만에 너무 붙어 어디인지 모름 | — |

2안에서 쓰던 "지도 밖 지곡 화살표 줄"은 철회했다. 지곡이 다시 지도 안으로 들어왔고,
같은 사실(다른 만이다)을 해역명 라벨이 대신 말한다.
`geo.js` 의 `distanceKm()` · `placeOffMapNotes()` 는 남겨 두었다 — 지금은 쓰는 곳이 없다.

### 1주차 배경지도 캡처 지침

확정 bbox 가 곧 캡처 범위다. 지침은 `assets/geo.js` 머리말에도 적어 두었다.

| | |
|---|---|
| 범위 | 북 **36.95** / 남 **36.15** / 서 **125.95** / 동 **126.90** |
| 비율 | 가로 : 세로 = **0.956 : 1** (85.0 × 88.9 km). 이 비율로 자른다 |
| 여백 | 없이 자른다. 이미지 가장자리가 곧 bbox 경계다 |
| 출처 | 브이월드 · OpenStreetMap · 국립해양조사원 / 국토지리정보원. 카카오·네이버·구글 캡처는 재배포 금지 (PRD §5). 화면에 출처 표기 (§11) |

비율이 어긋나면 `toPixel()` 이 어긋나 관측점이 육지에 찍힌다.
교체할 때 고칠 곳은 `index.html` 의 `<path class="app-land">` 두 개뿐이고,
점의 좌표는 다시 찍지 않는다.

---

## 결정을 기다리는 것

PRD 와 어긋나지는 않지만 확인이 필요한 것들. 확정되면 위 표로 옮기거나 지운다.

| 절 | 내용 |
|---|---|
| §16 | 국립수산과학원 고수온 특보 페이지의 정확한 주소. 지금은 기관 대표 주소(`www.nifs.go.kr`)를 링크하고 있다 |
| §16 | 해역 구분 확정. 확정되면 여섯 곳의 `(확인 중)` 을 한꺼번에 뗀다 (표기는 9번, 고칠 곳은 `geo.js` 의 `STATIONS` 한 군데) |
| §11 | 한계 블록 `관측점 한 곳의 자료이며 해역 전체를 대표하지 않습니다` 가 414px 폭에서 두 줄로 감긴다(402px / 374px). "7줄이 최종"인데 화면에서는 여덟 행이 된다. **블록을 만들어 실제로 확인함**. 그대로 두거나 / 그 블록만 14px 로 내리거나 / §11 문구를 줄이거나 |
| §16 | **Hobday et al.(2016) 원문 확인 후 §7 에 인용 병기.** 서지사항(저널·권·쪽)까지 적는다 |
| §16 | **강도 범주를 두지 않는 한계를 명시할지 검토.** 우리는 채택/미채택 두 갈래뿐이라 "얼마나 벗어났는가"를 말하지 않는다 |
| §5 | 지도에 창리의 수온을 표시할지. §11 「지도」 문구 목록에 수온 표기 형식이 없어 지금은 표시하지 않는다 |

---

## 디자인적용지침과 어긋난 것

PRD 가 아니라 `디자인적용지침.md` 기준이다.

| 장 | 무엇 | 왜 |
|---|---|---|
| 1장 | 타이포 스케일 전 단계 +2px (`--wv-text-md` 16→18) | 중장년·야외 기준으로 기본 스케일이 작음. `waves-design-system.css` 는 수정하지 않고 `app.css` 에서 토큰만 덮어씀 |
| 2장 | 수온을 `--wv-text-5xl`(52px)로 | 지침은 "`.wv-display`(50px)가 아니라 `.wv-title` 정도가 적당"인데, 설명 화면에 들어왔을 때 가장 먼저 보여야 한다는 판단으로 더 키웠다 |
| 2장 | 모든 박스를 컨테이너 안(374px)에 두고 모서리를 `--wv-radius` 로 둥글림 | 한때 여백 밖으로 빼내 전폭(414px)으로 썼으나, 모서리를 둥글리면 곡선이 화면 가장자리에 붙어 잘린 것처럼 보인다. 요인 카드·근거 박스·한계 박스·기준 박스가 모두 같은 폭·같은 모서리다. 요인 막대 트랙 폭은 374 → **334px** 로 되돌아갔다 |
| 2장 | 육지 색을 `--wv-surface-raised` → `--wv-neutral-100` | 라이트 테마에서 `--wv-surface-raised` 가 순백이라 카드 배경과 같아 육지가 보이지 않음 |
