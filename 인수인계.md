# 프로젝트 인수인계 — 새 대화창용

> 이 문서를 새 채팅에 통째로 붙여넣으면 바로 이어서 작업할 수 있습니다.
> 최종 갱신: 2026-07-31

---

## 1. 프로젝트 개요 및 목표

**충남 서해 연안 고수온 설명 서비스** — 정적 웹사이트 하나를 만드는 중입니다.

| | |
|---|---|
| 대상 사용자 | 충남 천수만 양식 어가, 어촌계 관계자. **중장년 이상** |
| 사용 환경 | **휴대폰 세로 화면**, 배 위·양식장 등 **야외 직사광선** |
| 진입 시점 | 기상청·국립수산과학원 고수온 특보를 **받은 직후** |
| 관측 지점 | **서산 창리 (fsch6)** 한 곳. 나머지 5곳은 "설명 준비 중" |
| 최종 마감 | **2026-08-27 (목)** |

### 이 서비스의 설계 원리 (가장 중요)

> **이 서비스는 경보를 울리지 않는다.**
> 어민은 이미 특보를 받았다. 그 다음에 생기는 질문 **"왜 이런 거지?"** 에만 답한다.
> 그리고 **원인이 없을 때는 없다고 말한다.**

따라서 아래는 기능 축소가 아니라 **설계상 하지 않는 것**입니다.

- 예측하지 않는다 (며칠 더 갈지)
- 안전을 판정하지 않는다
- 원인을 단정하지 않는다 ("~때문에", "가장 큰 요인" 금지)
- 알림·경보를 보내지 않는다
- 색으로 상태의 높고 낮음을 표현하지 않는다

### 화면 세 개

```
        ┌──────────────────┐
        │   ③ 정보          │  특보·위기경보 기준 설명
        └──┬───────────┬───┘
           │ ▲         │ ▲   ⓘ 특보란? / ← 뒤로
           ▼ │         ▼ │
  ① 지도 ──클릭──▶ ② 설명
        ◀──뒤로───
```

---

## 2. 기술 스택 및 환경

| | |
|---|---|
| 스택 | **빌드 도구 없는 HTML + CSS + 바닐라 JS** |
| 배포 | GitHub Pages (예정) |
| 개발 도구 | **Claude Code** (터미널) |
| OS | macOS. **사용자는 맥·터미널 초보** — 명령어는 복사해 붙일 수 있게 한 줄씩 |
| 작업 폴더 | `~/projects/sst-explainer` |
| 디자인 시스템 | **Waves Design System v1.2.1** (CSS 변수 기반, 971줄) |
| 폰트 | Google Fonts (Noto Serif KR / IBM Plex Sans KR / IBM Plex Mono) |

**사용자는 코드를 직접 작성하지 않습니다.** Claude Code에 지시해서 만듭니다.

---

## 3. 현재 파일 구조 및 데이터

```
~/projects/sst-explainer/
├── 03_PRD_3.6.md          95KB, 1713줄 — 요구사항 원본. 최우선 근거
├── waves-design-system.css 36KB, 971줄 — 수정 금지
├── 디자인적용지침.md        19KB — PRD × 디자인시스템 충돌 해소 + 실행 프롬프트
├── 작업계획.md             — 오늘 할 일 + 주차별 일정
├── assets/                 (비어 있음)
└── data/                   (비어 있음)  ← fsch6.json 을 여기 만들어야 함
```

### PRD 3.6 절 번호 (참조용)

| 절 | 내용 |
|---|---|
| §0 | 서비스의 위치 (설계 원리) |
| §2 | 두 개의 28℃ — 일평균 vs 일 최고 |
| §5 | **화면 구조** — 세 화면 목업, 요인 막대 사양 |
| §7 | 요인 구조 — 그룹·백분위·채택 판정 |
| §8 | **근거 파일** — `evidence.json` 스키마, JSON 예시, 보류일 규약 |
| §10 | AI 문장과 검사기 — **자리표시자 방식** |
| §11 | **화면 문구 전체** — 화면에 나가는 모든 텍스트 |
| §13 | 검산과 채점 |
| §14 | 정정 이력 (18건) |
| §16 | 미확정 항목 |

### 만들어야 할 데이터 — `data/fsch6.json`

**PRD §8 「파일 형태 (완성본)」 블록을 그대로 복사**하면 됩니다. 숫자를 새로 만들지 마세요.
견본 A = **2021-07-31 창리 실측**입니다.

```json
{
  "schema_version": "1.0",
  "station": { "id":"fsch6","name":"서산 창리","area":"충남 천수만",
               "lat":36.6163,"lon":126.3717 },
  "status": {
    "observed_at":"2021-07-31", "sst_basis":"daily_mean",
    "current_sst":28.47,      "current_sst_raw":28.4708,
    "daily_max_sst":29.00,    "daily_max_sst_raw":29.0000,
    "climatology_mean":26.25, "climatology_mean_raw":26.2451,
    "anomaly":2.23,           "anomaly_raw":2.2257,
    "stage":"advisory", "stage_label":"주의보 기준 도달",
    "days_over_advisory":2,
    "stage_caveat":"공식 특보는 해역 단위이며 전망을 포함합니다. 이 값은 관측점 재계산치입니다."
  },
  "data_quality": { "sst_missing_rate":0.0, "obs_count":48,
                    "judgment_withheld":false, "withheld_reason":null },
  "groups": [
    { "id":"heat_input","name":"열유입","display_order":1,
      "adopted":true,"representative_percentile":90.7,
      "factors":[
        {"id":"solar","name":"일사량","window":"7일","as_of":"2021-07-30",
         "value":23.14,"value_raw":23.1429,"unit":"MJ/m²",
         "percentile":87.3,"clim_sample_size":158,"quality":"ok"},
        {"id":"airtemp","name":"기온","window":"7일","as_of":"2021-07-30",
         "value":34.37,"value_raw":34.3714,"unit":"℃",
         "percentile":90.7,"clim_sample_size":172,"quality":"ok"}]},
    { "id":"background","name":"배경장","display_order":2,
      "adopted":false,"representative_percentile":61.0,
      "factors":[
        {"id":"cum_anomaly","name":"30일 누적편차","window":"30일","as_of":"2021-07-31",
         "value":12.40,"value_raw":12.3971,"unit":"℃·일",
         "percentile":61.0,"clim_sample_size":172,"quality":"ok"}]}
  ],
  "conditions":[{"id":"tidal_phase","label":"neap",
                 "display_text":"소조기 — 바닷물 섞임이 약해지는 시기입니다"}],
  "adoption_threshold":{"percentile":90,"unit":"group_representative_max",
                        "decided_at":"2026-07-30","note":"사전 고정. 결과를 보고 바꾸지 않음"},
  "climatology":{"method":"rolling_5y_min3","years":4,"sample_size":172,
                 "window_days":21,"is_climate_normal":false,
                 "note":"기후평년값(30년) 기준에 미달합니다"},
  "explanation":{
    "plain_template": null, "plain": null,
    "detailed_template": null, "detailed": null,
    "verification":{"passed":false,"retries":0,"blocked":true}},
  "disclaimer":"이 서비스는 국립수산과학원의 공식 특보를 대신하지 않습니다."
}
```

**`explanation` 은 위처럼 비웁니다.** AI 문장은 4주차(8/24~)에 만들고,
지금은 화면이 §11의 차단 문구를 띄우는 게 정상 동작입니다.

> 이번 시각의 설명 문장을 만들지 못했습니다. 위의 수치는 그대로 유효합니다.

### 검산 (JSON 만든 뒤 반드시)

| 확인 | 기대값 |
|---|---|
| `current_sst_raw − climatology_mean_raw` | **2.2257** |
| 열유입 `representative_percentile` = 요인 백분위의 **최대값** | **90.7** = max(87.3, 90.7) |
| 90 이상 → `adopted` | **true** |
| 배경장 61.0 → `adopted` | **false** |

---

## 4. 절대 규칙

### 데이터·표시

1. **화면 숫자는 원값(`_raw`)에서 계산한다.** 표기값(`current_sst`)을 다시 반올림하지 않는다.
   - 수온·평년값·요인값: 소수 **첫째 자리 사사오입** (28.4708 → `28.5℃`)
   - 상위%: `round(100 − percentile)` (87.3 → `상위 13%`)
   - 평년 대비: **절댓값**을 첫째 자리 반올림해 **0.5 이상**이면
     `평년보다 N℃ 높습니다/낮습니다`, **0.5 미만**이면 `평년과 비슷합니다`
   - 이 변환 함수들을 **`assets/format.js` 하나로** 만들고 세 화면이 공유한다.
     나중에 AI 문장 렌더러가 그대로 재사용한다 (PRD §10)
2. `groups[].display_order` **순서대로** 그린다. 값 크기로 정렬하지 않는다.
3. `adopted:false` 인 그룹은 **`판정 기준에 못 미칩니다` 만** 쓴다. 값도 막대도 그리지 않는다.
4. **채택 그룹이 하나도 없으면** 그룹을 아예 나열하지 않고
   `평년을 크게 벗어난 요인이 없습니다.` **한 줄만** 쓴다.
5. `stage` 가 `advisory` 또는 `warning` 일 때만 연속일수 줄을 보여준다.
6. `stage` 가 `withheld` 면 요인 영역 전체를 **판정 보류 문구로 대체**한다.
7. `ⓘ 원인 기여도가 아닙니다` 안내는 **막대가 그려진 화면에서만** 띄운다.
8. **화면 문구는 전부 PRD §11에서 가져온다.** 임의로 쓰지 않는다.

### 디자인

9. **라이트 테마 고정.** `<html data-theme="light">`
   Waves는 다크가 기본이지만 **야외 직사광선에서 안 보인다.**
10. **상태 배지는 다섯 단계 모두 `.wv-badge--neutral` 하나.**
    `--danger` · `--warning` · `--success` 를 **어디에도 쓰지 않는다.**
    빨간 배지는 사실상 경보이고, PRD가 금지했다.
11. **`.wv-progress` 를 요인 막대로 쓰지 않는다.** 의미가 반대다
    (채워진 양 vs 분포에서의 위치). 진행 바로 그리면
    `원인 기여도가 아닙니다` 를 **그림이 정면으로 반박**한다.
12. 물결(`.wv-waves`)은 **지도·정보 화면에만, 애니메이션 없이.** 설명 화면엔 쓰지 않는다.
13. **`waves-design-system.css` 를 수정하지 않는다.** 추가 스타일은 `assets/app.css` 에 쓰고,
    거기서도 `--wv-*` 시맨틱 토큰만 참조한다. 색상값을 직접 쓰지 않는다.
14. 휴대폰 세로. `.wv-container` (max-width 414px)

### 안 만드는 것

15. **외부 CDN은 폰트 하나뿐.** 다른 스크립트를 불러오지 않는다.
    폰트가 로드되지 않아도 화면 골격은 떠야 한다.
16. **설명하지 않는 지점에는 수온을 표시하지 않는다.** 시간이 남아도 하지 않는다.
17. 지도 해역명은 **여섯 곳 모두 `(추정)`** 을 붙인다. 창리도 예외가 아니다.
18. 지도 배경은 **개념도**를 쓴다. 저작권 확인 안 된 지도 이미지를 넣지 않는다.

---

## 5. 현재 상태와 다음에 할 일

### ✅ 완료

- PRD 3.6 확정 (1713줄, 정정 이력 18건)
- 디자인 시스템 확보 + PRD와의 충돌 5건 해소 (`디자인적용지침.md`)
- 폴더 생성, Claude Code 설치·인증 완료
- 문서 3종이 작업 폴더에 배치됨

### ⬜ 아직 아무 코드도 없음

`assets/` 와 `data/` 가 비어 있습니다. HTML 파일도 없습니다.

### 다음 작업 — 순서대로

**① `data/fsch6.json` 만들기 + 검산** (위 3장 참조)

**② 화면 세 개 만들기**
`디자인적용지침.md` **4장 「Claude Code 프롬프트 (교체본)」** 의 코드 블록을
그대로 실행하면 됩니다. 규칙이 전부 들어 있습니다.

| 파일 | 내용 |
|---|---|
| `index.html` | 지도. 지점 6곳, 창리만 설명 제공 |
| `station.html` | 설명. `data/fsch6.json` 을 fetch. `?sample=b`·`?sample=c` 지원 |
| `info.html` | 정보. 정적 텍스트 |
| `assets/format.js` | 숫자 변환 함수 (위 규칙 1) |
| `assets/app.css` | 추가 스타일만 |

**③ 오늘 끝나기 전 확인**

- [ ] **일사(87.3) 마커는 임계선 왼쪽, 기온(90.7) 마커는 오른쪽** ← 오늘의 핵심
- [ ] 배지가 회색. `--danger`·`--warning`·`--success` 가 결과물에 없음
- [ ] `?sample=b` 에서 `평년을 크게 벗어난 요인이 없습니다` 가 뜸
- [ ] 세 화면이 서로 오감
- [ ] 폰트 링크를 지워도 화면이 무너지지 않음
- [ ] 설명 문장 자리에 차단 문구가 보임
- [ ] 정보 화면에 개발 메모(`1주차에 확인` 등)가 없음

---

## 6. 알려진 이슈 (새 세션에서 알고 있어야 할 것)

### ⚠ 요인 막대 — 이 프로젝트에서 가장 까다로운 지점

견본 A는 **일사 87.3(미달) / 기온 90.7(초과) / 임계 90** 입니다.
"일사는 못 미치는데 기온이 넘어서 그룹으로는 채택된다"가 이 견본을 고른 이유이고,
**시연의 핵심 장면**입니다. 그런데 두 번 실패했습니다.

| 사고 | 내용 |
|---|---|
| PRD §14 #16 | 목업이 두 마커를 **모두 임계선 오른쪽**에 그려 논지와 그림이 반대였음 |
| PRD §5 (프로토타입에서 발견) | 임계선을 그려도 **90.7과 90은 374px에서 2.6px 차이** → 12px 마커가 선을 덮음 |

**대응:** 임계선을 마커보다 **위(`z-index: 2`)** 에 그리고,
트랙 **위아래로 4px씩 삐져나오게** 합니다.

**임계선 위치는 `adoption_threshold.percentile` 에서 읽습니다. 90을 하드코딩하지 않습니다.**
라벨은 `기준 상위 {N}%` — `90`이라고 쓰면 옆의 `상위 13%`와
**커지는 방향이 반대**라 비교가 안 됩니다.

| 요인 | 화면 | 읽히는 것 |
|---|---|---|
| 일사량 | `상위 13%` vs `기준 상위 10%` | 13 > 10 → 못 미친다 |
| 기온 | `상위 9%` vs `기준 상위 10%` | 9 < 10 → 넘었다 |

### ⚠ PRD §5 목업의 ASCII 그림은 아직 틀립니다

```
기온: ├───────────────────●┊────┤   ← 마커가 임계선 왼쪽 (틀림)
```

25칸 ASCII로는 87.3(21.8칸)·90(22.5칸)·90.7(22.7칸)을 구분할 수 없습니다.
**그림이 아니라 백분위 숫자를 기준으로 구현하세요.** 문서는 나중에 고칩니다.

### ⚠ 견본 B·C 는 아직 JSON이 없습니다

| 견본 | 날짜 | 상태 | 막히는 곳 |
|---|---|---|---|
| B | 2021-08-05 | `warning`, 7일 연속, **요인 채택 없음** | **PRD에 배경장 백분위가 없음.** 원자료 재계산 필요 |
| C | 2021-08-11 | `withheld`, 결측률 39.6%, 관측 29회 | 규약은 §8에 있음 |

**견본 C 필드 규약 (§8):** `groups: []`, `conditions: []`,
`current_sst`·`climatology_mean`·`anomaly` 는 **`null`**,
`days_over_advisory: 0`, `daily_max_sst`(28.60)와 최상위 `climatology` 블록은 **담는다**.

**화면이 `null` 을 만나도 깨지지 않아야 합니다.**

### 나중 (오늘 아님)

- 견본 D (실시간 `rolling_24h` 형태) — 8/3 API 확인 성공에 의존
- 견본 E (`anomaly` 음수) — 1주차에 개발용 구간에서 선정
- AI 문장 (§10 자리표시자 방식) — 4주차
- 실시간 갱신 파이프라인 (GitHub Actions, 1시간 주기) — 2주차

---

## 7. 사용자에게 답할 때

- **맥·터미널 초보입니다.** 명령어는 **복사해서 붙일 수 있게 한 줄씩**, 무엇을 하는 명령인지 함께 설명해 주세요.
- Claude Code가 승인을 물으면 어떤 성격의 명령인지(읽기/쓰기/삭제) 알려주면 도움이 됩니다.
- 한 번에 여러 갈래를 주면 길을 잃습니다. **다음 한 걸음**만 주세요.
- 막혔을 때는 "오늘 딱 하나만 한다면 무엇인가"로 좁혀 주세요.
  → 지금 그 답은 **`data/fsch6.json` 만들고 견본 A 화면 하나 띄우기** 입니다.
