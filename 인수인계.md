# 프로젝트 인수인계 — 새 대화창용

> 이 문서를 새 채팅에 통째로 붙여넣으면 바로 이어서 작업할 수 있습니다.
> **최종 갱신: 2026-08-03 (1주차 첫날 마감)**

---

## 0. 30초 요약

충남 서해 **고수온 설명 서비스**. 어민이 특보를 받은 뒤 **"왜 이런 거지?"** 에만 답하는 정적 웹사이트.
관측점은 **서산 창리(fsch6)** 한 곳. **마감 2026-08-27(목).**

**7/31 프로토타입 완성 → 8/3 하루에 1주차 아홉 항목 중 다섯이 닫혔고 `CHANGES.md`가 0건 → 70건이 됐다.**

**PRD(`03_PRD_3.7.md`)는 8/3에 한 줄도 고치지 않았다.** 모든 변경은 `CHANGES.md`에 쌓았다.

---

## 1. 설계 원리 — 가장 중요하다

> **이 서비스는 경보를 울리지 않는다.**
> 어민은 이미 특보를 받았다. 그 다음에 생기는 질문 **"왜 이런 거지?"** 에만 답한다.
> **그리고 원인이 없을 때는 없다고 말한다.**

**설계상 하지 않는 것 (판이 바뀌어도).**

| 항목 | 사유 |
|---|---|
| 알림·경보 | §0과 충돌 |
| 예측 | 며칠 더 갈지 말하지 않는다 |
| 안전 판정 | 안전/위험을 말하지 않는다 |
| 원인 단정 | 「기여도」가 아니라 「평년을 벗어난 정도」다 |
| 색으로 상태 표현 | 설명 화면 배지는 다섯 단계 모두 회색. 색은 정보 화면 전용 |

**두 번째 주장이 §13 채점 지표(침묵 일치율)로 측정된다.** 이것이 이 프로젝트의 중심이다.

---

## 2. 오늘(8/3) 확정된 것 — 새 세션이 반드시 알아야 할 여덟

이 여덟이 어제까지의 문서와 다르다. **PRD 본문은 아직 옛 값이므로 `CHANGES.md`가 우선한다.**

| # | 무엇 | 값 |
|---|---|---|
| 1 | **시간 해상도** | 과거·실시간 모두 **1시간(H1 정시), 만점 24**. `daily_max_sst`만 30분 유지 |
| 2 | **평년값 창 경계** | **A1 — 분석 기간 밖 자료를 쓴다.** 규칙만으로 결정됐다(A2는 표본 44일을 만들어 §7의 3년 하한 129일과 충돌) |
| 3 | **「3년 미만」 판정** | **C2 — 창 43일 중 절반 이상 있으면 그 해를 1년으로 센다** (일 단위) |
| 4 | **배경장 30일 창** | 보간 금지를 그대로 적용. 창에 결측이 하나라도 있으면 값 없음 |
| 5 | **결측률 20% 초과일** | **어떤 계산에도 쓰지 않는다** — 배경장 창에서도, 평년 표본에서도 뺀다 |
| 6 | **`withheld`의 뜻** | **수온을 못 믿는 경우만.** 평년 부족·ASOS 미산출은 `stage` 정상값 + 해당 값만 `null` |
| 7 | **`schema_version`** | **2.0** (`obs_count` 만점이 48→24로 바뀌어 major) |
| 8 | **실시간 API** | 창리를 준다. 실시간을 범위 안에 유지한다 |

### 실시간 API — 문서와 다른 것 셋

```
발급처  국립수산과학원 nifs.go.kr  (공공데이터포털 아님)
주소    https://www.nifs.go.kr/api/OpenAPI_json
        문서의 /OpenAPI_json 은 302 리다이렉트 — curl 에 -L 없으면 0바이트
인코딩  EUC-KR  (JSON도)
키      .env 의 NIFS_KEY
```

- `id=risaCode&gru_nam=W` — 서해 관측소 목록
- `id=risaList` — 전국 최신 스냅숏. **변수는 `key` 하나뿐이고 시계열이 없다**
- 저층 수온은 오지 않는다. **중층은 온다**(`obs_lay=2`)

---

## 3. 파일 구조

```
03_PRD_3.7.md          요구사항 원본. 최우선 근거. 8/3에 안 고쳤다
CHANGES.md             PRD 미반영 70건. ← 실제로 확정된 것은 여기 있다
디자인적용지침.md / 인수인계.md
waves-design-system.css   수정 금지
index.html · station.html · info.html
assets/  app.css · geo.js · map.js · station.js · format.js · info.js
data/    fsch6.json · sample-b.json · sample-c.json
data/api/     ① 실시간 API 확인 · ④ 특보 이력 · ⑤⑥ 정보탭·지도 출처 · 고수온속보 PDF
data/verify/  ⑧ 시간해상도 · ⑦ 평년창경계 · 견본재생성 · ③ 스키마확정
scripts/verify.py      계산·검증 스크립트. 하위 명령 아홉
.env                   NIFS_KEY. .gitignore 에 등록됨
```

### PRD 3.7 절 번호

`§0` 서비스의 위치 · `§1` 용어 · `§2` 두 개의 28℃ · `§3` 자료 구간 · `§4` 사용자 ·
`§5` 화면 구조 · `§6` 범위 · `§7` 요인 구조 · `§8` 근거 파일 · `§9` 화면 견본 ·
`§10` AI 문장과 검사기 · `§11` 화면 문구 · `§12` 일정 · `§13` 검산과 채점 ·
`§14` 정정 이력 · `§15` 해상 풍속 · `§16` 미확정 항목

---

## 4. `scripts/verify.py` — 계산이 여기 하나로 고정돼 있다

**3.7 이전에는 문서에 숫자만 있고 그 값을 낸 코드가 없었다**(§12:1496).
그래서 같은 원자료에서 다른 숫자가 나온 적이 두 번 있었다(§14 #2·#3).
**이제 코드가 문서를 재현한다. 숫자를 손으로 고치지 말 것.**

```
python3 scripts/verify.py samples   §9 견본 원값
python3 scripts/verify.py rates     §13 채택률
python3 scripts/verify.py clim      평년 분포
python3 scripts/verify.py hourly    ⑧ 30분 vs H1 vs H2
python3 scripts/verify.py edge      ⑦ 창 경계 A1 vs A2 (비용 기록용)
python3 scripts/verify.py bg        배경장 산출률
python3 scripts/verify.py bc        ⑦ B·C
python3 scripts/verify.py evidence  근거 파일 재현 + check_schema 불변식 검사
python3 scripts/verify.py build     견본 JSON 3종 재생성 (파일을 고친다)
```

**`build` 가 견본을 만들고 `evidence` 가 그것을 검증한다.** 견본 JSON 을 손으로 고치지 말 것 —
`evidence` 가 그 자리에서 불일치를 잡는다.

원자료는 `~/김수민/원자료` (ASOS CSV · 창리 `total_*.zip`).

### 구현 주의 둘 — 실물로 걸린 적이 있다

1. **파이썬 `round()` 금지.** 은행가 반올림 + 부동소수점 오차로 사사오입과 어긋난다.
   경계값 전수 검사에서 `round()` **2005건 오답** / JS `Math.round(x*10)/10` 0건.
   **1/10 단위 정수 산술로 계산할 것.**
2. **float으로 평균을 낸 뒤 반올림하지 말 것.** 2017-06-17 일평균은 정확히 22.25℃인데
   48개를 float으로 더하면 22.2499…가 되어 22.2로 내려간다.

---

## 5. 절대 규칙

### 자료·표시
- **검증하지 않은 숫자를 화면에 띄우지 않는다.** 설명하지 않는 지점에는 수온을 표시하지 않는다
- **보간 금지.** 창 안에 결측이 있으면 값을 만들지 않는다 (7일 창·30일 창 모두)
- **`adopted`는 세 가지다.** `true` / `false`(봤는데 못 미쳤다) / `null`(못 봤다). **섞으면 안 된다**
- **요약 문구는 「봤다」가 전제다.** `adopted:null`이 하나라도 있으면 "없습니다"라고 말하지 않는다
- 화면 표시는 **소수 1자리**(원값 사사오입). 근거 파일은 2자리 + `_raw` 원값
- 해역명은 **`{해역} 해역(확인 중)`** — 여섯 곳 전부. 확인되면 한꺼번에 뗀다

### 문서
- **PRD는 승인 전까지 고치지 않는다.** 어긋나는 것은 `CHANGES.md`에 쌓는다
- **결과를 보고 기준을 바꾸지 않는다.** 판단 기준은 숫자를 보기 전에 고정하고 기록에 적는다
- **확인 못 한 것은 채우지 않는다.** "확인 못 함"으로 남긴다
- **불편한 결과도 그대로 적는다**

### AI 문장 (§10)
- **AI는 숫자를 쓰지 않는다.** 자리표시자만 쓰고 렌더러가 값을 넣는다
- 검사는 여섯 줄뿐이다. 규칙을 늘려 고치려 한 적이 있고 실패했다(§14 #13)

---

## 6. 현재 상태 — 1주차 아홉 항목

| | 항목 | 상태 |
|---|---|---|
| ① | 실시간 API 확인 | **닫힘** |
| ② | 환경·GitHub | **닫힘** — <https://ssum23.github.io/sst-explainer/> |
| ③ | 스키마 확정 | **닫힘** (결정 일곱 + `check_schema` 불변식 검사) |
| ④ | 천수만 특보 이력 | **닫힘** — 9년 전부 확정 |
| ⑤ | 정보 탭 기준값·출처 | **닫힘** — 위기경보 5단계·예비특보 25℃ 기관 원문 확보. 나머지 기준은 #88 |
| ⑥ | 지도 배경 출처·좌표 | **조사 닫힘** — OSM 교체·bbox 투영법 적용은 2주차 |
| ⑦ | 평년값 창 경계 | **닫힘** |
| ⑧ | 30분→1시간 | **닫힘** |
| ⑨ | 견본 E 선정 | **닫힘** — E `2020-09-22` · E′ `2021-09-13` |

### 다음에 할 일 — 순서대로

1. **⑨ 견본 E** — `anomaly` 음수인 날. §10 렌더 검사 18·19를 돌릴 근거가 없다
2. **⑤ 남은 둘** — 위기경보 다섯 단계 전부의 발령기준 · 특보 3단계 수온 기준.
   **「고수온·적조 종합대책」 본문**을 찾아야 한다 (CHANGES #88)
3. **⑥ 지도** — OpenStreetMap 교체 · bbox 투영법 (CHANGES 후보에 있음)
4. **2주차** — 화면 완성 · 골든 케이스 · 견본 D(실시간) · 갱신 파이프라인 · **검사기·렌더러**

---

## 7. 알려진 이슈 — 새 세션이 알고 있어야 할 것

### ⚠ §9 견본 A의 배경장이 재현되지 않는다

문서값 `12.3971` / 백분위 `61.0` / 표본 `172`가 **어느 규칙으로도 나오지 않고 전부 그 두 배쯤이다.**
§12:1496이 기록한 "그 값을 낸 코드가 어디에도 없었다"와 같은 자리다.
**추적은 멈추고 새 규칙으로 재산출했다.** §14에 정정으로 남긴다.

### ⚠ §13의 「배경장 개발용 17일 채택」은 거의 전부 계산 결함이었다

채택 13일 중 12일이 2020-05-15~05-26이고 백분위가 **전부 정확히 100.0**이었다.
그 구간의 평년 연도에 2017년이 하루도 기여하지 않아 기준선이 두 해로만 만들어졌고,
두 해의 누적편차는 정의상 0 근처에 모이므로 백분위가 100으로 튄 **구조적 산물**이다.
**C2 확정으로 사라졌고, 배경장은 개발용 221일 중 1일만 발동한다.**

**그래도 배경장을 지금 빼지 않는다.** §13이 사전 고정한 기준은 "채점용에서 판정한다"이고,
개발용 결과를 보고 빼면 §3이 막으려던 바로 그것이 된다.

### ⚠ 속보 원인 서술 4년치가 전부 열유입이고 배경장 계열은 0건이다

2022 "북태평양 고기압" · 2023 "폭염의 지속" · 2024 "지속되는 폭염" · 2025 "장마전선의 소멸 이후 폭염".
§13 대응표가 열유입으로 기울어 있다는 걱정이 실측으로 확인됐다.
**배경장 채택일은 구조적으로 전부 불일치가 된다.** 채점 전에 대응표 보강이 필요하다.

### ⚠ 분석 기간 9/30이 2024년을 놓친다

천수만 해제일이 **2024년 10/2**로 이틀 초과다. 8년 중 7년만 담는다.
앞쪽 5/15는 여유가 크다(가장 이른 첫 특보가 2022년 7/8).
**"특보 기간을 다 담는다"고 쓸 수 없다.**

### ⚠ 정보 화면의 위기경보가 4칸인데 실제는 5단계다

관심 · 주의 · 경계 · **심각Ⅰ** · **심각Ⅱ**. 그리고 다섯 단계 기준을 다 찾았다.
"모르는 것에 의미색을 칠하지 않는다"던 회색 결정의 전제가 바뀐다. **원문 대조 후 판단.**

### ⚠ 지도 — 국외반출 규제와 투영법

- **브이월드·국토지리정보원 자료를 GitHub(미국 서버)에 올리면 「측량성과 국외반출」에 걸린다.**
  저작권과 별개 규제다. **OpenStreetMap 권고**(ODbL, 재배포 명시 허용, 해외 원본)
- **§5의 `0.956:1`은 지상거리 비율이다.** 웹메르카토르 이미지에 §5 선형식을 쓰면
  최대 **143 m** 오차 — 관측점이 육지에 찍힌다. **EPSG:4326 렌더링 + 출력 비율 0.954**

### ⚠ 요인 막대 — 이 프로젝트에서 가장 까다로운 지점

- 진행 바가 아니라 **트랙 위의 점 하나**(지름 10px). 채우면 "기여도"로 읽힌다
- 임계선은 **점선**이고 **마커보다 위에, 마커 밖으로** 빼야 한다.
  견본 A의 **일사 88.6**은 임계 90에서 **1.4%p** 떨어져 트랙 334px 기준 **4.7px**이라
  **마커 반지름 5px 안에 들어와 선이 통째로 덮인다.**
  (기온 92.4는 2.4%p → 8.0px 로 겨우 벗어난다. `디자인적용지침.md:110` 의 「0.7%·2.6px」은
  **3.6 옛 창 값 90.7 에서 나온 수**이며 §14 #19 로 창 규칙이 바뀌어 더는 맞지 않는다)
- 임계선 색은 **`--wv-text`**. `--wv-border-strong`은 마커 위 대비가 2.11:1이라 안 보인다
- 스케일을 50~100으로 확대하지 않는다. 붙어 보이는 것이 정직하다

---

## 8. 작업 방식

- **사용자는 맥·터미널 초보다.** 명령은 **한 줄씩** 주고 **무엇을 하는 명령인지** 함께 적는다
- **한 번에 여러 갈래를 주지 않는다. 다음 한 걸음만.**
- 결정이 필요하면 **판단 기준을 숫자 보기 전에 고정**하고 기록에 적는다
- **인증키를 대화에 붙여넣지 않는다.** `.env`에서 읽어 쓴다
- 커밋 전 키 검사:
  ```
  git add -A && git diff --cached -U0 | grep -qF "$NIFS_KEY" && echo "⚠ 키" || git commit -m "..."
  ```

---

## 9. 오늘의 교훈 셋

1. **문서 안에서는 찾을 수 없는 오류가 있다.** 원자료로 내려가야 나온다.
   8/3에 나온 §14급 셋이 전부 그랬다.
2. **판단 기준을 먼저 고정하면 결론이 뒤집힌다.** ⑦에서 첫 조건(5%)만 있었으면
   A2가 열려 표본 44일짜리 판정을 할 뻔했다. 두 번째 조건이 막았다.
3. **차이의 크기와 화면의 변화는 다르다.** ⑧에서 "0.05℃ 넘는 날 2일"로 보면
   무시할 만했는데, 반올림 후 표시가 바뀌는 날은 **57일(9.0%)** 이었다.

---

## 9-2. PRD 3.8 반영 — **진행 중 (2026-08-04)**

**계획**: 107건(현재 111건)을 **의존 묶음 일곱**으로 나눠 **묶음당 커밋 하나**.
메시지에 `[n/7]` 이 있어 **커밋 이력만 보면 어디까지 됐는지 안다.**

| 묶음 | 무엇 | 커밋 | 상태 |
|---|---|---|---|
| **F1** | §0 창리·해역 · §1 RISA · §2 특보 이력 · §3 접촉 기록 | `5430428` | **완료** |
| **F2** | §5 위기경보 5단계·지점 표 · §6 중층 | `c720ada` | **완료** |
| **F3** | §12 인증키·API 실측·수집기 2단계 | `890fd34` | **완료** |
| **F4** | §13 2020년 · §14 #11 병기 · §16 1주차 결과 | `6643776` | **완료** |
| **A** | 시간 해상도 — 만점 48→24 | `f05c912` | **완료** |
| **B** | §7 평년값 — A1 · C2 · 배경장 · 43×n 이론값 | `904307c` | **완료** |
| **C** | §8 스키마 — `2.0` · enum 셋 · 실시간 필드 | `2db6d03` | **완료** |
| **E** | §13 채점 — C2 파급 숫자 | — | **남음** (11건) |
| **G** | §11·§8 화면 정직성 (#69·#70) | — | **남음** (12건) |
| **D** | §9 견본 다섯 · §8 JSON · §10 시험문장 | — | **남음** (14건) |

**남은 순서: E → G → 파일명 `git mv` → D → CHANGES 이력 이송.**
**D 가 가장 크고 앞의 전부가 값으로 수렴하는 자리**라 마지막이다.

### 지키고 있는 것 — 이어받을 때 꼭 볼 것

1. **`CHANGES.md` 머리말의 「진리 조건」 표를 먼저 읽는다.**
   §5·§7·§8 은 **새 값으로 덮고**, **§14 는 그때 값을 지키며 병기만** 한다.
   지금까지 §14 는 **한 글자도 덮어쓰지 않았다** — #11 에 괄호만 붙였고
   이력 표의 `87.3`·`90.7`·`31.6%`·`2.6px` 은 그대로다.
2. **묶음마다 코드로 대조한다.** B 에서 249/221/79 · 44일 · 31.7% · 3.2% · 53% 가
   `verify.py` 출력과 일치함을 확인했다. **커밋 전에 `evidence` 로 견본 다섯 재현**을 본다.
3. **파일명은 아직 `03_PRD_3.7.md` 다.** G 다음 D 직전에 `git mv` 하고
   참조 5곳(`인수인계.md` 2 · `CHANGES.md` 2 · `data/verify/…시간해상도.md` 1)을 고친다.
4. **스크립트로 여러 곳을 고칠 때는 항목마다 저장한다.** 한 번에 모아 쓰다가
   중간에 실패해 앞선 변경이 통째로 유실된 적이 있다.

---

## 10. 매일 첫 걸음 — 이 셋부터

0. **전날 수집분을 커밋·push 한다** — `data/realtime/fsch6-YYYY-MM.ndjson`.
   `launchd` 는 **git 을 건드리지 않는다**(PRD 작업 중에 끼어들지 않게 한 선택이다).
   그 뒷면이 **수집분이 이 맥에만 쌓인다**는 것이다.
   「영구 보관」을 정한 근거가 **「다시 얻을 수 없다」**였는데
   로컬에만 있으면 맥이 잘못됐을 때 그대로 사라진다. **하루 한 번은 올린다.**


1. **수집기가 밤새 쌓였는지 확인** — `data/realtime/fsch6-2026-08.ndjson`

   ```
   launchctl list | grep sst-explainer          # 등록돼 있나
   tail -5 data/realtime/collect.log            # 마지막 결과
   wc -l data/realtime/fsch6-2026-08.ndjson     # 하루면 48줄씩 는다
   ```

   **2026-08-04 10:33 부터 `launchd` 가 돈다.** GitHub Actions `schedule` 은
   16회 중 2회만 돌아 버렸다(자세한 것은 `data/realtime/README.md`).
   **맥이 자면 안 도는 것이 이 방식의 약점**이라 아침 확인이 계속 필요하다.

   안 늘었으면 `collect.log` 의 `결과:` 줄이 사유를 말한다.

2. **「건너뜀 → 실패」 이름 갈래 정하기** — 아직 미승인이다.
   지금은 정상(쌓음)과 사고(건너뜀)가 요약에서 낱말로 구분되지 않는다.
   제안: 모든 건너뜀을 **`실패 — 사유`** 로 부른다. 훑어보면 `실패`만 찾으면 된다.
   (중복 검사가 없으므로 「변화 없음 — 이미 있는 관측」이라는 정상 상태는 존재하지 않는다)

3. **2주차 시작** — 화면 완성 · 골든 케이스 · 견본 D(실시간) ·
   갱신 파이프라인 · **검사기·렌더러**.
   `CHANGES.md` 107건이 PRD 미반영이다. **반영 전에 머리말의
   「문서마다 진리 조건이 다르다」 표를 먼저 읽을 것** — §14 를 §5 처럼 고치면
   7/31 에 무슨 일이 있었는지가 사라진다.
