# 디자인 적용 지침

**Waves Design System v1.2.1 × PRD 3.5**
2026-07-31 · 프로토타입 작업용

---

## 이 문서가 필요한 이유

Waves는 잘 만들어진 시스템이지만 **범용 모바일 앱을 전제로 설계**돼 있습니다.
이 서비스는 전제가 다릅니다 — 중장년 어민이 **야외에서**, **특보를 받은 직후**,
**불안한 상태로** 봅니다. 그리고 PRD가 "색으로 상태를 표현하지 않는다",
"경보를 울리지 않는다"를 설계 원리로 못 박았습니다.

그냥 얹으면 다섯 곳에서 부딪힙니다. 오늘 결정하고 가는 편이 낫습니다.

---

# 1. 충돌 다섯과 결정

## 충돌 ① 다크가 기본이다 — 야외에서 안 보인다

Waves는 `:root`가 곧 다크입니다. 원본 Figma가 전부 어두운 그라디언트라서요.

**그런데 이 서비스의 사용 환경은 배 위, 양식장, 한여름 직사광선입니다.**
다크 화면은 야외에서 반사가 심해 거의 안 보입니다. PRD §4가 대상을
"중장년 이상"으로 잡은 것도 겹칩니다 — 대비가 낮으면 더 불리합니다.

> ### 결정
> **`<html data-theme="light">` 를 기본으로 쓴다.**
> Waves에 라이트 테마가 이미 완성돼 있으므로 비용이 0입니다.
> 다크는 나중에 토글로 붙일 수 있고, 지금 붙일 필요는 없습니다.

라이트 테마는 그림자 강도까지 따로 낮춰놨습니다(`--wv-shadow-lg` 재정의).
설계자가 이미 고민한 흔적이니 그대로 씁니다.

---

## 충돌 ② 상태 색이 PRD 원칙과 부딪힌다

Waves에는 `--warning`(주황) `--danger`(빨강) `--success`(초록)가 있고
`.wv-badge--warning` 같은 변형이 준비돼 있습니다. **`주의보 기준 도달` 배지에
주황을, `경보 기준 도달`에 빨강을 쓰고 싶어집니다.**

PRD가 이걸 두 곳에서 막고 있습니다.

| PRD | 내용 |
|---|---|
| §5 지도 | 색으로 수온의 높고 낮음을 표현하지 않는다 |
| §8 · §16 | 배지가 §0("경보를 울리지 않는다")과 긴장 관계다. **2주차에 재검토** |

빨간 배지는 사실상 경보입니다. 그리고 PRD가 지적했듯 우리 배지의 "기준 도달"은
공식 특보와 **다른 것을 재현**합니다(공식은 전망을 포함하고 해역 단위입니다).
빨강으로 칠하면 그 차이가 지워집니다.

> ### 결정
> **프로토타입에서는 다섯 단계 모두 `.wv-badge--neutral` 하나로 통일한다.**
> 구분은 **글자**로만 한다 — `특보 기준 미만` / `예비특보 기준 도달` /
> `주의보 기준 도달` / `경보 기준 도달` / `판정 보류`.
>
> 색은 2주차에 실제 화면을 보고 정한다. **넣었다가 빼는 것보다
> 없이 시작해서 필요하면 넣는 편이 안전하다** — 시연에서 "왜 빨갛냐"는
> 질문을 받으면 답이 궁색해집니다.

`--danger` 계열은 이 프로젝트에서 **쓸 곳이 없습니다.** 오류 화면에도
쓰지 마세요. 이 서비스에서 빨강은 "위험"으로 읽힙니다.

---

## 충돌 ③ `.wv-progress`를 요인 막대로 쓰면 안 된다

가장 쓰고 싶어지는 컴포넌트인데, **의미가 정반대입니다.**

| | `.wv-progress` | PRD의 요인 막대 |
|---|---|---|
| 뜻 | **채워진 양** (13% 진행됨) | **분포에서의 가로 위치** |
| 시각 | 왼쪽부터 채워진 바 | 트랙 위의 **점 하나** |

PRD가 화면에 이 문구를 상시 노출하기로 한 이유가 정확히 이겁니다.

> ⓘ 가로 위치는 평년 분포에서 얼마나 바깥인지를 나타냅니다. **원인 기여도가 아닙니다.**

진행 바로 그리면 "일사량이 13% 기여했다"로 읽힙니다.
**문구로 부인하는 것을 그림이 주장하게 됩니다.**

> ### 결정
> **오늘 만드는 유일한 신규 컴포넌트는 이것 하나다.**
> `.wv-progress`의 트랙만 재사용하고 채움 바 대신 **마커**를 얹는다.
> **사양은 PRD 3.6 §5 「요인 막대」 표와 §11 「요인 영역」을 따른다.**

```
      0                87.3  90          100
      ├─────────────────●──┊─────────────┤
                        ↑   ↑
                    마커    임계선
                        기준 상위 10%
```

| 요소 | 값 |
|---|---|
| 트랙 | `.wv-progress` 를 배경으로만. 높이 8px, `--wv-radius-pill` |
| 스케일 | 백분위 **0~100** 선형. 50~100으로 확대하지 않는다 |
| 마커 | 원, 지름 12px, `--wv-accent`. `left: calc({percentile}% - 6px)` |
| 임계선 | `adoption_threshold.percentile` 위치. **90을 하드코딩하지 않는다** |
| 임계선 라벨 | **`기준 상위 {N}%`** — N = `round(100 − adoption_threshold.percentile)` |

### 임계선은 마커보다 위에, 마커보다 길게

**3.6이 프로토타입에서 발견한 것입니다.** 견본 A의 기온은 백분위 90.7로
임계선(90)에서 **0.7%밖에 안 떨어져 있습니다.** 374px 화면에서 **2.6px**이라
지름 12px짜리 마커가 선을 통째로 덮습니다.

**임계선을 그렸는데도 §9가 견본 A를 고른 이유가 화면에서 사라집니다.**

```css
/* 마커 */ z-index: 1;
/* 임계선 */ z-index: 2; top: -4px; bottom: -4px;  /* 트랙 위아래로 삐져나오게 */
```

### 임계선 라벨을 `90`이라고 쓰지 않는 이유

막대 오른쪽에 이미 `상위 13%`가 적혀 있습니다. **백분위 90과 상위 13%는
커지는 방향이 반대**라 나란히 두면 비교가 안 됩니다.

| 요인 | 화면 | 읽히는 것 |
|---|---|---|
| 일사량 | `상위 13%` vs `기준 상위 10%` | 13 > 10 → **못 미친다** |
| 기온 | `상위 9%` vs `기준 상위 10%` | 9 < 10 → **넘었다** |

---

## 충돌 ④ 물결 애니메이션이 파고로 읽힌다

`.wv-waves--animated`는 이 키트의 시그니처입니다. 그런데 **바다 서비스에서
움직이는 파도는 "파고"나 "물결 상태"를 뜻하는 것으로 읽힙니다.**
우리는 수온만 다루고 파고는 다루지 않습니다.

그리고 §0이 "경보를 울리지 않는다"인데, 움직이는 요소는 시선을 끕니다.

> ### 결정
> | 화면 | 물결 |
> |---|---|
> | 지도 · 정보 | **정적 물결 OK** (`.wv-waves`, `--animated` 없이) |
> | **설명 화면** | **쓰지 않는다** |
>
> 설명 화면은 수치와 문장이 주인공입니다. 배경이 움직이면 안 됩니다.

`prefers-reduced-motion` 규칙은 파일에 이미 구현돼 있습니다(398행). 좋습니다.

---

## 충돌 ⑤ 폰트가 Google Fonts CDN이다

Waves는 `<head>`에 Google Fonts `<link>`를 요구합니다.
PRD는 **"인터넷이 끊겨도 화면 골격은 떠야 한다. 외부 CDN을 쓰지 않는다"** 입니다.

> ### 결정
> **오늘은 CDN을 쓴다.** 폰트가 안 떠도 폴백(`system-ui`)으로 화면은 뜨므로
> "골격이 뜬다"는 요건은 지켜집니다.
> **2주차에 셀프 호스팅으로 바꾼다** — woff2 파일을 저장소에 넣고
> `@font-face`로 교체. 30분이면 됩니다.

**단, `font-variant-numeric: tabular-nums`가 body에 이미 걸려 있습니다.**
수온이 `28.5℃`에서 `28.6℃`로 갱신될 때 자릿수가 흔들리지 않습니다.
실시간 화면에 중요한 설정이니 지우지 마세요.

---

# 2. 컴포넌트 매핑

PRD 화면 요소를 Waves 클래스에 대응시킨 표입니다. **오른쪽 열이 비어 있으면
새로 만들어야 하는 것**이고, 지금 비어 있는 건 요인 막대 하나뿐입니다.

## 공통

| PRD 요소 | Waves |
|---|---|
| 화면 골격 | `.wv-screen` > `.wv-screen__body` |
| 폭 제한 (414px) | `.wv-container` |
| 머리말 (`← 지도로` / `ⓘ 특보란?`) | `.wv-appbar` + `.wv-appbar__title` |
| 뒤로/정보 버튼 | `.wv-btn .wv-btn--ghost .wv-btn--sm` |
| 구획선 | `.wv-divider` |
| 세로 배치 | `.wv-stack .wv-gap-5` |
| 좌우 배치 | `.wv-row .wv-between` |

## 지도 화면

| PRD 요소 | Waves |
|---|---|
| 지점 목록 | `.wv-list` + `.wv-list-item` |
| 지점명 / 해역(추정) | `.wv-list-item__title` / `.wv-list-item__meta` |
| 설명 제공 · 준비 중 | `.wv-badge--neutral` (**색 구분 금지**) |
| 배경 물결 | `.wv-waves` (정적) |

> **지도를 SVG로 못 만들겠으면 목록으로 시작하세요.**
> `.wv-list`로 지점 6개를 세로로 놓아도 "6곳 중 1곳만 설명한다"는
> 메시지는 그대로 전달됩니다. 개념도는 나중에 얹으면 됩니다.

## 설명 화면

| PRD 요소 | Waves |
|---|---|
| 지점명 | `.wv-overline` |
| **수온 (28.5℃)** | `.wv-display` 또는 `.wv-title` + `.wv-mono` |
| 평년 대비 문구 | `.wv-body` |
| 상태 배지 | `.wv-badge--neutral` |
| 자료 시각 3줄 | `.wv-caption` |
| 요인 그룹 카드 | `.wv-card--glass` |
| 그룹 이름 (열유입 · 배경장) | `.wv-subtitle` |
| **요인 막대** | **신규 — 위 충돌 ③** |
| 상위 % | `.wv-mono` |
| 미채택 문구 | `.wv-caption` |
| ⓘ 안내 | `.wv-alert` (기본형, `--warning` 아님) |
| 조석 조건 | `.wv-dot` + `.wv-body` |
| [쉬운 말][자세히] | `.wv-tabbar` + `.wv-tab` |
| 문장 차단 문구 | `.wv-alert` |
| 보류 문구 | `.wv-alert` |
| 한계 블록 7줄 | `.wv-card--flat` + `.wv-caption` |

**수온은 `.wv-display`(50px)가 아니라 `.wv-title`(34~41px) 정도가 적당합니다.**
50px는 원본 Figma의 손글씨 타이틀 크기라 숫자에는 과합니다.
`font-variant-numeric: tabular-nums`가 이미 걸려 있으니 mono를 따로 안 써도 됩니다.

## 정보 화면

| PRD 요소 | Waves |
|---|---|
| 특보 3단계 박스 | `.wv-card--flat` |
| 위기경보 4단계 박스 | `.wv-card--flat` |
| "서로 짝지어지지 않습니다" | `.wv-alert` |
| 기준 확인일 | `.wv-caption` |
| 출처 링크 | `.wv-btn--ghost` |

**두 박스를 위아래로 놓습니다.** 나란히 놓으면 예비특보=관심으로
짝지어진 것처럼 읽힙니다(PRD §5가 명시한 것).

---

# 3. 오늘 결정해야 할 것 하나 더

**요인 막대에 90 임계선을 그릴 것인가.**

PRD §5 목업이 이렇습니다.

```
일사량 (7일)              상위 13%
├──────────────┊───●──────┤
기온 (7일)                 상위 9%
├──────────────┊──────●───┤
```

`┊`가 무엇인지 PRD에 정의가 없습니다. 그리고 **두 마커가 모두 `┊` 오른쪽에
그려져 있는데, 실제로는 일사(87.3)가 임계 90에 못 미치고
기온(90.7)만 넘습니다.**

이게 중요한 이유 — PRD §9가 견본 A를 고른 이유가 바로 이것입니다.

> 일사 하나만 보면 90에 못 미치는데(87.3) 기온이 넘어서(90.7)
> 그룹으로는 채택된다. **그룹 판정이 실제로 어떻게 도는지 보여준다.**

**임계선이 없으면 이 논지가 화면에서 안 보입니다.** 시연의 핵심 장면이
그림으로 뒷받침되지 않습니다.

> ### 권고
> **임계선을 그린다.** 90 위치에 세로 실선 하나.
> 그러면 일사 마커는 왼쪽, 기온 마커는 오른쪽에 놓여
> "하나는 못 미치고 하나는 넘었다"가 한눈에 보입니다.
>
> **스케일은 0~100 백분위 그대로 쓴다.** 87.3과 90.7은 3.4밖에 차이 나지 않아
> 막대에서 거의 붙어 보이는데, 그게 정직합니다. **판별은 임계선과 숫자가 하고,
> 막대는 인상만 줍니다.** 50~100으로 확대하면 차이가 실제보다 커 보입니다.

정하시면 PRD §5 목업과 §11에 한 줄 추가하세요.

---

# 4. Claude Code 프롬프트 (교체본)

작업계획서 STEP 3의 프롬프트를 이것으로 바꿉니다.

```
첨부 파일 두 개를 먼저 읽어줘.
- 03_PRD_3.6.md  (요구사항. 화면 문구는 전부 여기서 가져온다)
- waves-design-system.css  (디자인 시스템. 수정하지 말고 그대로 쓴다)

정적 사이트를 만든다. 빌드 도구 없이 HTML + CSS + 바닐라 JS.
GitHub Pages에 그대로 올린다.

■ 디자인 시스템 사용 규칙

waves-design-system.css는 절대 수정하지 않는다.
추가 스타일이 필요하면 assets/app.css 에 따로 쓰고, 거기서도
--wv-* 시맨틱 토큰만 참조한다. 색상값을 직접 쓰지 않는다.

1) 라이트 테마를 기본으로 쓴다. <html data-theme="light">
   야외에서 보는 서비스라 다크는 쓰지 않는다.

2) 상태 배지는 다섯 단계 모두 .wv-badge--neutral 하나로 통일한다.
   --warning / --danger / --success 계열을 어디에도 쓰지 않는다.
   PRD가 "색으로 상태를 표현하지 않는다"를 원칙으로 정했다.

3) .wv-progress 를 요인 막대로 쓰지 않는다. 의미가 반대다.
   요인 막대는 아래 사양대로 새로 만든다.

4) 물결(.wv-waves)은 지도·정보 화면에만, 애니메이션 없이 쓴다.
   설명 화면에는 쓰지 않는다.

5) 폰트는 Google Fonts <link> 를 쓰되, 폰트가 로드되지 않아도
   화면 골격이 그대로 뜨는지 확인한다.

■ 요인 막대 (신규 컴포넌트) — PRD §5 "요인 막대" 표와 §11 "요인 영역"을 따른다

트랙 위에 마커 하나와 임계선 하나를 놓는다. 채우지 않는다.

  - 트랙: 높이 8px, border-radius var(--wv-radius-pill),
          background var(--wv-surface-raised), 가로 100%
  - 스케일: 백분위 0~100 을 트랙 폭에 선형 대응. 50~100 으로 확대하지 않는다
  - 마커: 지름 12px 원, background var(--wv-accent),
          left: calc({percentile}% - 6px), z-index 1
  - 임계선: adoption_threshold.percentile 위치에 세로 1px 실선.
          90 을 하드코딩하지 말고 파일에서 읽어라.
          var(--wv-border-strong), z-index 2,
          트랙 위아래로 4px씩 삐져나오게 (top:-4px; bottom:-4px)
  - 임계선 라벨: "기준 상위 N%" (N = round(100 - adoption_threshold.percentile))
  - 막대 오른쪽: "상위 N%"
  - 막대 아래: 요인 값 + 단위

임계선이 마커보다 위(z-index)에 있어야 하고 마커보다 길어야 한다.
견본 A 기온은 백분위 90.7 로 임계선(90)에서 2.6px 밖에 안 떨어져 있어,
그렇게 하지 않으면 12px 마커가 선을 통째로 덮는다.
PRD §5 에 이 사고가 기록돼 있다.

렌더 후 반드시 눈으로 확인할 것:
  일사(87.3) 마커는 임계선 왼쪽,  기온(90.7) 마커는 임계선 오른쪽.
  이게 이 서비스의 핵심 장면이다. 반대로 나오면 잘못 만든 것이다.
  (PRD §5 목업의 ASCII 그림은 해상도 한계로 이 순서를 정확히
   못 그리고 있으니, 그림이 아니라 백분위 숫자를 기준으로 삼아라.)

■ 요인 영역 표시 규칙 (PRD §11)

  - 채택된 그룹이 하나라도 있으면: 모든 그룹을 display_order 순으로.
    채택 그룹은 막대를, 미채택 그룹은 "판정 기준에 못 미칩니다" 만
  - 채택된 그룹이 하나도 없으면: "평년을 크게 벗어난 요인이 없습니다."
    한 줄만. 그룹을 나열하지 않는다
  - stage 가 withheld 면: 요인 영역 전체를 판정 보류 문구로 대체
  - ⓘ 안내는 막대가 그려진 화면에서만 띄운다 (견본 B·C 에는 안 띄운다)

■ 보류일 처리 (PRD §8 "보류일에 담는 것과 담지 않는 것")

견본 C 는 current_sst, climatology_mean, anomaly 가 전부 null 이고
groups 와 conditions 가 빈 배열이다. 화면이 null 을 만나도 깨지지 않아야 한다.
수온 자리와 평년 대비 자리를 비우고 보류 문구를 띄운다.

■ 화면 세 개 — PRD §5 목업 그대로

1) index.html  지도
   - 지점 6곳. 창리만 "설명 제공", 나머지는 "설명 준비 중"
   - 준비 중 지점에는 수온을 표시하지 않는다 (중요)
   - 해역명은 "가로림만(추정)" 형태로. 여섯 곳 모두. 창리도 예외 아님 (PRD §5)
   - SVG 개념도가 어려우면 .wv-list 목록으로 먼저 만든다
   - 오른쪽 위 "ⓘ 특보란?" → info.html

2) station.html  설명
   - data/fsch6.json 을 fetch 해서 그린다
   - ?sample=b, ?sample=c 로 data/sample-b.json, sample-c.json
   - 왼쪽 위 "← 지도로", 오른쪽 위 "ⓘ 특보란?"

3) info.html  정보
   - 정적 텍스트. 특보 박스와 위기경보 박스를 위아래로 놓는다
   - "← 뒤로" 는 history.back()

■ 반드시 지킬 것

- 휴대폰 세로. .wv-container (414px)
- groups[].display_order 순서대로 그린다. 값 크기로 정렬하지 않는다
- adopted 가 false 인 그룹은 "판정 기준에 못 미칩니다" 만 쓴다.
  값이나 막대를 그리지 않는다
- stage 가 advisory 또는 warning 일 때만 연속일수 줄을 보여준다
- stage 가 withheld 면 요인 영역 대신 보류 문구를 띄운다
- explanation.blocked 가 true 면 PRD §11 의 차단 문구를 띄운다
- 한계 블록 7줄은 항상 노출한다
- 화면 문구는 전부 PRD §11 에서 가져온다. 임의로 쓰지 않는다

■ 숫자 변환 — 함수 하나로 만들어 세 화면이 공유한다

원값(_raw)에서 계산한다. 표기값(current_sst 등)을 다시 반올림하지 않는다.

  fmtValue(raw, unit)   원값을 소수 첫째 자리 사사오입 + 단위
  fmtTop(percentile)    "상위 " + Math.round(100 - percentile) + "%"
  fmtAnomaly(raw)       절댓값을 첫째 자리 반올림해
                        0.5 이상이면 "평년보다 N℃ 높습니다/낮습니다"
                        0.5 미만이면 "평년과 비슷합니다"
  fmtDate(iso)          "7월 31일"

사사오입이다. JavaScript 의 Math.round 는 음수에서 다르게 동작하므로
절댓값을 먼저 취하고 반올림한 뒤 부호를 붙인다.

이 함수들은 나중에 AI 문장 렌더러가 그대로 재사용한다 (PRD §10).
assets/format.js 로 분리해줘.

■ 확인

- 외부 CDN 은 폰트 하나뿐이다. 다른 스크립트를 불러오지 않는다
- 세 화면이 서로 오갈 수 있다
- ?sample=b 에서 "평년을 크게 벗어난 요인이 없습니다" 가 보인다
```

---

# 5. 오늘 끝나고 볼 것

작업계획서 STEP 4에 이 세 줄을 더합니다.

- [ ] **`--danger` · `--warning` · `--success` 가 결과물 어디에도 없다**
      (`grep -r "danger\|warning\|success" *.html assets/app.css` 로 확인)
- [ ] **요인 막대에서 일사 마커가 임계선 왼쪽, 기온 마커가 오른쪽에 있다**
- [ ] **폰트 링크를 지워도 화면이 무너지지 않는다**
